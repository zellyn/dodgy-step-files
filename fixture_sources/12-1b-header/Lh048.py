"""Lh048 — Reader cannot consume non-seekable stream (well-formed content).

Catalog claim: on-disk file content is well-formed; the defect is in the
reader's I/O contract (no stream entry point). We emit a small valid
MANIFOLD_SOLID_BREP-equivalent body so byte assertions pass; the
non-seekable failure mode lives outside the bytes.
"""
from step_corpus.step_builder import StepFile

f = StepFile(catalog_id="Lh048",
             defect="reader API takes only filesystem path; content is well-formed")

orig = f.cartesian_point((0.0, 0.0, 0.0))
zdir = f.direction((0.0, 0.0, 1.0))
xdir = f.direction((1.0, 0.0, 0.0))
plc = f.axis2_placement_3d(orig, zdir, xdir)
plane = f.plane(plc)

def line_edge(p, dir_tuple, length, va, vb):
    d = f.direction(dir_tuple); vec = f.vector(d, length); ln = f.line(p, vec)
    return f.edge_curve(va, vb, ln)

p0 = f.cartesian_point((0.0, 0.0, 0.0))
p1 = f.cartesian_point((1.0, 0.0, 0.0))
p2 = f.cartesian_point((1.0, 1.0, 0.0))
p3 = f.cartesian_point((0.0, 1.0, 0.0))
v0 = f.vertex_point(p0); v1 = f.vertex_point(p1)
v2 = f.vertex_point(p2); v3 = f.vertex_point(p3)
e0 = line_edge(p0, (1.0, 0.0, 0.0), 1.0, v0, v1)
e1 = line_edge(p1, (0.0, 1.0, 0.0), 1.0, v1, v2)
e2 = line_edge(p2, (-1.0, 0.0, 0.0), 1.0, v2, v3)
e3 = line_edge(p3, (0.0, -1.0, 0.0), 1.0, v3, v0)
loop = f.edge_loop([
    f.oriented_edge(e0, True), f.oriented_edge(e1, True),
    f.oriented_edge(e2, True), f.oriented_edge(e3, True),
])
face = f.advanced_face([f.face_outer_bound(loop)], plane)
shell = f._emit_raw(f"CLOSED_SHELL('outer',(#{face.eid}))")
f._emit_raw(f"MANIFOLD_SOLID_BREP('valid_content',#{shell.eid})")
f.add_product_chain(f.shell_based_surface_model([]))
