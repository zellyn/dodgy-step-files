ISO-10303-21;
/*
 * P026 — Global OCCT unit setting contaminated across in-process clients
 *
 * ILLUSTRATIVE FIXTURE ONLY — this defect is NOT falsifiable from a single
 * .stp file. The underlying defect is a runtime / process-state interaction:
 * gmsh's `Geometry.OCCTargetUnit` setting persists into FreeCAD's STEP
 * export when both are loaded into the same Python process. To actually
 * reproduce, you must run a Python session that imports both gmsh and
 * FreeCAD/OCCT in sequence with the unit-setting middle step, then export
 * STEP — the export will be 1000x scaled.
 *
 * This file simply *depicts* the resulting artifact (unit context declares
 * MILLIMETRE but coordinates are emitted at 1000x, as if the values were
 * metres). CI validation classifies this entry as PROCESS-STATE — out of
 * scope for static-file oracles.
 *
 * Defect (FreeCAD #18980; FreeCAD <-> gmsh interop):
 *   After gmsh's OCCT instance sets `Geometry.OCCTargetUnit=M`, subsequent
 *   FreeCAD STEP exports emit coordinates scaled x1000 because the OCCT
 *   global unit setting was mutated.
 * Reproducer (process-level): not a file-content recipe. See catalog Notes.
 * Expected kernel: STEP export sets unit context per-call without inheriting
 *   global state.
 *
 * NOTE: this file's coordinates are 1000x what the geometric design intended.
 *   It is otherwise structurally valid — the symptom is purely numerical.
 */
HEADER;
FILE_DESCRIPTION(('P026 unit context says MM but coordinates are 1000x — global-state contamination'),'2;1');
FILE_NAME('P026.stp','2026-04-26T00:00:00',('catalog'),('cad'),'manual','','');
FILE_SCHEMA(('AUTOMOTIVE_DESIGN { 1 0 10303 214 1 1 1 1 }'));
ENDSEC;
DATA;
#10 = APPLICATION_CONTEXT('automotive design');
#11 = APPLICATION_PROTOCOL_DEFINITION('international standard','automotive_design',2001,#10);
#12 = PRODUCT_CONTEXT('',#10,'mechanical');
#13 = PRODUCT_DEFINITION_CONTEXT('part definition',#10,'design');
#20 = PRODUCT('P026_ASSY','','',(#12));
#21 = PRODUCT_DEFINITION_FORMATION('','',#20);
#22 = PRODUCT_DEFINITION('design','',#21,#13);
#23 = PRODUCT_DEFINITION_SHAPE('','',#22);
#30 = PRODUCT('CUBE','intended 10mm cube — exported as 10000mm','',(#12));
#31 = PRODUCT_DEFINITION_FORMATION('','',#30);
#32 = PRODUCT_DEFINITION('design','',#31,#13);
#33 = PRODUCT_DEFINITION_SHAPE('','',#32);
#40 = NEXT_ASSEMBLY_USAGE_OCCURRENCE('1','CUBE_INST','',#22,#32,$);
/* Unit context: MILLIMETRE */
#50 = ( LENGTH_UNIT() NAMED_UNIT(*) SI_UNIT(.MILLI.,.METRE.) );
#51 = ( NAMED_UNIT(*) PLANE_ANGLE_UNIT() SI_UNIT($,.RADIAN.) );
#52 = ( NAMED_UNIT(*) SI_UNIT($,.STERADIAN.) SOLID_ANGLE_UNIT() );
#53 = UNCERTAINTY_MEASURE_WITH_UNIT(LENGTH_MEASURE(0.001),#50,'distance_accuracy_value','');
#54 = ( GEOMETRIC_REPRESENTATION_CONTEXT(3) GLOBAL_UNCERTAINTY_ASSIGNED_CONTEXT((#53))
        GLOBAL_UNIT_ASSIGNED_CONTEXT((#50,#51,#52)) REPRESENTATION_CONTEXT('','3D') );
/* DEFECT: design intent was a 10 mm cube, but coordinates are emitted at
   10000 mm because OCCT global unit got mutated to METRE. */
#60 = CARTESIAN_POINT('',(0.0,0.0,0.0));
#61 = DIRECTION('',(0.0,0.0,1.0));
#62 = DIRECTION('',(1.0,0.0,0.0));
#63 = AXIS2_PLACEMENT_3D('cube_origin',#60,#61,#62);
#90 = CARTESIAN_POINT('cube_corner',(10000.0,10000.0,10000.0));
#91 = AXIS2_PLACEMENT_3D('cube_corner_axes',#90,#61,#62);
#64 = SHAPE_REPRESENTATION('cube_rep',(#63,#91),#54);
#65 = SHAPE_DEFINITION_REPRESENTATION(#33,#64);
#66 = REPRESENTATION_MAP(#63,#64);
#67 = MAPPED_ITEM('inst',#66,#63);
#80 = AXIS2_PLACEMENT_3D('assy_origin',#60,#61,#62);
#81 = SHAPE_REPRESENTATION('assy_rep',(#80,#67),#54);
#82 = SHAPE_DEFINITION_REPRESENTATION(#23,#81);
/* Extended catalog notes:
 * This reproducer is a minimal demonstrator of the §12.6 entry
 * referenced in the /* * / header above. The entity-graph above
 * is just enough to exercise the defect on a writer or reader
 * test harness; it is NOT a production-quality assembly.
 *
 * License: this file is part of a defect catalog assembled from
 * publicly described bug reports and recommended-practice notes.
 * No proprietary geometry or names are reproduced. The entity IDs
 * and minimal coordinates are author-original.
 *
 * Cross-refs: see STEP_PROBLEM_CATALOG.md §12.6 for full entry,
 * source list, sender attribution and expected kernel behavior.
 */
#900 = DESCRIPTIVE_REPRESENTATION_ITEM('catalog_id','P026');
#901 = DESCRIPTIVE_REPRESENTATION_ITEM('catalog_section','12.6 assembly hierarchy');
#902 = DESCRIPTIVE_REPRESENTATION_ITEM('catalog_status','pending');
#903 = DESCRIPTIVE_REPRESENTATION_ITEM('catalog_target_dir','step-examples/12-6-assembly/');
#904 = DESCRIPTIVE_REPRESENTATION_ITEM('catalog_corpus_origin','phase-04-resume-12-6 gap fill');
#905 = DESCRIPTIVE_REPRESENTATION_ITEM('catalog_authoring','manual reproducer');
ENDSEC;
END-ISO-10303-21;
