ISO-10303-21;
/* Ad105 - .stpz / .stpx archive container claims gigabytes of uncompressed data
 * Defect: a ZIP / gzip archive whose entry header advertises gigabytes
 * of uncompressed payload from a few KB compressed bytes (decompression
 * bomb). The defect lives in the archive wrapper, not the embedded
 * STEP body. This fixture shows only the embedded body; the actual
 * exploit is the wrapper's compressed-vs-uncompressed-size ratio.
 * Provenance tier: cross-file-state. */
HEADER;
FILE_DESCRIPTION(('Ad105 - .stpz archive bomb (witness only - bomb in wrapper)'),'2;1');
FILE_NAME('Ad105.stp','2026-04-30T00:00:00',(''),(''),'cad-research-suite','','');
FILE_SCHEMA(('AUTOMOTIVE_DESIGN { 1 0 10303 214 1 1 1 1 }'));
ENDSEC;
DATA;
/* Witness body: archive-bomb wrapper not present in this Part-21 fixture;
   the bug-reporter searchable terms are in this file's header. */
#1=APPLICATION_CONTEXT('automotive_design');
#10=CARTESIAN_POINT('benign',(0.0,0.0,0.0));
ENDSEC;
END-ISO-10303-21;
