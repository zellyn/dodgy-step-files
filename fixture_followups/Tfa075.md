---
fixture_id: Tfa075
defect_class: ShapeFix_ComposeShell.MakeFacesOnPatch_at2981
v3_record: OCCT_HEAL_COVERAGE_V3.md (search "MakeFacesOnPatch_at2981")
occt_source: src/ModelingAlgorithms/TKShHealing/ShapeFix/ShapeFix_ComposeShell.cxx
occt_lines: 2981-3271
occt_ref: master (unpinned — backfill hash on next visit)
status: weak-valid
attempts: 1
last_touch: 2026-06-15
---

## OCCT source pointer + prose

- **Path**: `src/ModelingAlgorithms/TKShHealing/ShapeFix/ShapeFix_ComposeShell.cxx`
- **Line range**: 2981–3271 (specifically the fast-path check at the top of MakeFacesOnPatch that short-circuits when only one loop is present).
- **Branch (prose-laundered)**: When MakeFacesOnPatch receives a single loop input, it has a fast-path that early-returns. The fast-path only handles the case where that single loop is a WIRE — but the check is written so that *any* single-loop input gets promoted as a face, including residual non-WIRE loop types (VERTEX_LOOP or polyloops). For non-WIRE loops, the promotion produces an ill-formed face because the loop structure doesn't carry the topology the downstream consumer expects.

## Attempts

### Attempt 1 (wave 1) — WEAK_VALID

- **Approach**: Patch with a single WIRE loop, passed through MakeFacesOnPatch to exercise the fast-path.
- **Result**: The WIRE-loop case actually works correctly (no bug). The defect target is specifically the *non-WIRE* loop case (VERTEX_LOOP for example). Fixture exercises the fast-path's check but not the buggy branch within it.

## Why it's hard

VERTEX_LOOP is an obscure Part-21 entity (a "loop" consisting of a single VERTEX — used in some degenerate topology cases). Constructing a face that legitimately uses VERTEX_LOOP as a boundary is uncommon outside of degenerate sphere or torus pole representations. Need to find a setup where the healer would naturally encounter a VERTEX_LOOP being passed through MakeFacesOnPatch.

## Next directions

- Construct a fixture with an `OPEN_SHELL` containing a face that has a `VERTEX_LOOP` as its `FACE_BOUND` (instead of an `EDGE_LOOP`). VERTEX_LOOP is in the schema but rarely used.
- Alternative: use a `POLY_LOOP` — also a non-WIRE loop type.
- Look at how MakeFacesOnPatch is invoked upstream to see what loop type might legitimately reach it as a single non-WIRE entry — that's the natural fixture pattern.
- Consider whether the bug is even reproducible from a passive file (the non-WIRE case might only arise mid-pipeline) → potential `kernel-test-pair` if so.
