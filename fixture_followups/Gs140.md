---
fixture_id: Gs140
defect_class: ShapeAnalysis_Surface.IsVClosed bezier-pole-coincidence
v3_record: OCCT_HEAL_COVERAGE_V3.md (search "IsVClosed")
occt_source: src/ModelingAlgorithms/TKShHealing/ShapeAnalysis/ShapeAnalysis_Surface.cxx
occt_lines: (backfill)
occt_ref: master
status: invalid
attempts: 1
last_touch: 2026-06-17
---

## OCCT source pointer + prose

Branch (prose-laundered): IsVClosed uses a knot-multiplicity shortcut
to declare a Bezier surface V-closed when the first and last V-poles
coincide. The shortcut bypasses an explicit geometric check; a Bezier
with pole-coincidence in V is declared closed even when the parametric
behavior across V is not periodic.

## Attempts

### Attempt 1 (wave 55, 2026-06-17) — INVALID

- **Approach**: Bezier surface with coincident first/last V-poles + non-periodic
  knot vector.
- **Refutation**: Fixture has `#70=LINE('',#60,#70)` — circular self-reference
  (LINE entity refs itself as the direction-vector). Parser rejects before
  IsVClosed is reached. Also single-edge EDGE_LOOP at #73.

## Why it's hard

The pole-coincidence pattern requires a Bezier surface (rational or
not), not just a LINE/EDGE topology. The synthesis agent generated
both surface scaffolding AND boundary topology, but the topology
turned circular at LINE construction.

## Next directions

- Regenerate with explicit `VECTOR` entity between LINE and DIRECTION:
  ```
  #60=DIRECTION('',(1.0,0.0,0.0));
  #61=VECTOR('',#60,1.0);
  #70=LINE('',#10,#61);
  ```
- Ensure EDGE_LOOP has at least 3 edges forming a closed cycle.
- Reuse Gs100 (or another clean Gs1xx) as topology template.
