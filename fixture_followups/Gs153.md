---
fixture_id: Gs153
defect_class: ShapeAnalysis_Surface.CalcMaxDegree-knot-span-degeneracy
v3_record: OCCT_HEAL_COVERAGE_V3.md (search "CalcMaxDegree")
occt_source: src/ModelingAlgorithms/TKShHealing/ShapeAnalysis/ShapeAnalysis_Surface.cxx
occt_lines: (backfill)
occt_ref: master
status: weak-valid
attempts: 1
last_touch: 2026-06-17
---

## OCCT source pointer + prose

Branch (prose-laundered): CalcMaxDegree computes the effective B-spline
degree from knot multiplicities. The check should validate that
`sum(knot_multiplicities) == n_poles + degree + 1` per parametric
direction; when this invariant is violated, the algorithm silently
returns an incorrect effective degree instead of raising a
diagnostic.

## Attempts

### Attempt 1 (wave 58, 2026-06-17) — WEAK_VALID

- **Approach**: B_SPLINE_SURFACE_WITH_KNOTS with intentionally
  mismatched knot multiplicities (4 knot values × multiplicity 1 = 4,
  but with 3 poles + degree 2 in each direction the required sum is
  3+2+1 = 6).
- **Refutation**: The fixture IS the defect (intentional knot-sum
  violation to test whether CalcMaxDegree catches it), but whether the
  file even loads depends on parser tolerance:
  - If OCCT's STEP reader validates knot-sum at parse time → file
    rejected → CalcMaxDegree never invoked → demo fails
  - If reader tolerates → CalcMaxDegree runs → can observe whether the
    bug fires
  - Adversarial reviewer flagged INVALID because the geometry violates
    Part-21 B-spline schema; correct, but that violation IS the defect.

## Why it's hard

This is a "parser-strict field defect": the fixture has to intentionally
violate a schema invariant that the bug's *target code* is supposed to
catch but doesn't. The fixture's success depends on the parser layer
being more permissive than the analyser layer — which is the very
mismatch the defect represents.

## Next directions

- Test OCCT's actual STEP reader behaviour: does it reject this file
  at parse, or load it for downstream analysis?
- If parser is strict: convert to `kernel-test-pair` where the
  malformed B-spline is constructed via runtime API (bypassing the
  STEP parser entirely) and CalcMaxDegree is called directly.
- If parser is permissive: fixture stands as WEAK_VALID and the
  catalog entry should explicitly note "parser must tolerate this
  malformation for the bug to be observable".
