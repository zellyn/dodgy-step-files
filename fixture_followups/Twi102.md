---
fixture_id: Twi102
defect_class: ShapeFix_Wire.FixDegenerated_at2131
v3_record: OCCT_HEAL_COVERAGE_V3.md (search "ShapeFix_Wire.FixDegenerated_at2131")
occt_source: src/ModelingAlgorithms/TKShHealing/ShapeFix/ShapeFix_Wire.cxx
occt_lines: 2131-2204
occt_ref: master (unpinned — backfill hash on next visit)
status: weak-valid
attempts: 2
last_touch: 2026-06-15
---

## OCCT source pointer + prose

- **Path**: `src/ModelingAlgorithms/TKShHealing/ShapeFix/ShapeFix_Wire.cxx`
- **Line range**: 2131–2204 (OCCT master at the time of the v3 deep-pass; hash to be backfilled on next visit)
- **Branch (prose-laundered, NOT a code copy)**: Around line 2131 the FixDegenerated implementation iterates wire edges and, when it needs to insert a degenerate edge at the wire's tail, computes an insertion index using modular arithmetic against the current edge count. The intended behaviour is "wrap from index n back to index 0", but the modulo expression can produce an index that doesn't match the implicit assumption downstream code makes about cycle direction, producing an invalid topology.

## Attempts

### Attempt 1 (wave 1, 2026-06-15) — REFUTED

- **Approach**: 3-edge sequence P0→P1→P2→P2 with the third edge degenerate (zero-length P2→P2 self-loop).
- **Refutation**: Wire was not topologically closed (P2 ≠ P0). IsReady() rejects the wire before reaching line 2131 modulo logic.

### Attempt 2 (wave 2, 2026-06-15) — WEAK_VALID

- **Approach**: 4-edge closed loop P0→P1→P2→P3→P0 with E4 degenerate at the wire-end boundary (P3==P0, zero-length).
- **Result**: Adversarial verifier says the geometry is now syntactically reachable but the actual line-2131 wraparound bug doesn't fire because `(3 % 4 = 3)` lands safely without wrapping. The bug requires an index value of exactly `n` or higher (so the modulo "wraps from n back to 0").

## Why it's hard

The bug is in modular arithmetic at the wire boundary. To force `n3 == n`, the algorithm has to be in a state where the natural index would have been `n` (one past the last edge). That state is normally only reached when the algorithm has *already* iterated past the wire, which is the symptom we're trying to demonstrate but also the precondition. Hard to set up from a static file without driving the algorithm through a specific call sequence.

## Next directions

- Try a wire with the degenerate edge at position 0 (first edge) instead of the tail — that changes how the index arithmetic interacts with the wraparound.
- Try a 5+ edge wire where the degenerate sits at the boundary between two ranges the algorithm processes separately.
- Re-read the source around line 2131 to identify the exact precondition for the wraparound case; the v3 record may have understated which index value triggers it.
- Consider whether this is actually a `kernel-test-pair` candidate (the bug surfaces only under a specific FixDegenerated call sequence, not from passive load).
