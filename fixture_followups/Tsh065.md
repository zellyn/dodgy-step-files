---
fixture_id: Tsh065
defect_class: tessellated-shell-mixed-node-lists
v3_record: OCCT_HEAL_COVERAGE_V3.md (search "Tsh065" or related tessellation classes)
occt_source: src/ModelingAlgorithms/TKShHealing/ — tessellation reading layer
occt_lines: (backfill on next visit)
occt_ref: master (unpinned)
status: weak-valid
attempts: 1
last_touch: 2026-06-16
---

## OCCT source pointer + prose

Branch (prose-laundered): a tessellated shell can carry per-face node
lists that disagree about shared-edge node identity. The reader is
supposed to detect inconsistency between adjacent face's node lists
along their shared edge, but the check is structural-only and won't
fire when geometry is minimal (e.g. two triangles).

## Attempts

### Attempt 1 (regen 2026-06-16) — WEAK_VALID

- **Approach**: Tessellated shell with 2 triangles per face, intentionally
  inconsistent node-list ordering at the shared edge.
- **Result**: Adversarial review found structure correct (PRODUCT chain
  present, Part-21 clean), but the connectivity ambiguity is in
  comments rather than self-evidently broken in the geometry. The
  reader-level node-list check needs more triangles to produce a
  detectable mismatch.

## Why it's hard

Tessellated-shell defects depend on the reader's specific consistency
check; a 2-triangle fixture has too few node-list entries to
unambiguously demonstrate the disagreement.

## Next directions

- Rebuild with 4+ triangles per face so the shared edge has multiple
  intermediate nodes; mismatch one of them between the two faces.
- Document expected reader behavior in the catalog entry more
  explicitly so a future regen knows what "demonstrably wrong"
  looks like.
- Consider whether this is actually a `kernel-test-pair` candidate
  — the bug surfaces only when a specific reader API is invoked on
  the file, not from passive load.
