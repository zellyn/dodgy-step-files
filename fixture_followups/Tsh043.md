---
fixture_id: Tsh043
defect_class: regen-recovery (file truncated mid-construction)
status: invalid
attempts: 1
last_touch: 2026-06-17
---

## Issue

File is 25 lines, contains only the PRODUCT chain tail (#9000-#9023) and
references entities #82, #9005, #9010 that are not defined in the file.
The regen agent produced a truncated output during one of the haiku-batch
recoveries.

The shell `#9021=SHELL_BASED_SURFACE_MODEL('sbsm',(#82));` references face
`#82` but no face entity is declared.

## Next directions

- Regenerate against the canonical Tsh100 exemplar with full topology.
- Likely a haiku batch-truncation case; pair with a sibling that completed
  fully to use as the topology template.

## Lint rule candidate

Add an "undefined #-reference" check to construction_lint that flags any
`#N` reference where N is not declared as `#N=Type(...);` somewhere in the
same DATA section. Would have caught this at synthesis time.
(In scope for a separate review — not implemented in this autonomous run.)
