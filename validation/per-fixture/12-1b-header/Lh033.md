# Lh033 — Multi-DATA-section file with cross-section reference via `@section_name#NNN`

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh033.stp`](../../step-examples/12-1b-header/Lh033.stp)  

## Sources
- 16-iso-spec ISO 10303-21 Ed.3 §7 (multi-section data, cross-section reference syntax)

## Description

Edition-3 introduces named DATA sections (`DATA('section_name')`) and cross-section references of the form `@section_name#NNN` to refer to instance NNN inside a different section. Edition-2 readers (the majority) recognise only `DATA;` (no name) and a single anonymous data section. Such readers either ignore the section name (collapsing all instances into one global pool, where `#NNN` may collide between sections) or choke on the `@` syntax.

## Reproducer recipe

two named DATA sections `'geometry'` and `'pmi'`; an instance in `'pmi'` references `@geometry#2`.

## Expected kernel behavior

parse named DATA sections and resolve `@name#NNN` cross-references against the named section; reject duplicate section names. Edition-2-only kernels should reject the file rather than silently collapse handles.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

**See also**: Lh024.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `reject` |
| `occt_heal_off` | `reject` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_NO_DATA)` |
| `manifold` | `step_read_failed` |
| `occt_diag_on` | 1:**** ERR StepFile : Undefined Parsing: Line 34: Incorrect... |
| `occt_diag_off` | 1:**** ERR StepFile : Undefined Parsing: Line 34: Incorrect... |
| `gmsh_diag` | 1:**** ERR StepFile : Undefined Parsing: Line 34: Incorrect... |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=reject/reject gmsh=reject ifc=schema_n/a` |

## Cross-references

**See also**: [Lh024](Lh024.md)
