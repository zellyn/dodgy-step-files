# Lh042 — SIGNATURE section appearing before DATA (signature-over-empty-content)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh042.stp`](../../step-examples/12-1b-header/Lh042.stp)  

## Sources
- 16-iso-spec ISO 10303-21 Ed.3 §9 (SIGNATURE position)

## Description

Edition-3 places SIGNATURE near end-of-file by convention so the signed content is fully known at signature time. A producer placing SIGNATURE before DATA either signed an empty file or computed the signature over content not yet emitted. Receivers must reject as inconsistent or warn that the signature does not cover the full payload.

## Reproducer recipe

HEADER → SIGNATURE → DATA → END (SIGNATURE before DATA).

## Expected kernel behavior

reject section ordering where SIGNATURE precedes DATA, or, in tolerant mode, document that the signature covers only the prefix.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT rejects (`reject`). OCC enforces stricter framing than the spec text requires.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `reject` |
| `occt_heal_off` | `reject` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `step_read_failed` |
| `occt_diag_on` | 1:**** ERR StepFile : Undefined Parsing: Line 23: Incorrect... |
| `occt_diag_off` | 1:**** ERR StepFile : Undefined Parsing: Line 23: Incorrect... |
| `gmsh_diag` | 1:**** ERR StepFile : Undefined Parsing: Line 23: Incorrect... |

**Cross-oracle classification**: OCC stricter than spec — pure-Python accepts

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT rejects (`reject`). OCC enforces stricter framing than the spec text requires.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=reject/reject gmsh=reject ifc=schema_n/a` |
