# Lh007 — Multiple schema names listed inside a single FILE_SCHEMA

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh007.stp`](../../step-examples/12-1b-header/Lh007.stp)  
**Sender**: producers targeting multiple AP versions (e.g. AP203 + AP214)  

## Sources
- 06-nist-sfa S012, 11-translator-vendors W047

## Description

`FILE_SCHEMA` carries `('AP203_CONFIG_CONTROL_DESIGN','AUTOMOTIVE_DESIGN')`. Most receivers consume only the first; some pick the most-capable; behavior is implementation-defined and divergent.

## Reproducer recipe

`FILE_SCHEMA(('AP203_CONFIG_CONTROL_DESIGN','AUTOMOTIVE_DESIGN'));` or with embedded comments inside the parentheses.

## Expected kernel behavior

warn; pick the most-capable supported schema deterministically; do not crash. Document which one was chosen.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- disallowed: `crash`

## Notes

Validation observed: silent-empty (no shape produced) rather than the cited divergent-pick behavior. Specific divergent-pick code path may not be exercised at fixture scale; the kernel-mishandling-by-silent-acceptance still demonstrates the defect class (implementation-defined behavior on multi-schema lists). **See also**: Lh006.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **OCC behavior**: silent-empty (no shape produced) rather than the cited divergent-pick behavior. Specific divergent-pick code path may not be exercised at fixture scale; the kernel-mishandling-by-silent-acceptance still demonstrates the defect class (implementation-defined behavior on multi-schema lists).

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Lh006](Lh006.md)
