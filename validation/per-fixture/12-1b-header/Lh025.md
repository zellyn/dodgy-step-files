# Lh025 — Mixing `#NNN` (entity) and `@NNN` (value) namespaces (Ed.3)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh025.stp`](../../step-examples/12-1b-header/Lh025.stp)  

## Sources
- 16-iso-spec N035

## Description

Edition 3 introduces `@NNN` value-instance identifiers (e.g. `@1=POSITIVE_LENGTH_MEASURE(...)`) alongside `#NNN` entity-instance identifiers. The two integer namespaces are disjoint: `#1` and `@1` are distinct instances. Files that put a `@NNN` value-instance reference where a `#NNN` entity-instance reference is required (or vice versa), or that use the `@`-prefix syntax in plain Edition-2 contexts, break readers that collapse the two namespaces into one map and silently merge unrelated objects.

## Reproducer recipe

`#1=PRODUCT(...); @1=POSITIVE_LENGTH_MEASURE(2.5); #2=ITEM_DEFINED_TRANSFORMATION('',$,@1);`

## Expected kernel behavior

maintain two separate dispatch tables; reject if an attribute slot expects `#NNN` but the value reference is `@NNN` (or vice versa).

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**Originally also catalogued as**: Ls040.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 27) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references


**Originally also catalogued as**: Ls040
