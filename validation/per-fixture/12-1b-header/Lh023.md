# Lh023 — Whitespace, tab, or comment between `#` and digits of an instance ID

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh023.stp`](../../step-examples/12-1b-header/Lh023.stp)  

## Sources
- 07-stepcode-ifcopenshell T018, 16-iso-spec N030

## Description

stepcode tolerates `# 12 = ENT(...)` and even `# /*c*/ 12 = ENT(...)`; the IfcOpenShell Lark grammar rejects whitespace between `#` and digits. ISO 10303-21 §5.6 declares `#NNN` a single token, so spaces inside it are non-conformant. Treating tab as a separator inside an identifier produces silent corruption (`#1<TAB>2` becomes `#12`).

## Reproducer recipe

`# 12 = IFCPERSON(...);` or `# /* note */ 12 = IFCPERSON(...);` or `#1\t2 = …`.

## Expected kernel behavior

strictly reject whitespace/comment inside `#NNN`; tolerant readers may accept whitespace between tokens but never inside identifiers, and must warn rather than silently merge digits.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

**Originally also catalogued as**: Ls039. **See also**: Ls038.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Ls038](Ls038.md)

**Originally also catalogued as**: Ls039
