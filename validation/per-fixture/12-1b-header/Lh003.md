# Lh003 — Missing `ENDSEC;` between sections

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh003.stp`](../../step-examples/12-1b-header/Lh003.stp)  

## Sources
- 07-stepcode-ifcopenshell T039, 16-iso-spec N049

## Description

A `HEADER`/`DATA`/`ANCHOR`/`REFERENCE`/`SIGNATURE` section runs into the next section keyword without an intervening `ENDSEC;` (or an `ENDSEC` token without trailing semicolon). Confuses parsers expecting strict bracketing.

## Reproducer recipe

`HEADER;...FILE_SCHEMA(('IFC4'));DATA;#1=A();END-ISO-10303-21;` (no `ENDSEC;` before `DATA;`); or `HEADER;...ENDSEC<newline>DATA;` (semicolon missing).

## Expected kernel behavior

reject with line-numbered diagnostic naming the offending section.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**Originally also catalogued as**: Ls021, Ad040.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `reject` |
| `occt_heal_off` | `reject` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_NO_DATA)` |
| `manifold` | `step_read_failed` |
| `occt_diag_on` | 1:**** ERR StepFile : Undefined Parsing: Line 37: Incorrect... |
| `occt_diag_off` | 1:**** ERR StepFile : Undefined Parsing: Line 37: Incorrect... |
| `gmsh_diag` | 1:**** ERR StepFile : Undefined Parsing: Line 37: Incorrect... |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=reject/reject gmsh=reject ifc=schema_n/a` |

## Cross-references


**Originally also catalogued as**: Ls021, Ad040
