# Lh024 — Reuse of `#NNN` across different DATA sections (Ed.3 multi-section)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh024.stp`](../../step-examples/12-1b-header/Lh024.stp)  

## Sources
- 07-stepcode-ifcopenshell T029, 16-iso-spec N041, 16-iso-spec N042

## Description

Edition 3 allows multiple `DATA;…ENDSEC;` sections, each with its own `#NNN` namespace; cross-section references use `@section_name#N` syntax. Tools targeting Edition 1 either fail at the second `DATA;` or silently flatten namespaces, causing collisions when `#10` appears in both sections.

## Reproducer recipe

`HEADER;...ENDSEC;DATA;#10=PRODUCT(...);ENDSEC;DATA('extra',('AUTOMOTIVE_DESIGN'));#10=APPLICATION_CONTEXT('...');ENDSEC;END-ISO-10303-21;`

## Expected kernel behavior

accept N≥1 DATA sections; scope `#N` IDs per-section; resolve cross-section references via the proper Ed.3 syntax; never flatten namespaces across sections.

## Notes

**Originally also catalogued as**: Ls025, Ad025. **See also**: Ad080, Lh022.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_DUP_REF)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 2:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 2:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 2:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Ad080](Ad080.md), [Lh022](Lh022.md)

**Originally also catalogued as**: Ls025, Ad025
