# Lh026 — Edition-3 ANCHOR / REFERENCE / SIGNATURE sections present in Edition-2 readers

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh026.stp`](../../step-examples/12-1b-header/Lh026.stp)  

## Sources
- 06-nist-sfa S015, 07-stepcode-ifcopenshell T030

## Description

Edition 3 introduces three optional sections: ANCHOR (named external entry points), REFERENCE (named external links via URI), SIGNATURE (digital signature). `implementation_level` becomes `'4;1'`. Older Edition-2 readers either fail to parse the sections, or strip and re-parse, or warn-and-skip — behavior is implementation-defined.

## Reproducer recipe

`HEADER;...ENDSEC;ANCHOR;<a>=#1;ENDSEC;DATA;...` or add a SIGNATURE section after DATA.

## Expected kernel behavior

under Ed.3, accept and pass through; warn on SIGNATURE that cannot be validated; under strict Ed.2, reject with a clear edition-mismatch diagnostic.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT rejects (`reject`). OCC enforces stricter framing than the spec text requires.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `reject` |
| `occt_heal_off` | `reject` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `step_read_failed` |
| `occt_diag_on` | 1:**** ERR StepFile : Undefined Parsing: Line 48: Incorrect... |
| `occt_diag_off` | 1:**** ERR StepFile : Undefined Parsing: Line 48: Incorrect... |
| `gmsh_diag` | 1:**** ERR StepFile : Undefined Parsing: Line 48: Incorrect... |

**Cross-oracle classification**: OCC stricter than spec — pure-Python accepts

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT rejects (`reject`). OCC enforces stricter framing than the spec text requires.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=reject/reject gmsh=reject ifc=schema_n/a` |
