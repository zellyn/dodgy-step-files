# Lh043 — Header `FILE_DESCRIPTION` with comments inside string

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh043.stp`](../../step-examples/12-1b-header/Lh043.stp)  

## Sources
- OCCT MANTIS#0030397

## Description

A `FILE_DESCRIPTION` record's strings contain `/* */` sequences that legacy readers strip as comments. The result is corrupted description content and possibly truncated header records.

## Reproducer recipe

`FILE_DESCRIPTION(('Test /* note */ part'),'2;1');` — reader strips `/* note */` from inside the description string.

## Expected kernel behavior

comment-stripping never applies inside string literals; bracket comment characters with string-context state.

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT rejects (`reject`). OCC enforces stricter framing than the spec text requires.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `reject` |
| `occt_heal_off` | `reject` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `reject` |
| `part21_strict` | `accept` |
| `manifold` | `step_read_failed` |
| `occt_diag_on` | 1:**** ERR StepFile : Undefined Parsing: Line 4: Incorrect ... |
| `occt_diag_off` | 1:**** ERR StepFile : Undefined Parsing: Line 4: Incorrect ... |
| `gmsh_diag` | 1:**** ERR StepFile : Undefined Parsing: Line 4: Incorrect ... |

**Cross-oracle classification**: OCC stricter than spec — pure-Python accepts

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT rejects (`reject`). OCC enforces stricter framing than the spec text requires.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=reject/reject gmsh=reject ifc=reject` |
