# Lh022 — Duplicate instance ID (`#N=...` defined twice within one DATA section)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh022.stp`](../../step-examples/12-1b-header/Lh022.stp)  

## Sources
- 07-stepcode-ifcopenshell T015, 12-adversarial A006, 16-iso-spec N034

## Description

Within one DATA section each `#N` may appear on the LHS at most once. Concatenating two STEP files with overlapping IDs, or merging sub-files without renumbering, produces duplicates. Silent overwrite causes semantic divergence; in C++ implementations, freeing the first instance while references still point at it transitions to use-after-free.

## Reproducer recipe

`DATA; #10=PRODUCT('a','a','',(#11)); #10=PRODUCT('b','b','',(#12)); ENDSEC;` or `#19=IFCPROJECT(...); #19=IFCSITE(...);`

## Expected kernel behavior

reject with `E_DUPLICATE_INSTANCE_ID` before constructing the second instance; never silently overwrite or free-then-reuse.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

**Originally also catalogued as**: Ls037, Ad006. **See also**: Ad038, Lh024. **OCC behavior**: silently accepts duplicate instance IDs (typically last-write-wins) without diagnostic; kernel mishandling — the catalog above forbids silent overwrite.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_DUP_REF)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `occt_diag_off` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `gmsh_diag` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Ad038](Ad038.md), [Lh024](Lh024.md)

**Originally also catalogued as**: Ls037, Ad006
