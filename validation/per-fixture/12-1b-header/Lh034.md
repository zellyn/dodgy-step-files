# Lh034 — REFERENCE section pointing at unresolvable external anchor

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh034.stp`](../../step-examples/12-1b-header/Lh034.stp)  

## Sources
- 16-iso-spec ISO 10303-21 Ed.3 §8 (REFERENCE / ANCHOR cross-file linking)

## Description

Edition-3 ANCHOR / REFERENCE / SIGNATURE sections support cross-file linking. A REFERENCE entry names an external instance via a URI plus `#anchor_name`; the other end (a separate file) must publish that anchor in its ANCHOR section. When the receiver imports a file with REFERENCE to an unresolvable target (companion file missing, anchor name misspelt, URI scheme unsupported), the kernel must decide whether to defer-load, fail-load, or substitute a placeholder.

## Reproducer recipe

REFERENCE section listing `<file:companion-file.stp#missing_anchor>` and `<https://example.invalid/no-such-file.stp#also_missing>`.

## Expected kernel behavior

report the unresolvable reference with a precise diagnostic naming the URI and anchor; allow the rest of the file to load with a placeholder if the implementer's policy is partial-load, otherwise reject deterministically.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Cross-oracle: pure-Python Part-21 validator rejects (reject(E_UNRESOLVED_REFS)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_UNRESOLVED_REFS)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: OCC silently heals — pure-Python rejects this

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator rejects (reject(E_UNRESOLVED_REFS)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
