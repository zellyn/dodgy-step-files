# Lh036 — SIGNATURE section with unknown signature algorithm

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh036.stp`](../../step-examples/12-1b-header/Lh036.stp)  

## Sources
- 16-iso-spec ISO 10303-21 Ed.3 §9 (SIGNATURE section)

## Description

Edition-3 SIGNATURE sections are intended to carry cryptographic signatures over file content. The spec does not mandate a specific algorithm, so producers and consumers must agree out-of-band. A consumer that does not implement SIGNATURE must skip, warn, or reject; what it must NOT do is parse the signature payload as DATA and fault on the unknown record format.

## Reproducer recipe

SIGNATURE section with an opaque payload `'algo=unknown-future-1; payload=00112233...EEFF;'`.

## Expected kernel behavior

skip SIGNATURE sections by default if the algorithm is unsupported; never let signature payload bytes enter the DATA-section parser.

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT rejects (`reject`). OCC enforces stricter framing than the spec text requires.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `reject` |
| `occt_heal_off` | `reject` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `step_read_failed` |
| `occt_diag_on` | 1:**** ERR StepFile : Undefined Parsing: Line 27: Incorrect... |
| `occt_diag_off` | 1:**** ERR StepFile : Undefined Parsing: Line 27: Incorrect... |
| `gmsh_diag` | 1:**** ERR StepFile : Undefined Parsing: Line 27: Incorrect... |

**Cross-oracle classification**: OCC stricter than spec — pure-Python accepts

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT rejects (`reject`). OCC enforces stricter framing than the spec text requires.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=reject/reject gmsh=reject ifc=schema_n/a` |
