# Lh039 — Multi-DATA-section file with `#NNN` reuse and ambiguous local reference

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1b (`12-1b-header`)  
**Fixture**: [`step-examples/12-1b-header/Lh039.stp`](../../step-examples/12-1b-header/Lh039.stp)  

## Sources
- 16-iso-spec ISO 10303-21 Ed.3 §7 (intra-section vs cross-section reference)

## Description

Edition-3 multi-DATA-section files may reuse `#NNN` in different sections; intra-section references default to the local section. A producer may emit a bare `#NNN` inside one section that should resolve against another (intent unclear), or copy/paste a reference forgetting the `@name` prefix. The receiver resolves to the wrong instance silently.

## Reproducer recipe

DATA section `'geom'` defines `#1` and `#2`; DATA section `'pmi'` defines its own `#1` and `#2` and creates an `AXIS2_PLACEMENT_3D(...,#1,#2,@geom#2)` whose first two arguments resolve locally.

## Expected kernel behavior

resolve bare `#NNN` strictly against the local section; never silently fall back to another section. Document or reject duplicate intra-section instance numbers across sections only when cross-references are explicit.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

**See also**: Lh024, Lh033.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `reject` |
| `occt_heal_off` | `reject` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_NO_DATA)` |
| `manifold` | `step_read_failed` |
| `occt_diag_on` | 1:**** ERR StepFile : Undefined Parsing: Line 31: Incorrect... |
| `occt_diag_off` | 1:**** ERR StepFile : Undefined Parsing: Line 31: Incorrect... |
| `gmsh_diag` | 1:**** ERR StepFile : Undefined Parsing: Line 31: Incorrect... |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `no-target-byte` (? attempts) |
| Status | `pending` |
| Expected validation | `occt=reject/reject gmsh=reject ifc=schema_n/a` |

## Cross-references

**See also**: [Lh024](Lh024.md), [Lh033](Lh033.md)
