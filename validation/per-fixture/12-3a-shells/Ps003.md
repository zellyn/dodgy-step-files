# Ps003 — Single ADVANCED_FACE with same_sense flipped on outer skin

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Ps003.stp`](../../step-examples/12-3a-shells/Ps003.stp)  

## Sources
- deduced from ADVANCED_FACE.same_sense semantics
- bug-report patterns "one face is inside-out", "ray-cast hits surface from wrong direction", "hole in shell where there shouldn't be one"

## Description

A unit cube where five faces correctly orient outward but the TOP face has ADVANCED_FACE.same_sense=.F., effectively inverting that face's normal so it points down into the solid instead of up. Topology is consistent (every edge has degree 2), the shell is closed, and the kernel reports a valid solid. Tier-3 introspection (face-normal outward-test against a near-by interior point) reveals one face's effective normal points the wrong way.

## Reproducer recipe

Unit cube with the standard 6-face / 12-edge / 8-vertex topology. The top face's ADVANCED_FACE has same_sense=.F.; the underlying PLANE.position.axis still points +Z. All loops are oriented in the "with-surface" sense.

## Expected kernel behavior

Verify each face's effective outward normal is consistent with the shell's outside; flip, reject, or report the inconsistent face. A correct kernel should not silently return a solid that fails point-in-solid tests near the flipped face.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

Tier-3 metric: ray cast from a clearly-exterior point along
-Z toward the cube — should hit the top face's outward side first. With
same_sense=.F. the outward side is now inward.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(27)` |
| `gmsh_autofix_off` | `shape(27)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 6
- **n_edges_total**: 24
- **n_vertices_total**: 48
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |
| 1 | plane | 1.0 | 5000001.000000001 | 4 |
| 2 | plane | 1.0 | 5000000.999856222 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 24`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(27) ifc=schema_n/a` |
