# Ps007 — Assembly child placed at identity instead of intended offset

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Ps007.stp`](../../step-examples/12-3a-shells/Ps007.stp)  

## Sources
- deduced from ITEM_DEFINED_TRANSFORMATION semantics
- bug-report patterns "component at origin", "assembly children all stacked at 0,0,0", "NAUO transform lost", "child position not applied"

## Description

A NEXT_ASSEMBLY_USAGE_OCCURRENCE wires the right parent / child product pair, but the bound REPRESENTATION_RELATIONSHIP_WITH_TRANSFORMATION's ITEM_DEFINED_TRANSFORMATION uses the same AXIS2_PLACEMENT_3D for its source and target — yielding the identity transform. The producer's intent was a non-identity offset (e.g., a bolt at (50,50,0) in the assembly's frame). Topology and references all validate; kernel reports one part instance attached to the assembly. Tier-3 introspection (resolved transform = source^-1 · target compared to identity, plus a "did-the-instance-actually-move" check) reveals the placement is ineffectual.

## Reproducer recipe

Standard PRODUCT chain with one assembly PRODUCT and one child PRODUCT. NAUO with id='1' linking them. The ITEM_DEFINED_TRANSFORMATION uses AXIS2_PLACEMENT_3D #701 for both transform_item_1 and transform_item_2 (or two distinct placements with identical location and axes).

## Expected kernel behavior

Detect identity transforms in assembly-instance edges (or, more strongly, surface the resolved transform of every NAUO so downstream code can confirm it matches the producer's intent). When a NAUO's effective transform is identity, that may be intentional or not; a kernel should expose the fact rather than hide it.

## Notes

Tier-3 metric: resolved-transform matrix per NAUO; flag any whose Frobenius distance to identity is below working precision.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(27)` |
| `gmsh_autofix_off` | `shape(27)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 6
- **n_edges_total**: 24
- **n_vertices_total**: 48
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |
| 1 | plane | 1.0 | 5000000.999856222 | 4 |
| 2 | plane | 1.0 | 5000001.000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 24`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(27) ifc=schema_n/a` |
