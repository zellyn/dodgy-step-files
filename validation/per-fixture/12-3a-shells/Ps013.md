# Ps013 — Chiral part mirrored by silent X-coordinate negation

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Ps013.stp`](../../step-examples/12-3a-shells/Ps013.stp)  

## Sources
- deduced from CARTESIAN_POINT semantics combined with CARTESIAN_TRANSFORMATION_OPERATOR_3D scale
- bug-report patterns "left-hand part imported as right-hand", "thread direction reversed", "bracket mirrored after import"

## Description

An L-shaped bracket whose intended right-hand configuration was mirrored across the X axis during export — every X coordinate was negated. Topology, face count, edge count, and unit context are all unchanged; the part is a valid L-bracket, just the wrong-handed one. For chiral mechanical parts (threads, hand-side brackets, steering linkages) this is a critical defect that the kernel cannot detect from the geometry alone.

## Reproducer recipe

An extruded L-shape with vertices at (0,0), (-1,0), (-1,2), (1,2), (1,1), (0,1) (in z=0 plane), extruded to z=1 — i.e., the X-mirror of the producer's intended footprint (0,0), (1,0), (1,2), (-1,2), (-1,1), (0,1).

## Expected kernel behavior

A kernel cannot self-detect chirality errors on a single file; but it can expose the AABB and the principal- axes / inertia-tensor of every solid so downstream tools comparing against a known-good reference can detect mirroring (negative determinant of the rotation that aligns inertial axes).

## Notes

Tier-3 metric: determinant of the rigid alignment between this part's principal axes and a reference part's; -1 indicates a mirror.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(39)` |
| `gmsh_autofix_off` | `shape(39)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 8
- **n_edges_total**: 36
- **n_vertices_total**: 72
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 3.0 | 10000001.000000002 | 6 |
| 1 | plane | 1.0 | 5000001.000000001 | 4 |
| 2 | plane | 3.0 | 10000000.999712443 | 6 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 36`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(39) ifc=schema_n/a` |
