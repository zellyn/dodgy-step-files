# Tsh023 — Empty `EDGE_LOOP` / empty face list on shells

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Tsh023.stp`](../../step-examples/12-3a-shells/Tsh023.stp)  

## Sources
- 03-occt-tests.md R045 (bug33307)
- 04-occt-translation.md Q001, Q028, Q029

## Description

STEP file with empty `EDGE_LOOP(...,())` crashes reader. Similarly: `EDGE_BASED_WIREFRAME_MODEL` with empty boundary or empty `connected_edge_set`; `FACE_BASED_SURFACE_MODEL` with empty face list.

## Reproducer recipe

`#10=EDGE_LOOP('',());` or `#N=FACE_BASED_SURFACE_MODEL('',());`.

## Expected kernel behavior

reject the malformed entity, continue reading; never crash.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `crash`

## Notes

**See also**: Twi001. Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21. **OCC behavior**: crashes (signal 11) on the empty `EDGE_LOOP` — exactly the kernel mishandling the catalog above forbids; documented here so kernel authors know this is what OCC currently does.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `signal(11)` |
| `occt_heal_off` | `signal(11)` |
| `gmsh_autofix_on` | `signal(11)` |
| `gmsh_autofix_off` | `signal(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `process_signal` |

**Cross-oracle classification**: Spec-clean Part-21; OCC crashes (kernel-side bug)

## Tier-3 geometric introspection

- **load**: `None`
- **n_faces**: 0
- **n_edges_total**: None
- **n_vertices_total**: None
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=signal(11)/signal(11) gmsh=signal(11) ifc=schema_n/a` |

## Cross-references

**See also**: [Twi001](Twi001.md)
