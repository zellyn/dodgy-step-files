# Bo025 — Two ADVANCED_FACEs on duplicate CYLINDRICAL_SURFACEs share a smooth (G1) edge mis-tagged as a sharp crease

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Bo025.stp`](../../step-examples/12-3a-shells/Bo025.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT BRepLib::EncodeRegularity (uncovered class evidence)

## Description

Two ADVANCED_FACEs on CYLINDRICAL_SURFACEs (often defined as two distinct surface entities with identical axis and radius — duplicates of one cylinder) meet along a shared circular edge; their normals at the edge are tangent-continuous (G1) but the edge is tagged in the model as a sharp boundary (e.g., `C0` continuity). Or the inverse: an edge is tagged smooth but the two surfaces meet at an angle. Bug-reporter language: "edge continuity wrong", "smooth edge marked sharp", "fillet edge marked as crease", "duplicate cylinders should share one surface".

## Reproducer recipe

Two ADVANCED_FACEs on two CYLINDRICAL_SURFACE entities that are duplicates (same axis, same radius) sharing a tangent edge, with the edge marked `C0` in the source model. (In STEP this is implicit in face tessellation tools but not in the entity graph; the reproducer is geometric — surfaces meet smoothly.)

## Expected kernel behavior

Recompute regularity from face normals; correct the tag.

## Notes

Affects downstream meshing and shading. Provenance tier: runtime-only — bytes alone cannot demonstrate this defect; STEP edge entities have no continuity-tag attribute, so the "C0 vs G1" mistagging exists only as kernel-internal regularity state populated during the regularity-encoding pass. The static fixture encodes only the geometric trigger (two cylinders meeting tangentially). A behavioral test against the regularity-encoder is the appropriate venue.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(10)` |
| `gmsh_autofix_off` | `shape(10)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 8
- **n_vertices_total**: 16
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | cylinder | 6.283185307179586 | 1.9999998000000399 | 4 |
| 1 | cylinder | 6.283185307179586 | 1.9999998000000407 | 4 |

## Provenance / kernel-behavior annotations

- **Provenance tier**: `runtime-only` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(10) ifc=schema_n/a` |
