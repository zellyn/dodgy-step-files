# Bo030 — EDGE_CURVE start VERTEX_POINT lies off the underlying LINE: inner-vertex tolerance smaller than the deviation it bounds

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Bo030.stp`](../../step-examples/12-3a-shells/Bo030.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT BRepLib::UpdateInnerTolerances (uncovered class evidence)

## Description

A vertex deep inside a face/shell carries a tolerance value that's smaller than the actual geometric deviation between its 3D point and the curves/surfaces it sits on. The vertex's "tolerance ball" doesn't actually contain the geometry it indexes. Bug-reporter language: "vertex tolerance too small", "tolerance underestimates gap", "inner tolerance lies".

## Reproducer recipe

An `EDGE_CURVE` whose endpoints are vertices declared with tolerance `1e-7` but whose 3D coordinates are 1e-3 distant from the curve evaluation at the boundary parameter.

## Expected kernel behavior

Recompute inner tolerances from actual deviations; bump tolerance up, or rebuild geometry to match.

## Notes

Distinct from N-tolerance entries (which are about declared vs. file-scope tolerance); this is per-vertex.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 25) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
