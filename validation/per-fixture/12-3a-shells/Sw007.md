# Sw007 — Fast-sewing cannot find the canonical vertex for a wire endpoint

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Sw007.stp`](../../step-examples/12-3a-shells/Sw007.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT BRepBuilderAPI_FastSewing.cxx:480 (FS_FindVertexError) (uncovered class evidence)

## Description

While building the fast-sewing vertex table, a wire endpoint hashes to a bucket containing only vertices at very different 3D coordinates — no near-coincident match within the sewing tolerance. The algorithm cannot decide whether to dedup or insert. Typical fixture: two VERTEX_POINTs with widely separated coordinates (e.g., (0,0,0) and (1e6,0,0)) that nonetheless share a hash bucket. Bug-reporter language: "fast sew vertex hashing mismatch", "vertex not found", "widely-separated VERTEX_POINTs collide in hash".

## Reproducer recipe

Two VERTEX_POINTs whose 3D coordinates differ by a large magnitude (e.g., (0,0,0) and (1e6,0,0)) that, due to the underlying float bit pattern, fall into the same hash bucket but are spatially distant — possibly with no edges or shells defined.

## Expected kernel behavior

Use spatial hashing with tolerance, not bit-equal hashing; fall back to the slow path.

## Notes

Rare; depends on hash function used. Provenance tier: runtime-only — bytes alone cannot demonstrate this defect; the symptom is a hash-bucket collision inside the fast-sewing vertex table during a sewing operation. The static fixture encodes only the inputs; whether they collide depends on the kernel's specific hash function and table size. A behavioral test against the sewing implementation is the appropriate venue.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `runtime-only` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `beyond-10` (rank 30) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
