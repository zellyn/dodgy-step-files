# Ps010 — ORIENTED_EDGE.orientation=.F. on every half-edge of a wire

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Ps010.stp`](../../step-examples/12-3a-shells/Ps010.stp)  

## Sources
- deduced from ORIENTED_EDGE semantics
- bug-report patterns "wire goes the wrong way", "loop traversed backwards", "edge orientation flag inverted"

## Description

A planar face with a square outer loop. Each underlying EDGE_CURVE is constructed so its 3D direction matches the intended CCW traversal — but every ORIENTED_EDGE on that loop has orientation=.F., reversing each one. Adjacent endpoints still match (the loop closes), every edge has degree 2, kernel accepts the file silently. Tier-3 introspection (signed-area of the loop in the face's parametric domain vs. surface normal) reveals the loop traverses CW where CCW was intended.

## Reproducer recipe

Square face at z=0 with vertices at (0,0), (1,0), (1,1), (0,1). Four EDGE_CURVEs whose underlying LINE direction vectors trace the CCW path. Four ORIENTED_EDGEs each with orientation=.F. EDGE_LOOP composed of those four.

## Expected kernel behavior

Verify wire-orientation invariant (chained start/end matching modulo orientation flag) AND signed-area consistency with the face surface normal. A clean wire passes both; this fixture passes the first but fails the second.

## Notes

Tier-3 metric: signed area of the loop in face UV space. Differs from the area whose absolute value the kernel reports if the signed-area check is not run.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].surface_type == "plane"`
- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
