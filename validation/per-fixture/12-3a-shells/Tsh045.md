# Tsh045 — `MANIFOLD_SOLID_BREP` whose outer shell loses the closed flag after face unification

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Tsh045.stp`](../../step-examples/12-3a-shells/Tsh045.stp)  

## Sources
- OCCT MANTIS#0030927
- OCCT MANTIS#0028227

## Description

A solid is built from a closed shell (six cube faces sharing every edge twice). After a face-unification pass, the kernel rebuilds the shell with merged faces but neglects to reset the "closed" / "watertight" flag on the wires/shell. Downstream consumers that rely on the flag — STL export, meshing, Boolean operations — treat the result as an open shell even though topologically it still closes.

## Reproducer recipe

`CLOSED_SHELL` of six rectangular faces sharing all 12 edges; one pair of co-planar coplanar faces left as separate faces with an artificial internal edge; expected to be unified by a downstream pass. Producer-side defect: the input file's wires have a `closed` flag computed before merging that becomes stale after merging.

## Expected kernel behavior

recompute the closed/watertight flag from current topology after any healing or simplification pass; do not propagate stale flags from the input file.

## Notes

**See also**: Tfa016, Tsh001.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-10` (rank 7) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa016](Tfa016.md), [Tsh001](Tsh001.md)
