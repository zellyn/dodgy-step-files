# Tsh064 — Same-surface face merge edge-removal history contract is undocumented

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Tsh064.stp`](../../step-examples/12-3a-shells/Tsh064.stp)  

## Sources
- OCCT MANTIS#0026488
- OCCT GitHub#371
- OCCT GitHub#941

## Description

When adjacent same-surface faces are merged, interior edges are removed from the result. Some downstream tools expect those removed edges to remain in the shape's modification history map; others expect them removed entirely. The contract is not documented and varies between versions.

## Reproducer recipe

Two coplanar faces sharing edge E. Unify; query the modification history for E. Versions disagree on whether E maps to "deleted" or to "absorbed by face F".

## Expected kernel behavior

removed-edge contract is documented and consistent: every removed edge has an explicit history record (deleted / modified-into / split-into); never silently drop edges from history.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`
- disallowed: `silent-accept`

## Notes

**See also**: Tsh059, Tsh058. Defect manifests at runtime (modification history is API-level state); fixture establishes the input precondition (two coplanar faces sharing edge E). **OCC behavior**: silently accepts the input shape with no diagnostic; the contract drift is what the catalog above documents — the merge-history contract is not yet codified, and the catalog claim describes the desired future behavior. Provenance tier: runtime-only — bytes alone cannot demonstrate this defect; the symptom is an undocumented contract for the post-merge modification-history map (a kernel API-level data structure produced by the merge operation). Demonstrating this requires a kernel runtime trace of the modification-history map after the merge.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `runtime-only` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-3` (rank 3) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Tsh059](Tsh059.md), [Tsh058](Tsh058.md)
