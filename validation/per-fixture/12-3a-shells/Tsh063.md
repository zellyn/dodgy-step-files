# Tsh063 — Adjacent toroidal faces with identical parameters are not detected as same-surface

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Tsh063.stp`](../../step-examples/12-3a-shells/Tsh063.stp)  

## Sources
- OCCT MANTIS#0030100
- OCCT MANTIS#0029358
- OCCT MANTIS#0032619

## Description

Two adjacent faces on the same `TOROIDAL_SURFACE` (e.g., a torus split into halves by a planar cut, both halves later resurrected) are not detected as sharing a surface because the surface-equality check uses pointer comparison rather than geometric equivalence. Both halves reference distinct `TOROIDAL_SURFACE` entities with identical parameters, so adjacent same-surface face merging skips them.

## Reproducer recipe

Two `ADVANCED_FACE`s on two `TOROIDAL_SURFACE` entities with identical major / minor radius and identical `AXIS2_PLACEMENT_3D`. Same-surface check returns false; merge skipped.

## Expected kernel behavior

surface-equality check compares structural parameters (axis, radii, etc.) not pointers; named parameter equality collapses to single canonical surface.

## Notes

**See also**: Tfa016, Tsh047.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa016](Tfa016.md), [Tsh047](Tsh047.md)
