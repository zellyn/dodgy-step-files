# Ps006 — Self-overlapping shell: one face double-covers a region of another

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Ps006.stp`](../../step-examples/12-3a-shells/Ps006.stp)  

## Sources
- deduced from CLOSED_SHELL face-set semantics
- bug-report patterns "shell has T-vertices", "two faces on same plane overlap", "mesh has double-covered triangles"

## Description

A "closed" shell where a single planar region is represented twice: once by a large face spanning the whole region, and once by a smaller face on the same supporting PLANE covering a sub-region. The smaller face's edges are not shared with the larger face's loop — they form a separate boundary. Edge-degree-per-shell checks pass within each face's own loop. Kernel accepts the file silently; tier-3 introspection (point-in-face count over a sample grid) reveals certain points are "inside" two faces simultaneously.

## Reproducer recipe

A unit cube whose bottom z=0 face is represented TWICE — once as the full unit square, once as the (0..0.5, 0..0.5) quadrant of the same PLANE. Both faces appear in the same OPEN_SHELL.

## Expected kernel behavior

Detect overlapping face supports in a shell — either by checking that no two faces share a positive-area region of the same supporting surface, or by enforcing the edge-degree-2 invariant globally (overlapping faces fail it). Should reject, repair, or surface the overlap as a quality metric.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Tier-3 metric: cumulative covered-area-per-supporting-surface vs. union-area. For a clean shell these match; here they differ by the area of the overlay quadrant. T-vertices appear at (0.5, 0, 0), (0.5, 0.5, 0), (0, 0.5, 0).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(34)` |
| `gmsh_autofix_off` | `shape(34)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 7
- **n_edges_total**: 28
- **n_vertices_total**: 56
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |
| 1 | plane | 1.0 | 5000001.000000001 | 4 |
| 2 | plane | 1.0 | 5000000.999856222 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 28`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(34) ifc=schema_n/a` |
