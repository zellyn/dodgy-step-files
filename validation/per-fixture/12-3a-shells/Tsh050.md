# Tsh050 — Edges on shared face boundary not deduplicated after merge

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Tsh050.stp`](../../step-examples/12-3a-shells/Tsh050.stp)  

## Sources
- OCCT MANTIS#0026466
- OCCT MANTIS#0026572
- OCCT MANTIS#0027271

## Description

After two same-domain faces are merged, the new face's wire contains every edge from both inputs, including edges that were on the shared boundary between the two faces. Those interior edges should be removed (they are no longer on the merged face's boundary) but instead appear as zero-area dangling segments.

## Reproducer recipe

Two coplanar quad faces sharing one edge. After merging, expected wire is the 6-edge boundary of the union; defective wire is the 8-edge boundary plus the shared edge listed twice with opposite orientations.

## Expected kernel behavior

edges shared between merged faces are removed from the resulting wire; only true boundary edges of the union are retained.

## Notes

**See also**: Tsh046, Tsh049. Provenance tier: runtime-only — bytes alone cannot demonstrate this defect; the static fixture encodes only the merge precondition (two coplanar faces sharing an edge). The defective wire arises only after the kernel performs the merge operation. A behavioral test against the merge implementation is the appropriate venue.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `runtime-only` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Tsh046](Tsh046.md), [Tsh049](Tsh049.md)
