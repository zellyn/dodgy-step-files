# Tsh044 — Free edges in OPEN_SHELL distinguished from non-watertight defects (e.g., two adjacent ADVANCED_FACEs each define their own EDGE_CURVE on a common boundary instead of sharing one)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Tsh044.stp`](../../step-examples/12-3a-shells/Tsh044.stp)  

## Sources
- OCCT `ShapeAnalysis_Shell::CheckOrientedShells` (`ShapeAnalysis_Shell.hxx:52`)

## Description

A shell-orientation analyzer checks that each edge appears either once (free edge — legal in an open shell) or twice with opposite FORWARD/REVERSED orientations (legal for a connected pair). Edges appearing twice with the same orientation, or three or more times, are defects. The diagnostic must distinguish a true non-watertight ("naked", "leaky") defect from a legitimately open free boundary.

## Reproducer recipe

An OPEN_SHELL of two squares sharing no common edge — every edge appears exactly once. The "shell" is two disjoint plates; orientation analysis must label every edge as a free edge (legal) and flag the absence of any sharing as informational.

## Expected kernel behavior

a per-edge tally with three buckets — shared-with-opposite-orientation (good), free (legal in open shells, optionally reported), and pathological (same-orientation pair, or count>2). Kernels intending to build a closed solid must reject the second bucket; kernels handling open shells may accept it.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Free edges are also called "naked edges" in some kernels; both terms in scope.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-10` (rank 8) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
