# Tsh047 — Same-domain face unification crosses cylinder seam and produces non-manifold result

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Tsh047.stp`](../../step-examples/12-3a-shells/Tsh047.stp)  

## Sources
- OCCT MANTIS#0030185
- OCCT MANTIS#0030534
- OCCT MANTIS#0030174

## Description

A cylindrical face with a closed seam has been emitted as two half-cylinder faces sharing a seam edge. The kernel's face-unification step recognises both carry the same underlying `CYLINDRICAL_SURFACE` and merges them — but in doing so reuses the seam edge as an interior edge, producing a non-manifold result (the seam edge belongs to one merged face but is referenced twice).

## Reproducer recipe

Two `ADVANCED_FACE`s on the same `CYLINDRICAL_SURFACE` (radius R, axis Z), parameter ranges `U=[0,π]` and `U=[π,2π]`, sharing two vertical seam edges along `U=0` and `U=π`. After merging, the seam at `U=0` is preserved but the merged face lists it twice in its outer wire.

## Expected kernel behavior

never merge across a periodic seam; treat closed-surface seams as topology-relevant even when surface geometry is identical on both sides.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: Tsh046, Tfa016.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Tsh046](Tsh046.md), [Tfa016](Tfa016.md)
