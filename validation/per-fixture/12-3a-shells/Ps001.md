# Ps001 — Negative-volume cube: every face normal points inward

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Ps001.stp`](../../step-examples/12-3a-shells/Ps001.stp)  
**Sender**: not sender-specific; observed when a writer normalizes faces using a wrong convention or after an in-place "flip-all-normals" debug op leaks into a release.  

## Sources
- deduced from MANIFOLD_SOLID_BREP semantics
- bug-report patterns "STEP file has negative volume", "model imports with reversed normals", "mass-properties says volume = -1000mm^3"

## Description

A topologically closed CLOSED_SHELL whose every constituent face has its supporting surface's stored normal pointing into the volume rather than out. Loop orientations are internally consistent with the flipped surface frames, so edge-degree-2 and orientation-coherence checks pass. Kernel accepts the file silently; tier-3 introspection (signed volume integral, ray-cast-from-far-away parity) reveals signed volume is negative — i.e., the "solid" is actually the complement of the intended cube.

## Reproducer recipe

A unit cube with 8 vertices, 12 line edges, 6 PLANE surfaces. Each PLANE's AXIS2_PLACEMENT_3D.axis points toward the cube center rather than away. Each face's edge-loop is consistent with that flipped axis. Wrap as MANIFOLD_SOLID_BREP and reference from ADVANCED_BREP_SHAPE_REPRESENTATION.

## Expected kernel behavior

Compute signed volume (or equivalent enclosure parity) and detect negative orientation. Either flip all face normals to restore positive volume, reject as malformed, or expose the signed-volume measurement so downstream code can decide. Should not silently report a positive mass or a positive enclosed volume.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

Tier-3 metric: signed-volume integral over the closed shell. A clean unit cube returns +1.0; this fixture returns -1.0.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(27)` |
| `gmsh_autofix_off` | `shape(27)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 6
- **n_edges_total**: 24
- **n_vertices_total**: 48
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |
| 1 | plane | 1.0 | 5000000.999856222 | 4 |
| 2 | plane | 1.0 | 5000001.000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 24`
- `brepcheck.valid == true`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(27) ifc=schema_n/a` |
