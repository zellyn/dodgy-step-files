# Ps005 — Two MANIFOLD_SOLID_BREPs at identical coordinates in one SHAPE_REPRESENTATION

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Ps005.stp`](../../step-examples/12-3a-shells/Ps005.stp)  

## Sources
- deduced from SHAPE_REPRESENTATION list semantics
- bug-report patterns "duplicate body", "two solids occupying same space", "mass-properties returns 2x the mass", "boolean union deduplicates"

## Description

A single SHAPE_REPRESENTATION lists two MANIFOLD_SOLID_BREPs that occupy the same coordinate space. They share no entities; vertices, edges, faces, and shells are all distinct instances but with bit-identical coordinates. Each individual solid validates. The kernel reports two bodies; the producer's intent was one. Tier-3 introspection (pairwise spatial-overlap check among solids in a representation, or AABB-equality + face-set hash) reveals the duplicate.

## Reproducer recipe

Build a unit cube as MANIFOLD_SOLID_BREP #510. Build a second unit cube with freshly-renumbered entities at the same coordinates as #1110. Reference both from ADVANCED_BREP_SHAPE_REPRESENTATION.items.

## Expected kernel behavior

Detect spatially-coincident solids in a single representation and either deduplicate, report the redundancy, or expose count/AABB metrics that downstream code can use. Should not silently propagate doubled mass / volume / face counts to downstream consumers.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- disallowed: `silent-accept`

## Notes

Tier-3 metric: count of solids in representation whose AABBs match within working precision AND whose face counts match.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(54)` |
| `gmsh_autofix_off` | `shape(54)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 12
- **n_edges_total**: 48
- **n_vertices_total**: 96
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |
| 1 | plane | 1.0 | 5000000.999856222 | 4 |
| 2 | plane | 1.0 | 5000001.000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 48`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(54) ifc=schema_n/a` |
