# Ps004 — Left-handed AXIS2_PLACEMENT_3D on a child shape

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Ps004.stp`](../../step-examples/12-3a-shells/Ps004.stp)  

## Sources
- deduced from AXIS2_PLACEMENT_3D semantics
- bug-report patterns "imported part is mirrored", "AXIS2_PLACEMENT mirror", "right-hand part appeared left-hand"

## Description

An ADVANCED_BREP_SHAPE_REPRESENTATION includes a child AXIS2_PLACEMENT_3D whose axis and ref_direction, after Gram-Schmidt orthonormalization, induce a basis whose third (Y) vector points in the opposite of the right-hand-rule direction the producer assumed. AXIS2_PLACEMENT_3D only stores axis + ref_direction (the third basis vector is computed), so individual unit-and-orthogonality checks pass and no diagnostic fires. Kernel accepts the file silently; tier-3 introspection (det(basis) check on every placement) reveals a reflection masquerading as a rotation.

## Reproducer recipe

A simple tetrahedral solid. Plus a separate AXIS2_PLACEMENT_3D with axis=(0,0,1) and ref_direction=(0,1,0) — this yields a computed Y_local = axis × ref_direction = (-1,0,0), which the producer downstream uses as if it were +X. Both placements appear in the SHAPE_REPRESENTATION's items list.

## Expected kernel behavior

Compute the determinant of the basis formed by (ref_direction, axis × ref_direction, axis); reject or warn on negative determinant. A correct kernel should not silently propagate a reflection through downstream transforms.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

Tier-3 metric: determinant of computed basis matrix. Should be +1 for a right-handed frame; this fixture has determinants both ways.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(15)` |
| `gmsh_autofix_off` | `shape(15)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 4
- **n_edges_total**: 12
- **n_vertices_total**: 24
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 0.4999999999999999 | 5000001.000000001 | 3 |
| 1 | plane | 0.4999999999999999 | 5000001.000000002 | 3 |
| 2 | plane | 0.5 | 5000000.985978433 | 3 |

## Tier-3 assertions (machine-checked)

- `face[3].surface_type == "plane"`
- `n_edges_total >= 12`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(15) ifc=schema_n/a` |
