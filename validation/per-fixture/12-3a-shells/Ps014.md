# Ps014 — Position tolerance frame cites datum compartments in wrong order

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3a (`12-3a-shells`)  
**Fixture**: [`step-examples/12-3a-shells/Ps014.stp`](../../step-examples/12-3a-shells/Ps014.stp)  

## Sources
- deduced from DATUM_REFERENCE_COMPARTMENT order semantics
- bug-report patterns "GD&T datum order wrong", "PMI compartments swapped", "primary/secondary/tertiary mixed up"

## Description

A block with three labeled DATUM_FEATUREs (A on bottom, B on front, C on left). The producer intended a position tolerance |⌖|0.05|A|B|C|. Due to a list-ordering bug in the PMI exporter, the DATUM_SYSTEM lists compartments in order (A, C, B). The schema accepts any order; the tolerance is mathematically valid but yields a different inspection regime — secondary and tertiary roles swap, so the constraint axes the gauge uses are different.

## Reproducer recipe

A 10x10x10 block with three DATUMs (A, B, C) mapped to bottom/front/left faces via three GISUs. A POSITION_TOLERANCE attached to a DATUM_SYSTEM whose datum_constraints list is (A_compartment, C_compartment, B_compartment).

## Expected kernel behavior

A kernel cannot detect a "wrong order" without knowing the producer's intent, but it can surface the ordered list of (datum-letter, compartment-role) tuples per tolerance so downstream PMI tools can validate against the drawing's drawing- number-keyed expected order. A correct kernel should not normalize compartment order silently.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

Tier-3 metric: list of (tolerance, [(datum_letter, compartment_index)]) tuples. Cross-checked against an external drawing-of-record to flag swaps.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(27)` |
| `gmsh_autofix_off` | `shape(27)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 6
- **n_edges_total**: 24
- **n_vertices_total**: 48
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |
| 1 | plane | 99.99999999999999 | 50000001.303873554 | 4 |
| 2 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 24`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(27) ifc=schema_n/a` |
