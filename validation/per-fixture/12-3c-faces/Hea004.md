# Hea004 — Free-bound contour analysis filters EDGE_LOOPs with sub-precision wiggle vertices (many short sub-segments, deviations below tolerance)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Hea004.stp`](../../step-examples/12-3c-faces/Hea004.stp)  

## Sources
- OCCT `ShapeAnalysis_FreeBoundsProperties::CheckContours` (`ShapeAnalysis_FreeBoundsProperties.hxx:133`)

## Description

An OPEN_SHELL with multiple free bounds (EDGE_LOOPs) where most are smooth but one has measurable defects. A precision parameter controls which contours are flagged for downstream healing (those whose defect magnitude exceeds the precision). Typical fixture: an EDGE_LOOP with a long side decomposed into many sub-segments, with intermediate VERTEX_POINTs wiggling by +/-1e-7 (well below typical tolerance) — sub-precision noise encoded as separate ORIENTED_EDGEs.

## Reproducer recipe

As Hea003 with an EDGE_LOOP whose perimeter has sub-precision wiggles (e.g., 13 ORIENTED_EDGEs where 10 sub-segments along one side encode a vertex wiggle of +/-1e-7 in one axis).

## Expected kernel behavior

filter contours by the supplied precision; emit only the problematic ones.

## Notes

Companion to Hea003.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(27)` |
| `gmsh_autofix_off` | `shape(27)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 13
- **n_vertices_total**: 26
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999989999999 | 50000001.5 | 13 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 13`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `beyond-10` (rank 25) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(27) ifc=schema_n/a` |
