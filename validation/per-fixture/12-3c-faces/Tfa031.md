# Tfa031 — Locations attached to sub-shapes — instance flattening

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa031.stp`](../../step-examples/12-3c-faces/Tfa031.stp)  

## Sources
- 05-occt-shapefix F056, F084 (ShapeUpgrade_RemoveLocations)
- 13-quaoar B001 (spurious face-level locations from STEP), B028

## Description

After STEP round-trip, individual faces acquire near-identity `TopLoc_Location` transforms even though the source carried no per-face transforms. The reader interprets a top-level `Compound` as an assembly and propagates locations down to leaves. Locations are computationally meaningless but break `IsEqual`/hash dedup.

## Reproducer recipe

Build a `TopoDS_Compound` containing a single shell; write to STEP; re-read; iterate faces and check `TopoDS_Shape::Location().IsIdentity()`.

## Expected kernel behavior

heal — collapse identity locations on leaf faces during reader (or in a healer pass); `RemoveLocations` flattens orphan locations on sub-shapes.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: Tfa030.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999997 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-10` (rank 8) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa030](Tfa030.md)
