# Tfa037 — Face-level healing pipeline must orchestrate wire/orientation/seam/area sub-fixes (e.g., ADVANCED_FACE with outer rectangle plus a defective inner FACE_INNER_BOUND triangle)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa037.stp`](../../step-examples/12-3c-faces/Tfa037.stp)  

## Sources
- OCCT `ShapeFix_Face::Perform` (`ShapeFix_Face.hxx:165`)

## Description

A single ADVANCED_FACE has multiple defects: unordered wire edges, an inner wire whose orientation classifies it as outer when it should be inner, a sub-tolerance hole, plus a missing seam (on a periodic surface). Each defect alone is fixable; the orchestrator must apply fixes in an order that does not destabilize others — fixing wires before orientation produces a different result than the reverse.

## Reproducer recipe

A square face with one out-of-order inner wire whose enclosed area is sub-tolerance and whose orientation flag is wrong.

## Expected kernel behavior

a face-level orchestrator that runs fix-wires, fix-orientation, fix-missing-seam, fix-small-area-wire, fix-natural-bounds in an order that converges; report status per sub-fix.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

Cites Perform's status codes (DONE1..DONE5, FAIL1..FAIL4).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `signal(11)` |
| `occt_heal_off` | `signal(11)` |
| `gmsh_autofix_on` | `signal(11)` |
| `gmsh_autofix_off` | `signal(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `process_signal` |

**Cross-oracle classification**: Spec-clean Part-21; OCC crashes (kernel-side bug)

## Tier-3 geometric introspection

- **load**: `None`
- **n_faces**: 0
- **n_edges_total**: None
- **n_vertices_total**: None
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-10` (rank 9) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=signal(11)/signal(11) gmsh=signal(11) ifc=schema_n/a` |
