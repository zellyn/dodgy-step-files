# Tfa030 — `ADVANCED_FACE` `PLANE` `AXIS2_PLACEMENT_3D` ignored relative to raw-global `CARTESIAN_POINT` bounds (subshape replacement helper drops local placement)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa030.stp`](../../step-examples/12-3c-faces/Tfa030.stp)  

## Sources
- 03-occt-tests R020 (OCC22, OCC24)
- 13-quaoar B028

## Description

A face's local placement transform is lost or ignored relative to its bounding vertices. Common signature: an `ADVANCED_FACE` references a `PLANE` whose `AXIS2_PLACEMENT_3D` origin is shifted (e.g. to (3,4,5)) and x-axis rotated (e.g. 30°), but the `EDGE_LOOP`'s `CARTESIAN_POINT`s are written in raw global coordinates (e.g. (0,0,0)..(10,10,0)) as if the placement were identity. Equivalently, a subshape-replacement helper that replaces a face / edge / vertex inside a parent shape historically loses the local placement transform attached to the subshape, producing shifted geometry on the result.

## Reproducer recipe

Programmatically attach a non-identity transform to a face, insert it back into a shell, run a healing pass that replaces the face, and check whether the transform survives.

## Expected kernel behavior

when sub-shapes carry non-identity location, absorb the location into the underlying surface/curve at the time of replacement, then reset sub-shape location to identity. Declared assembly locations must be preserved exactly.

## Notes

**See also**: Tfa031.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 100.00000000000003 | 1.999990000099999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 27) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa031](Tfa031.md)
