# Tfa017 — Same-domain face merge inflates vertex tolerance on the result

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa017.stp`](../../step-examples/12-3c-faces/Tfa017.stp)  

## Sources
- 03-occt-tests R030 (bug26642, bug26656), R033

## Description

A same-domain face merge dissolves the shared edge of two coplanar faces and assigns the result a tolerance equal to the worst of the inputs (~1e-1) instead of recomputing it from the actual post-merge geometry (would have been ~2e-7). Downstream operations then see inflated tolerances on a result that didn't need them.

## Reproducer recipe

Two coplanar faces with vertex tolerances ~2e-7; fuse; run a same-domain merge; check vertex tolerances on the result.

## Expected kernel behavior

recompute tolerance from actual post-merge geometric deviation; preserve the input precision when merged vertices coincide within source tolerance.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: Tfa016.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(15)` |
| `gmsh_autofix_off` | `shape(15)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 8
- **n_vertices_total**: 16
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |
| 1 | plane | 99.99999999999999 | 50000001.00000001 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 8`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(15) ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa016](Tfa016.md)
