# Hea009 — Convert curves and surfaces of a shape (e.g., FACE_OUTER_BOUND on a degree-2 B_SPLINE_CURVE_WITH_KNOTS) to Bezier representation

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Hea009.stp`](../../step-examples/12-3c-faces/Hea009.stp)  

## Sources
- OCCT `ShapeUpgrade_ShapeConvertToBezier::Perform` (`ShapeUpgrade_ShapeConvertToBezier.hxx:108`)

## Description

A shape whose surfaces and curves are mixed analytic (LINE, PLANE, CYLINDRICAL_SURFACE) and B-spline. The convert-to-Bezier pipeline must handle each representation kind, splitting at non-Bezier breaks (C0 knots, kinks) where needed. Typical fixture: an ADVANCED_FACE with a single FACE_OUTER_BOUND whose curve is a B_SPLINE_CURVE_WITH_KNOTS with 5 control points and degree 2 — a B-spline that is not yet a single Bezier segment and must be converted. Some receivers (e.g., Bezier-only meshers) require this normalization.

## Reproducer recipe

An ADVANCED_FACE on a B-spline surface with C0 knot, or an EDGE_CURVE whose 3D curve is a degree-2 B_SPLINE_CURVE_WITH_KNOTS (not yet Bezier) — both must convert to Bezier.

## Expected kernel behavior

emit a shape whose every curve and surface is a Bezier (single segment) or a sequence of Beziers; preserve geometric fidelity within working precision.

## Notes

Search terms: "convert to Bezier", "Bezier-only export", "split B-spline at C0".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `beyond-10` (rank 12) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
