# Tfa064 — Missing-seam reconstruction fails on ADVANCED_FACE on CYLINDRICAL_SURFACE whose wire contains a partial seam line plus horizontal CIRCLE arcs

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa064.stp`](../../step-examples/12-3c-faces/Tfa064.stp)  

## Sources
- OCCT MANTIS#0025553

## Description

A face on a closed surface has a wire that includes part of the seam edge but not all. The seam-fix routine adds the missing portion to the wire — but inserts it at the wrong position, producing a wire whose edges are no longer in topological order. Wire becomes invalid.

## Reproducer recipe

Cylindrical face whose wire has the seam at `U=0` for the lower half (`V=[0, h/2]`) but not the upper half (`V=[h/2, h]`). Heal inserts the missing upper half at the wire's tail rather than splicing it adjacent to its existing partner.

## Expected kernel behavior

seam-fix must splice the missing seam segment in topological order, adjacent to the existing partial seam; preserve wire-edge ordering.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: Twi084. **OCC behavior**: the validity-checker does not flag the seam-fix wire-ordering defect; tier-3 valid-flag should be ignored for this entry. The reader most likely silently fixes or ignores the misordered seam splice on import.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(7)` |
| `gmsh_autofix_off` | `shape(7)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | cylinder | 6.283185307179585 | 1.0000000000000002 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].surface_type == "cylinder"`
- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `beyond-10` (rank 11) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(7) ifc=schema_n/a` |

## Cross-references

**See also**: [Twi084](Twi084.md)
