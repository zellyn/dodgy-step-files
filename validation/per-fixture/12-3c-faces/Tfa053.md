# Tfa053 — Composite-surface patches do not connect on shared boundary

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa053.stp`](../../step-examples/12-3c-faces/Tfa053.stp)  

## Sources
- OCCT `ShapeExtend_CompositeSurface::CheckConnectivity` (`ShapeExtend_CompositeSurface.hxx:297`)

## Description

A RECTANGULAR_COMPOSITE_SURFACE has a 3D gap on the shared boundary between two neighboring patches — patches do not actually touch within the connectivity tolerance. Faces built atop the composite have invalid topology because the surface is not a single continuous manifold.

## Reproducer recipe

Two abutting plane patches whose touching edges should be at U=1 but the second patch's origin is at `(1.005,0,0)` instead of `(1,0,0)`.

## Expected kernel behavior

detect the gap; either reject the composite, or report the gap as a quality metric so faces atop it can be flagged. Repair (snapping patches together) is also valid.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Surface-side counterpart to Gp034 (complex-curve connectivity). **OCC behavior**: the validity-checker does not flag this defect; tier-3 valid-flag should be ignored for this entry. The reader most likely silently fixes or ignores the patch-gap on import.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(10)` |
| `gmsh_autofix_off` | `shape(10)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 8
- **n_vertices_total**: 16
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 2.0 | 10000000.999999998 | 4 |
| 1 | plane | 2.0 | 10000000.999999998 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].surface_type == "plane"`
- `n_edges_total >= 8`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(10) ifc=schema_n/a` |
