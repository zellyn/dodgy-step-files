# Tfa011 — `ADVANCED_FACE` with multiple `FACE_OUTER_BOUND` entries (face needs split)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa011.stp`](../../step-examples/12-3c-faces/Tfa011.stp)  

## Sources
- 05-occt-shapefix F031 (ShapeFix_Face::FixSplitFace)
- 13-quaoar B004 (face with duplicated outer contour)

## Description

An `ADVANCED_FACE` is emitted with two or more `FACE_OUTER_BOUND` entries when STEP requires exactly one. Common signature: `ADVANCED_FACE.bounds` contains two distinct `FACE_OUTER_BOUND` instances for two disjoint regions (e.g. two 10×10 squares each with its own `EDGE_LOOP`). Often the result of a botched rehost/MakeFace where every 3D edge — including those already representing the contour — was reprojected onto a new surface without dedup, producing two pcurves tracing the same or two separate contours.

## Reproducer recipe

`ADVANCED_FACE` with two `FACE_OUTER_BOUND` records (or, equivalently, one outer wire and one `FACE_BOUND` that geometrically duplicates it).

## Expected kernel behavior

heal — split into two faces (one per outer wire) or merge if outer wires coincide; reject if the loops cross.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(18)` |
| `gmsh_autofix_off` | `shape(18)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 8
- **n_vertices_total**: 16
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000001.000000015 | 4 |
| 1 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `load == "ok"`
- `n_edges_total >= 8`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 17) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(18) ifc=schema_n/a` |
