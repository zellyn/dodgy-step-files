# Tfa061 — Sewing of thin faces takes excessive time during seam-edge creation

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa061.stp`](../../step-examples/12-3c-faces/Tfa061.stp)  

## Sources
- OCCT MANTIS#0030831

## Description

A shell contains many extremely thin faces (strip width below tolerance) on closed surfaces. The seam-edge-creation step in shape healing iterates per-face and per-edge with quadratic complexity, taking minutes on shells with thousands of thin faces. The defect is not algorithmic correctness but the file-pattern: input that exposes a known performance pathology.

## Reproducer recipe

Shell of ~400 ribbon-thin faces, each 0.001 mm wide by 100 mm long, sharing edges along their long sides. Process through sewing.

## Expected kernel behavior

sewing/seam-creation should be sub-quadratic in face count; thin-face handling must not degenerate to all-pairs edge comparison.

## Notes

**See also**: Tfa040, Tsh046, Pf027. Fixture is a scaled representative (12 ribbon faces) of the catalog's ~400; topology pattern (shared long edges between thin ribbons) is identical.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_DUP_REF)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `occt_diag_off` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `gmsh_diag` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa040](Tfa040.md), [Tsh046](Tsh046.md), [Pf027](Pf027.md)
