# Tfa040 — Small-face dispatcher orchestrates spot/strip/pin classification (e.g., sub-tolerance ADVANCED_FACE outer loop ~0.001x0.001 mm)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa040.stp`](../../step-examples/12-3c-faces/Tfa040.stp)  

## Sources
- OCCT `ShapeFix_FixSmallFace::Perform` (`ShapeFix_FixSmallFace.hxx:41`)

## Description

An ADVANCED_FACE that simultaneously satisfies multiple "small face" heuristics — bounding box below precision (sub-tolerance spot face, e.g., a 0.001 x 0.001 mm square outer EDGE_LOOP at the origin), aspect ratio extreme in one direction (strip), and a sharp single-vertex pinch (pin). The pipeline must classify before applying a removal/healing strategy; the wrong classification produces a different (worse) result.

## Reproducer recipe

A near-zero-area ADVANCED_FACE whose outer EDGE_LOOP vertices are all within 1e-3 of `(0,0)` — a sub-tolerance spot face.

## Expected kernel behavior

a deterministic classifier that picks the dominant defect (e.g., spot beats strip beats pin), then dispatches to the specific fixer.

## Notes

Umbrella entry over FixSpotFace/FixStripFace/FixPinFace.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1e-06 | 5001.000000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].area < 1e-3`
- `face[0].sliver_aspect_max_min > 1e3`
- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 42) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
