# Tfa036 — Face on composite surface with boundary crossing patch seams

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa036.stp`](../../step-examples/12-3c-faces/Tfa036.stp)  

## Sources
- OCCT `ShapeFix_ComposeShell::Perform` (`ShapeFix_ComposeShell.hxx:106`)

## Description

An ADVANCED_FACE references a RECTANGULAR_COMPOSITE_SURFACE assembled from multiple sub-patches, *or* two ADVANCED_FACEs share the same FACE_OUTER_BOUND wire while referencing two different host PLANEs (e.g., one PLANE at origin, another offset to (1,0,0)) that abut at a patch seam. The face's boundary wire crosses one or more patch seams; pcurves are not split per patch. To evaluate the face's geometry, the kernel must reassemble per-patch fragments while keeping pcurves continuous across patch boundaries.

## Reproducer recipe

Two coplanar PLANE patches abutting at U=1, either as a RECTANGULAR_COMPOSITE_SURFACE or as two ADVANCED_FACEs sharing one FACE_OUTER_BOUND; the wire is a 2x1 rectangle whose top/bottom edges cross the U=1 seam.

## Expected kernel behavior

split the face's edges at patch boundaries (or treat the composite as one logical surface and lift pcurves accordingly); produce a shell of patch-local sub-faces with continuous wire connectivity, or reject if the composite cannot be unified.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Existing Tfa entries cover related composite-surface defects. **See also**: Tfa053, Twi055, Gp019.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(10)` |
| `gmsh_autofix_off` | `shape(10)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 8
- **n_vertices_total**: 16
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 2.0 | 10000000.999999998 | 4 |
| 1 | plane | 2.0 | 10000000.999999998 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].surface_type == "plane"`
- `n_edges_total >= 8`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(10) ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa053](Tfa053.md), [Twi055](Twi055.md), [Gp019](Gp019.md)
