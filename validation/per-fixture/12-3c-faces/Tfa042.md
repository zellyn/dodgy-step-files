# Tfa042 — Strip face: face whose width is below tolerance

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa042.stp`](../../step-examples/12-3c-faces/Tfa042.stp)  

## Sources
- OCCT `ShapeFix_FixSmallFace::FixStripFace` (`ShapeFix_FixSmallFace.hxx:53`)

## Description

A face is bounded by two long edges that are confused along their full length (separated by < tolerance) and two tiny end edges. The face is a degenerate sliver — typically the residue of a Boolean-cut subtraction along a feature edge.

## Reproducer recipe

A face whose outer wire is `(0,0)→(100,0)→(100,1e-3)→(0,1e-3)→(0,0)` — aspect ratio 1e5.

## Expected kernel behavior

remove the strip face and merge its long edges into one (or absorb into a neighboring face); alternatively reject.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Search terms: "strip face", "sliver face", "thin face", "ribbon face".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 0.1 | 500000000.99999994 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].sliver_aspect_max_min > 1e3`
- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
