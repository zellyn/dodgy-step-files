# Tfa065 — Healed face uses inflated tolerance instead of original (small gap between adjacent EDGE_LOOP vertices, e.g., 1e-3 mm between V3 and next-edge start)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa065.stp`](../../step-examples/12-3c-faces/Tfa065.stp)  

## Sources
- OCCT MANTIS#0026620
- OCCT MANTIS#0027122

## Description

A face whose constituent edges have native tolerance ~1e-7 goes through a healing pass that inflates the face-level tolerance to ~1e-1 to absorb a single localised gap. The bloated tolerance then contaminates all downstream operations on the face.

## Reproducer recipe

Rectangular face with three good edges (tolerance 1e-7) and one edge with a 1e-3 gap to its neighbour. After healing the face tolerance is set to 1e-1 (>> the worst gap) rather than 1.5e-3.

## Expected kernel behavior

face / shell tolerance set to the maximum measured deviation across constituent edges, not orders of magnitude above; do not inflate tolerance preemptively.

## Notes

**See also**: Tfa017, N040.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5005001.0 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `beyond-10` (rank 23) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa017](Tfa017.md), [N040](N040.md)
