# Tfa060 — Shell parametric coverage exceeds host surface bounds

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa060.stp`](../../step-examples/12-3c-faces/Tfa060.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT BRepLib_MakeShell ShellError.ShellParametersOutOfRange (uncovered class evidence)

## Description

A face within a shell has a bounding wire whose pcurve excursion in UV exceeds the `u_min..u_max`/`v_min..v_max` of the underlying surface. The face "wraps around" or "overshoots" the surface. Bug-reporter language: "face parameters out of range", "wire goes off surface".

## Reproducer recipe

An `ADVANCED_FACE` on a `RECTANGULAR_TRIMMED_SURFACE` with `u1=0, u2=1`. The bounding wire's pcurve runs from `u=0` to `u=1.5`.

## Expected kernel behavior

Clamp pcurve to surface domain, or extend the surface trim, or reject.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Synonyms: "uv parameters too wide", "face overshoots surface".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `False`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=empty ifc=schema_n/a` |
