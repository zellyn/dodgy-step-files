# Tfa051 — Face needs splitting (knot insertion / sub-faces) for downstream tools

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa051.stp`](../../step-examples/12-3c-faces/Tfa051.stp)  

## Sources
- OCCT `ShapeUpgrade_FaceDivide::Perform` (`ShapeUpgrade_FaceDivide.hxx:73`)

## Description

A face on a surface with internal C0 knots, large parameter span, or multiple analytic regions. Downstream tools handle smaller pieces better — meshers produce more uniform elements, Booleans avoid the C0 line, fillet engines find tangents. The face-divide pipeline produces a shell of replacement sub-faces.

## Reproducer recipe

A square face whose underlying surface has a C0 line at U=0.5; the division must split the face into two sub-faces along that isoline.

## Expected kernel behavior

emit a shell of sub-faces with consistent orientation, sharing edges along the cut, with re-stitched outer wires. The split criteria are kernel policy.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

Face-side counterpart to Gp033 (curve split) and Gs049 (surface split).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | bspline | 103.92734634830808 | 9.999998200000357 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-10` (rank 7) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
