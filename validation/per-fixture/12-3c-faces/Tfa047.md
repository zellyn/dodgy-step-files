# Tfa047 — Single-strip ADVANCED_FACE detection with U/V direction classification (long thin face, e.g., 100 x 0.001 mm rectangle outer EDGE_LOOP)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa047.stp`](../../step-examples/12-3c-faces/Tfa047.stp)  

## Sources
- OCCT `ShapeAnalysis_CheckSmallFace::CheckSingleStrip` (`ShapeAnalysis_CheckSmallFace.hxx:105`)

## Description

A face is a single strip (two long confused edges) but the diagnostic must additionally report whether the strip extends in the U or V direction of the host surface. Without that, the strip cannot be unfolded or merged into a neighbor; the absorbing face's parameter direction needs to align with the strip's long axis.

## Reproducer recipe

As Tfa042 — the diagnostic should return "strip in U" because the long edges run along U.

## Expected kernel behavior

classify direction (0/1/2 for not-strip / U / V), return both long edges plus direction.

## Notes

Diagnostic counterpart of FixStripFace.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 0.1 | 500000000.99999994 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].sliver_aspect_max_min > 1e3`
- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-10` (rank 8) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
