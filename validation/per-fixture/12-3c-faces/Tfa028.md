# Tfa028 — Full-revolution `CYLINDRICAL_SURFACE` `ADVANCED_FACE` with single seam `EDGE_CURVE` used twice (split_angle: angular span exceeds cap)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa028.stp`](../../step-examples/12-3c-faces/Tfa028.stp)  

## Sources
- 03-occt-tests R012 (split_angle)
- 05-occt-shapefix F049 (ShapeUpgrade_ShapeDivideAngle)
- 09-healing-menus H052

## Description

Cones, cylinders, `SURFACE_OF_REVOLUTION` faces with angular range exceeding a configured threshold (90°, 180°, full revolution) must be subdivided. Common signature: a full-revolution `CYLINDRICAL_SURFACE` `ADVANCED_FACE` whose `FACE_OUTER_BOUND` carries top and bottom `CIRCLE` `EDGE_CURVE`s with start==end vertex (closed circular edges) plus a single seam `EDGE_CURVE` used twice in the same `EDGE_LOOP` — once with `.T.` and once with `.F.` — instead of the two split-seam halves a downstream consumer expects. Downstream meshers misbehave on full-revolution faces; CAM tool-axis derivation expects per-segment.

## Reproducer recipe

Full revolution face on `CYLINDRICAL_SURFACE` (angular span 2π).

## Expected kernel behavior

heal — split into N faces (configurable angular cap); preserve the underlying surface; insert seam edges between segments.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: Tfa013. Provenance tier: runtime-only — the bytes encode a valid full-revolution face with a doubled seam edge, but the catalogued defect is the kernel's split-angle healing pass behavior (whether and how it subdivides the face). The healing decision and its quality manifest only at kernel runtime; the static fixture encodes only the trigger geometry.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(6)` |
| `gmsh_autofix_off` | `shape(6)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | cylinder | 314.1592653589793 | 3.9999998800000043 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].surface_type == "cylinder"`
- `n_edges_total >= 4`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `runtime-only` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 15) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(6) ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa013](Tfa013.md)
