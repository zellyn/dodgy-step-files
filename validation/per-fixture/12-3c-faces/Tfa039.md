# Tfa039 — Face has multiple wires that intersect in UV

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa039.stp`](../../step-examples/12-3c-faces/Tfa039.stp)  

## Sources
- OCCT `ShapeFix_Face::FixIntersectingWires` (`ShapeFix_Face.hxx:216`)
- `ShapeFix_IntersectionTool::FixIntersectingWires` (`ShapeFix_IntersectionTool.hxx:75`)

## Description

An ADVANCED_FACE has an outer wire and one or more inner wires whose 2D pcurves cross each other in the host surface's parameter space. The wires are not nested; they overlap. The face cannot bound a simple region.

## Reproducer recipe

A square face with two diagonal "inner" wires that cross at the face center.

## Expected kernel behavior

detect the crossing(s); split the face into multiple regions whose wires are non-crossing, merge the crossing wires into a single non-simple wire and resolve, or reject. This entry covers both the face-level fixer and the lower-level IntersectionTool counterpart.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Collapses two OCCT methods (different layers, same defect class). **See also**: Tfa055.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 8
- **n_vertices_total**: 16
- **brepcheck.valid**: `False`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 159.99999999999997 | 50000000.99999999 | 8 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 8`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=reject ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa055](Tfa055.md)
