# Hea011 — Shape-healing driver runs a runtime-configured operator sequence over a GEOMETRIC_SET with sub-tolerance EDGE_CURVE, OPEN_SHELL, and same-parameter violation

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Hea011.stp`](../../step-examples/12-3c-faces/Hea011.stp)  

## Sources
- OCCT `ShapeProcess::Perform` (`ShapeProcess.hxx:87`)

## Description

A top-level driver runs a sequence of named healing operators ("fix-shape, split-closed-faces, ..."). The sequence is configured at runtime; operator parameters come from a context resource. Errors at any step must be aggregated; the driver must not silently skip failed operators. Typical fixture: a GEOMETRIC_SET with multiple defect classes — a sub-tolerance EDGE_CURVE (length ~1e-7 between near-coincident VERTEX_POINTs), a rectangular shell, and an EDGE_CURVE whose 3D length disagrees with its pcurve length (same-parameter violation).

## Reproducer recipe

A multi-defect GEOMETRIC_SET compound (sub-tolerance edge, rectangular shell, same-parameter pcurve mismatch); driver invoked with operator list "fix-shape, same-parameter, remove-small-edges".

## Expected kernel behavior

run operators in declared order; collect per-operator status; abort on operator-not-found, otherwise continue. Configuration parameters are pulled from the context.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- disallowed: `crash`

## Notes

Provides the runtime extension point for the healing pipeline. Defect manifests at runtime (operator-list invocation is API-level); fixture establishes the input precondition (a multi-defect compound mixing sub-tolerance edge, open shell, and same-parameter violation).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
