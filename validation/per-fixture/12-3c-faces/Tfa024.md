# Tfa024 — Glue Faces (coincident faces)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa024.stp`](../../step-examples/12-3c-faces/Tfa024.stp)  

## Sources
- 09-healing-menus H063, H102 (Salome/Netgen)
- 11-vendor W044

## Description

Two `ADVANCED_FACE`s are geometrically coincident — duplicate layers on the same underlying surface, often from botched Booleans. Both attached to the same shell.

## Reproducer recipe

Two `ADVANCED_FACE`s on the same `PLANE` with identical loops; both attached to the same `CLOSED_SHELL`.

## Expected kernel behavior

heal — keep one face, drop the duplicate; if coincidence is partial, log; merge results into a non-manifold-clean compound.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: Tsh026.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(14)` |
| `gmsh_autofix_off` | `shape(14)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 8
- **n_vertices_total**: 16
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |
| 1 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 8`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(14) ifc=schema_n/a` |

## Cross-references

**See also**: [Tsh026](Tsh026.md)
