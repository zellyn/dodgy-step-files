# Tfa044 — Pin face: long thin protrusion at a vertex

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa044.stp`](../../step-examples/12-3c-faces/Tfa044.stp)  

## Sources
- OCCT `ShapeFix_FixSmallFace::FixPinFace` (`ShapeFix_FixSmallFace.hxx:82`)

## Description

A face whose outer wire pinches to a single near-vertex pin — a sharp protrusion whose width tapers to 0 at one corner. Subsequent meshing/Boolean operations corrupt at the pin tip; the face's normal is undefined there.

## Reproducer recipe

A triangular face with vertices at `(0,0)`, `(10, 0.001)`, `(10, -0.001)` — opening angle ≈ 0.0002 rad.

## Expected kernel behavior

detect the pin; either snip it off (replace the pin tip with a vertex on the face's neighbor) or reject. Healing must preserve the integrity of incident shells.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Search terms: "pin face", "needle face", "spike face".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(7)` |
| `gmsh_autofix_off` | `shape(7)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 3
- **n_vertices_total**: 6
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 0.019999999999999993 | 50000000.99999999 | 3 |

## Tier-3 assertions (machine-checked)

- `face[0].sliver_aspect_max_min > 1e3`
- `n_edges_total >= 3`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(7) ifc=schema_n/a` |
