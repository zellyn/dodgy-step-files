# Tfa045 — Multiple inner FACE_BOUND wires of an ADVANCED_FACE touch tangentially at a single point (kissing wires sharing a VERTEX_POINT)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa045.stp`](../../step-examples/12-3c-faces/Tfa045.stp)  

## Sources
- OCCT `ShapeFix_IntersectionTool::FixIntersectingWires` (`ShapeFix_IntersectionTool.hxx:75`)
- see also Tfa039 for the crossing variant.

## Description

A face has two inner wires that touch at a single point in UV (tangent rather than crossing). The face boundary is technically simple (no edges cross) but the wires are kissing — under tolerance perturbations the kissing point becomes an actual crossing or a separation.

## Reproducer recipe

Two triangle wires inside a square face, both having a vertex at `(5,5)` and otherwise non-overlapping. The shared vertex is the kissing point.

## Expected kernel behavior

distinguish tangent kiss from crossing; either merge the two wires at the shared point, leave the kiss alone if topology permits (kernel-policy), or reject.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Distinct from Tfa039 because the tangency-vs-crossing decision is a different kernel branch. **OCC behavior**: the validity-checker does not flag this defect; tier-3 valid-flag should be ignored for this entry. The reader most likely silently fixes or ignores the kissing-wires case on import.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `top-10` (rank 8) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
