# Tfa056 — Face has a redundant wire (wire fully inside another wire of same face)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa056.stp`](../../step-examples/12-3c-faces/Tfa056.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT BRepCheck_Face.cxx:209 (RedundantWire) (uncovered class evidence)

## Description

A face has two inner wires (holes) where one is geometrically nested inside the other. The inner-most wire is then a "hole inside a hole", which is contradictory: that region was already excluded from the face. Bug-reporter language: "redundant wire", "hole inside hole", "useless inner loop".

## Reproducer recipe

An `ADVANCED_FACE` with one `FACE_OUTER_BOUND` (large square) and two `FACE_BOUND` entries — a smaller square hole, and a still-smaller square wire fully contained in the first hole.

## Expected kernel behavior

Detect nesting via UV-domain point-in-polygon; drop the redundant inner wire and warn, or reject as malformed.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Synonyms: "duplicate wire", "wire inside hole".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(1)` |
| `gmsh_autofix_off` | `shape(1)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 8e+100 | 1.0000000000000001e+107 | 0 |

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(1) ifc=schema_n/a` |
