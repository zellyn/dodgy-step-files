# Tfa032 — Same-domain face merge introduces overlap / self-intersection on boolean result

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa032.stp`](../../step-examples/12-3c-faces/Tfa032.stp)  

## Sources
- 03-occt-tests R074 (bug27082, bug27199)
- 18-other-issues I040, I052

## Description

Merging adjacent same-domain faces on a boolean result introduces new defects (overlapping faces, self-intersections) that were not present in the raw boolean output. The merge unions topology that should remain separate, e.g. a tapered-shape-sharing-edge case or a wedge-wedge-boundary case.

## Reproducer recipe

Boolean fuse a tapered shape with a box where their bounding boxes share an edge along x-min; raw boolean is valid; pass through same-domain face merging.

## Expected kernel behavior

a face-merge pass must produce topologically valid output; detect cases where merging would introduce overlap or self-intersection and skip the merge there with a diagnostic, or roll back the partial merge.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: Tfa016. **OCC behavior**: the validity-checker does not flag this same-domain-merge overlap; tier-3 valid-flag should be ignored for this entry. The reader most likely silently fixes or ignores the merge-induced overlap on import.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(17)` |
| `gmsh_autofix_off` | `shape(17)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 9
- **n_vertices_total**: 18
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 41.99999999999999 | 50000000.99999999 | 5 |
| 1 | plane | 49.99999999999999 | 50000001.00000001 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 9`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(17) ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa016](Tfa016.md)
