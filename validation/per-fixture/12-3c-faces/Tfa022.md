# Tfa022 — Almost-closed `EDGE_LOOP` with sub-tolerance gap and wrong Closed flag (ConnectEdgesToWires)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa022.stp`](../../step-examples/12-3c-faces/Tfa022.stp)  

## Sources
- 03-occt-tests R032 (bug25068, bug25333), R076 (bug22924 non-determinism)

## Description

A free-edge connector pass returns `EDGE_LOOP`s / wires whose closed-or-open flag disagrees with the wire's actual geometry — geometrically open wires marked closed, or vice versa. Common signature: an `EDGE_LOOP` of four edges whose last edge ends at a `VERTEX_POINT` (e.g. (0, 1e-6, 0)) that differs from the first edge's start vertex (e.g. (0, 0, 0)) by ~1e-6 mm — the loop is not closed by that tiny gap, and the file may carry the unclosed loop as a free EDGE_LOOP without any `FACE_OUTER_BOUND` / `ADVANCED_FACE` wrapper. Repeated runs over the same input also produce different orderings (non-deterministic output).

## Reproducer recipe

Set of free edges whose endpoint pairing yields one wire that almost closes within tolerance.

## Expected kernel behavior

recompute Closed flag from final endpoint coincidence within stated tolerance; deterministic ordering across 100 runs.

## Notes

**See also**: Twi027.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Twi027](Twi027.md)
