# Hea014 — Compound rebuild misses location chain when child SHAPE_REPRESENTATION via REPRESENTATION_RELATIONSHIP_WITH_TRANSFORMATION at non-identity placement

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Hea014.stp`](../../step-examples/12-3c-faces/Hea014.stp)  

## Sources
- OCCT MANTIS#0033806
- OCCT MANTIS#0033791

## Description

A document is processed through a generic shape-rebuild pass (`ShapeCustom`-class operation). The compound is rebuilt from modified children, but the rebuild loses the source compound's location attribute and any cached context state attached to root.

## Reproducer recipe

Compound containing one solid at non-identity location; pass through shape-rebuild. Output compound has identity location.

## Expected kernel behavior

rebuild operations preserve the input shape's TopLoc location; cached context propagates from input to output.

## Notes

**See also**: A067, A073. Defect manifests at runtime (TopLoc state is API-level); fixture establishes the input precondition (compound with solid at non-identity placement via REPRESENTATION_RELATIONSHIP_WITH_TRANSFORMATION).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `beyond-10` (rank 21) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |

## Cross-references

**See also**: [A067](A067.md), [A073](A073.md)
