# Tfa063 — Stack overflow on `RemoveSmallFaces` due to unbounded recursion

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa063.stp`](../../step-examples/12-3c-faces/Tfa063.stp)  

## Sources
- OCCT MANTIS#0026182

## Description

A face-cleanup pass that recursively removes small faces and re-checks neighbours overflows the stack on shells where a chain of small faces causes tail-recursion to bottom out only after thousands of frames. The defect is the file pattern: a long chain of thin faces, each smaller than the threshold and whose removal reveals another below the threshold.

## Reproducer recipe

Shell of 1000 stacked thin slabs, each 0.0001 mm thick, joined edge-to-edge so removing one exposes the next.

## Expected kernel behavior

small-face removal uses iteration with explicit worklist, not unbounded recursion; threshold thresholds are absolute, not relative to remaining shell.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- disallowed: `infinite-loop`

## Notes

**See also**: Tfa040, Pf027. Fixture is a scaled representative (10 slabs) of the catalog's 1000; topology pattern (chain of small faces each below threshold) is identical.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_DUP_REF)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `occt_diag_off` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `gmsh_diag` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa040](Tfa040.md), [Pf027](Pf027.md)
