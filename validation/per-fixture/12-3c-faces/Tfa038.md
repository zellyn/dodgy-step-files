# Tfa038 — Face on closed surface lacks outer boundary

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa038.stp`](../../step-examples/12-3c-faces/Tfa038.stp)  

## Sources
- OCCT `ShapeFix_Face::FixAddNaturalBound` (`ShapeFix_Face.hxx:195`)

## Description

An ADVANCED_FACE on a SPHERICAL_SURFACE or TOROIDAL_SURFACE (geometrically closed in both U and V) carries only inner FACE_BOUND wires (holes) and no FACE_OUTER_BOUND. The face is "the surface minus the holes". Without a synthesized natural boundary, area, sampling, and sewing are undefined.

## Reproducer recipe

An ADVANCED_FACE on a sphere with one tiny circular hole at the equator and no outer bound.

## Expected kernel behavior

detect that the surface is doubly-closed and the face has no left-oriented outer wire; synthesize a natural boundary that traverses the surface's full parameter domain. Alternative: reject if the kernel does not support implicit natural bounds.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Specifically named for closed surfaces — planar/cylindrical faces have explicit bounds.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(3)` |
| `gmsh_autofix_off` | `shape(3)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 1
- **n_vertices_total**: 2
- **brepcheck.valid**: `False`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | sphere | 0.0 | 49.694932446140356 | 1 |

## Tier-3 assertions (machine-checked)

- `face[0].surface_type == "sphere"`
- `n_edges_total >= 1`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(3) ifc=schema_n/a` |
