# Hea005 — Triangular notch detour detected on a free-bound EDGE_LOOP contour (sub-mm vertices break a straight side into 3+ segments)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Hea005.stp`](../../step-examples/12-3c-faces/Hea005.stp)  

## Sources
- OCCT `ShapeAnalysis_FreeBoundsProperties::CheckNotches` (`ShapeAnalysis_FreeBoundsProperties.hxx:135`)

## Description

A free-bound contour (EDGE_LOOP / outer wire) has one or more notches — sharp inward triangular detours where a straight side has been broken into 3+ ORIENTED_EDGEs through extra VERTEX_POINTs offset perpendicular to the main side (typical detour: (0,0,0)->(4.5,0,0)->(5.0,0.05,0)->(5.5,0,0)->(10,0,0)). The check must report each with width and depth so the user can decide whether to flatten or split the host face.

## Reproducer recipe

An OPEN_SHELL whose face's outer EDGE_LOOP has the notched-wire defect from Twi054 — vertices V_notch_a, V_notch_apex, V_notch_b interrupt an otherwise straight side.

## Expected kernel behavior

per-notch (location, width, depth) report; do not modify.

## Notes

Free-bound-level counterpart of Twi054.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(15)` |
| `gmsh_autofix_off` | `shape(15)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 7
- **n_vertices_total**: 14
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.97499999999998 | 50000000.99999999 | 7 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 7`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(15) ifc=schema_n/a` |
