# Hea013 — User-defined shape-process operator (UOperator) on face with scrambled inner FACE_BOUND triangle (near-coincident vertices, edges out of order)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Hea013.stp`](../../step-examples/12-3c-faces/Hea013.stp)  

## Sources
- OCCT `ShapeProcess_UOperator::Perform` (`ShapeProcess_UOperator.hxx:37`)

## Description

A kernel API for registering ad-hoc user operators in the shape-healing pipeline; same contract as Hea012 but injected by a caller rather than being part of the kernel's built-in operator set. Lets host applications splice domain-specific healing into the standard pipeline. Typical input fixture: a rectangular ADVANCED_FACE with an inner triangle FACE_BOUND whose VERTEX_POINTs are near-coincident and whose ORIENTED_EDGEs are listed in scrambled order — structurally identical input to Hea012.

## Reproducer recipe

As Hea012 (face with scrambled inner FACE_BOUND triangle) supplied to a user-registered operator.

## Expected kernel behavior

identical to Hea012 but allow runtime registration; sandbox the user operator if its execution diverges.

## Notes

Concrete subclass of Hea012's contract.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `beyond-10` (rank 15) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
