# Tfa043 — Single-face dispatcher: classify-and-heal small face

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa043.stp`](../../step-examples/12-3c-faces/Tfa043.stp)  

## Sources
- OCCT `ShapeFix_FixSmallFace::FixFace` (`ShapeFix_FixSmallFace.hxx:76`)

## Description

A standalone ADVANCED_FACE supplied without context. The dispatcher must classify it as spot, strip, or pin (or none), and then call the specific fixer. The classification is the entry-point for any small-face workflow.

## Reproducer recipe

A near-zero-area face supplied as the only face of a fixture.

## Expected kernel behavior

classify, dispatch, return the modified face (or null if removed). A kernel may also choose to refuse to mutate single faces and require shell-level context.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Functional duplicate of Tfa040 at a different API layer; both must be documented because they're separately testable.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1e-06 | 5001.000000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].area < 1e-3`
- `face[0].sliver_aspect_max_min > 1e3`
- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-10` (rank 7) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
