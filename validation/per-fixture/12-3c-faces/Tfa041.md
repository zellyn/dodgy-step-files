# Tfa041 — Spot face: face collapsed to a near-point

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa041.stp`](../../step-examples/12-3c-faces/Tfa041.stp)  

## Sources
- OCCT `ShapeFix_FixSmallFace::FixSpotFace` (`ShapeFix_FixSmallFace.hxx:44`)

## Description

An ADVANCED_FACE whose outer wire's bounding box is smaller than the kernel's working precision in both U and V. The face is geometrically a point — it has no usable interior and contributes nothing to the shape's volume or boundary.

## Reproducer recipe

A square face with vertices at `(0,0)`, `(0.001,0)`, `(0.001,0.001)`, `(0,0.001)`.

## Expected kernel behavior

remove the face and replace it with a vertex wherever the topology requires a referent; alternatively reject as malformed. The neighboring faces' wires may need re-stitching.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Search terms: "spot face", "point-collapsed face", "zero-area face", "tiny face".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1e-06 | 5001.000000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].area < 1e-3`
- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
