# Hea012 — Shape-process operator contract: pluggable healing step over face with scrambled inner FACE_BOUND triangle (near-coincident vertices, edges out of order)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Hea012.stp`](../../step-examples/12-3c-faces/Hea012.stp)  

## Sources
- OCCT `ShapeProcess_Operator::Perform` (`ShapeProcess_Operator.hxx:35`)

## Description

A single registered operator must (a) accept a context, (b) perform its healing step, (c) record changes back to the context for subsequent operators to observe. The contract is stateless w.r.t. the operator's own data — all transient state lives in the context. Typical fixture: an ADVANCED_FACE with a rectangular outer EDGE_LOOP and an inner FACE_BOUND triangle whose three VERTEX_POINTs are near-coincident at sub-mm distances and whose ORIENTED_EDGEs are listed in scrambled (non-head-to-tail) order.

## Reproducer recipe

A defective face supplied to a registered operator; the inner FACE_BOUND has near-coincident triangle vertices and out-of-order edges; the operator runs and the context's "modified" flag is set.

## Expected kernel behavior

each operator implements the same Perform(context) interface; the driver does not need per-operator special casing.

## Notes

Abstract base contract.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
