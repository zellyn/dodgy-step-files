# Tfa019 — Adjacent `ADVANCED_FACE`s with distinct `VERTEX_POINT`/`EDGE_CURVE` for the same shared boundary (FaceConnect: vertices not shared)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa019.stp`](../../step-examples/12-3c-faces/Tfa019.stp)  

## Sources
- 05-occt-shapefix F058 (ShapeFix_FaceConnect)
- 11-vendor W003 (crack), W045 (duplicate vertices)
- 13-quaoar B017

## Description

Two `ADVANCED_FACE`s that touch along a physical shared boundary use distinct `VERTEX_POINT` instances on each side and distinct `EDGE_CURVE` instances for what should be one shared edge — each owns its own vertex / edge copy. Common signature: an `OPEN_SHELL` of two coplanar faces meeting at, say, x=10, where face A uses vertices (#30, #31) and edge #71 along that border while face B uses vertices (#34, #37) and edge #77 — duplicate vertices at the seam needing connect / sew. Distance-only checks pass but the shell has zero-tolerance topological cracks.

## Reproducer recipe

Two `ADVANCED_FACE`s sharing an identical `B_SPLINE_CURVE_WITH_KNOTS` geometry but each referencing its own `EDGE_CURVE` and `ORIENTED_EDGE`.

## Expected kernel behavior

heal — merge coincident vertices/edges within tolerance; downgrade cracks to gaps then run gap pipeline. After healing, edge ancestor-count = 2 for shared boundaries.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**Originally also catalogued as**: Twi015, Gs020. **See also**: Tsh042, Twi043.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(18)` |
| `gmsh_autofix_off` | `shape(18)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 8
- **n_vertices_total**: 16
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000001.00000001 | 4 |
| 1 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 8`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(18) ifc=schema_n/a` |

## Cross-references

**See also**: [Tsh042](Tsh042.md), [Twi043](Twi043.md)

**Originally also catalogued as**: Twi015, Gs020
