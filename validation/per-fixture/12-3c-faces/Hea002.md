# Hea002 — Solid-level healing on MANIFOLD_SOLID_BREP wrapping an OPEN_SHELL (instead of CLOSED_SHELL): iterate shell fixes and re-evaluate closure

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Hea002.stp`](../../step-examples/12-3c-faces/Hea002.stp)  

## Sources
- OCCT `ShapeFix_Solid::Perform` (`ShapeFix_Solid.hxx:56`)

## Description

A MANIFOLD_SOLID_BREP built from one or more shells with internal defects, in particular one whose `outer` slot wraps an OPEN_SHELL rather than a CLOSED_SHELL — a manifold solid requires a closed shell. The solid-level orchestrator must propagate fixes inward to shells/faces and then re-evaluate solid-level invariants (closure, orientation toward outer void, finiteness).

## Reproducer recipe

A MANIFOLD_SOLID_BREP whose outer shell is an OPEN_SHELL (not a CLOSED_SHELL); the kernel must detect the non-closure and either decline to construct the solid or insert capping shells.

## Expected kernel behavior

shell-by-shell healing followed by solid-level closure check; reject if the post-fix shells still don't bound a finite volume.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Search terms: "non-watertight solid", "open solid", "leaky solid", "non-manifold solid".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 18) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
