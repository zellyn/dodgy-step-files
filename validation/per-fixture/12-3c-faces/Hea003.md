# Hea003 — Free-bound properties pipeline computes area/perimeter/notches together

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Hea003.stp`](../../step-examples/12-3c-faces/Hea003.stp)  

## Sources
- OCCT `ShapeAnalysis_FreeBoundsProperties::Perform` (`ShapeAnalysis_FreeBoundsProperties.hxx:95`)

## Description

A shell with one or more free bounds (open boundaries). The kernel's free-bound analyzer must compute, per bound, the area of the contour, perimeter, average length-to-width ratio, average width, and notch list — all derived from the same boundary representation in one pass to avoid recomputing.

## Reproducer recipe

An open shell containing a single rectangular face; the analyzer reports a single free bound with width 10, length 10, area 100, no notches.

## Expected kernel behavior

produce a structured per-bound report; downstream tools filter by area/perimeter/ratio thresholds.

## Notes

Replaces ad-hoc per-property queries with a unified pipeline.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
