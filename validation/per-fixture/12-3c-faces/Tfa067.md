# Tfa067 — Top face dropped after STEP import (CLOSED_SHELL cube whose top FACE_OUTER_BOUND edge has 3D LINE vs B_SPLINE_CURVE_WITH_KNOTS pcurve mismatch)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa067.stp`](../../step-examples/12-3c-faces/Tfa067.stp)  

## Sources
- OCCT MANTIS#0023473
- OCCT MANTIS#0024595
- OCCT MANTIS#0030052
- OCCT MANTIS#0029415
- OCCT MANTIS#0025045

## Description

A solid imported from STEP loses one or more faces during translation. Common cause: the face's `FACE_OUTER_BOUND` references an EDGE_LOOP whose edge translation fails silently — e.g., a 3D LINE going one way while the associated PCURVE is a degree-2 B_SPLINE_CURVE_WITH_KNOTS with control points that dip in V, producing a 3D-curve / pcurve mismatch beyond tolerance — so the entire face is dropped. Visible in viewers as a hole in the otherwise-solid part.

## Reproducer recipe

A CLOSED_SHELL six-face cube where the top face's EDGE_LOOP has one edge whose 3D LINE (e.g., (0,0,1)->(1,0,1)) and PCURVE (a B_SPLINE_CURVE_WITH_KNOTS in UV with a 0.1 dip in V) disagree by ~0.1 mm. STEP reader silently drops the top face; resulting shell has 5 faces.

## Expected kernel behavior

face-translation failures must be reported as diagnostics (not silently dropped); if all bounds cannot be resolved, mark the face as invalid but preserve it in the topology so downstream code can heal or report.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: Tsh001, Tsh044.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(27)` |
| `gmsh_autofix_off` | `shape(27)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 6
- **n_edges_total**: 24
- **n_vertices_total**: 48
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |
| 1 | plane | 1.0 | 5000001.000000001 | 4 |
| 2 | plane | 1.0 | 5000000.999856222 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 24`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `beyond-10` (rank 21) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(27) ifc=schema_n/a` |

## Cross-references

**See also**: [Tsh001](Tsh001.md), [Tsh044](Tsh044.md)
