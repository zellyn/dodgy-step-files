# Tfa048 — Strip face flagged using caller-supplied tolerance

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3c (`12-3c-faces`)  
**Fixture**: [`step-examples/12-3c-faces/Tfa048.stp`](../../step-examples/12-3c-faces/Tfa048.stp)  

## Sources
- OCCT `ShapeAnalysis_CheckSmallFace::CheckStripFace` (`ShapeAnalysis_CheckSmallFace.hxx:119`)

## Description

A face that is a strip only at tolerances larger than its own edge tolerances. The caller supplies a tolerance band; the analyzer determines whether the face fits the band (its width is less than the supplied tolerance). Permits queries like "is this face a strip at 1mm tolerance?" independent of its own tolerance metadata.

## Reproducer recipe

A 100x0.5 face whose edge tolerances are 1e-7 — only a strip at tolerances ≥ 0.5.

## Expected kernel behavior

parameterize the strip check with a caller-supplied tolerance; report direction and bounding edges.

## Notes

Generalization of Tfa047 with explicit tolerance.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 50.0 | 500000000.99999994 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].sliver_aspect_max_min > 1e3`
- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
