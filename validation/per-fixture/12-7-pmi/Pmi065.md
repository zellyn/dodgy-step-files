# Pmi065 — DDP / EER cross-file PMI references break on repackaging

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.7 (`12-7-pmi`)  
**Fixture**: [`step-examples/12-7-pmi/Pmi065.stp`](../../step-examples/12-7-pmi/Pmi065.stp)  

## Sources
- 17-standards-bodies (M030), 02-caxif (External Reference v3.1 §7.5.2)

## Description

AP242 supports referencing geometric elements (a specific face, edge) in a separate Part 21 file via `component_path_shape_aspect` / External Element Reference / DDP linking. When the package is repacked or split, the references break — PMI in file A points to a face in file B that no longer exists or has a different ID. Additionally, `component_path_shape_aspect.of_shape.product_definition_shape.definition` must reference the root product_definition; the leaf-end shape_aspect's `of_shape.definition` must reference the leaf product_definition. Mid-tree anchors are wrong.

## Reproducer recipe

DDP archive where assembly-level PMI in `assembly.stp` references face 47 in `part_3.stp`; rename/re-export `part_3.stp` and the link breaks. Or set `of_shape` to point at L-Bracket-Assembly (intermediate) when it should point at AS1 (root).

## Expected kernel behavior

Archive-stable requires DDP integrity check; reject if cross-file refs do not resolve. Verify the of_shape↔root invariant and reject mid-tree anchors. Persistent IDs (Pmi021) are the intended mitigation.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**See also**: A013, N044.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [A013](A013.md), [N044](N044.md)
