# Pmi072 — `DIMENSION_PAIR` with mismatched units between paired members

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.7 (`12-7-pmi`)  
**Fixture**: [`step-examples/12-7-pmi/Pmi072.stp`](../../step-examples/12-7-pmi/Pmi072.stp)  

## Sources
- 16-iso-spec AP242 §DIMENSION_PAIR

## Description

AP242 DIMENSION_PAIR couples two dimensions that are conceptually the same measurement (often one primary, the other a secondary or check) so downstream tolerance stack-up consumes them as a unit. The spec does not forbid mismatched units between the two halves; a producer that sources one half from an inch-based legacy CAD library and the other from a millimetre-based current export emits the pair with mixed units. The pair is mathematically inconsistent unless the receiver normalises units before comparing.

## Reproducer recipe

DIMENSION_PAIR(`width`,`height`); width MEASURE_REPRESENTATION_ITEM in mm; height MEASURE_REPRESENTATION_ITEM in inches via CONVERSION_BASED_UNIT.

## Expected kernel behavior

convert both halves to a canonical unit before comparison; warn on mismatched units; never silently treat the two values as if they were in the same unit.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- disallowed: `silent-accept`

## Notes

**See also**: U-family unit-mismatch entries. **OCC behavior**: silently consumes both halves of the `DIMENSION_PAIR` without unit normalisation or diagnostic; kernel mishandling — the catalog above forbids silent same-unit treatment.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
