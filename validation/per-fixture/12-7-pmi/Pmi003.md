# Pmi003 — Stacked vs composite tolerance frames mis-tagged

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.7 (`12-7-pmi`)  
**Fixture**: [`step-examples/12-7-pmi/Pmi003.stp`](../../step-examples/12-7-pmi/Pmi003.stp)  

## Sources
- 02-caxif (PMI v4.1 §10.1.1.2 Fig 108), 11-translator-vendors (W026)

## Description

Multiple `geometric_tolerance` rows grouped only for visual presentation ("stacked") are semantically independent. True "composite" tolerances must be linked by a `geometric_tolerance_relationship` whose `name` is `'composite'`. Writers that always emit `geometric_tolerance_relationship` with `name='composite'` (or who flatten composite frames into independent tolerances on export) inflate or destroy composite counts.

## Reproducer recipe

Two parallelism tolerances drawn one above the other in the same FCF, written with `geometric_tolerance_relationship('composite', t1, t2)` even though they are merely stacked. Conversely, a true composite position tolerance written as two unrelated `position_tolerance` instances.

## Expected kernel behavior

Use the `name='composite'` relationship strictly for true composite tolerances; preserve composite frame structure across round-trip.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
