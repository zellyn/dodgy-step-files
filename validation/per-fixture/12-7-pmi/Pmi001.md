# Pmi001 — Hole emitted as two half-cylinders breaks PMI feature association

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.7 (`12-7-pmi`)  
**Fixture**: [`step-examples/12-7-pmi/Pmi001.stp`](../../step-examples/12-7-pmi/Pmi001.stp)  
**Sender**: any writer that emits cylindrical holes as two `advanced_face` halves  

## Sources
- 02-caxif (PMI v4.1 §5.1, Fig.5
- §10.1.3.1), 02-caxif (CAx-IF Round 56J §2.6.4.3 PDC, §2.7 PDI)

## Description

A through-hole exported as two `advanced_face` instances backed by halves of a `cylindrical_surface`. PMI (diameter dimensions, position tolerances, datums) targeting the whole hole must reach both halves through a shared `shape_aspect` plus multiple `geometric_item_specific_usage`. Persistent UUIDs must be assigned to the pair as a `uuid_set_items` group, not one per half. Receivers that expect one face per cylindrical feature drop or duplicate the PMI link.

## Reproducer recipe

Block with one through-hole; emit two half-`advanced_face`s sharing the same `cylindrical_surface` axis with opposite parameter ranges; attach diameter `dimensional_size` and a position `geometric_tolerance` whose `shape_aspect` is referenced by two GISUs; bind a single `v5_uuid_attribute` via `uuid_set_items` to both halves.

## Expected kernel behavior

heal — recognize the two halves as one cylindrical feature (or honor multi-GISU on the shared `shape_aspect`); merge by `uuid_set_items` membership before reporting feature counts.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: N044.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(2)` |
| `gmsh_autofix_off` | `shape(2)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | cylinder | 4e+101 | 1.0 | 0 |
| 1 | cylinder | 4e+101 | 1.0 | 0 |

## Tier-3 assertions (machine-checked)

- `face[0].surface_type == "cylinder"`
- `face[1].surface_type == "cylinder"`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(2) ifc=schema_n/a` |

## Cross-references

**See also**: [N044](N044.md)
