# M024 — Mesh-to-BRep gaps at trim curves (non-watertight result)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.8 (`12-8-mixed`)  
**Fixture**: [`step-examples/12-8-mixed/M024.stp`](../../step-examples/12-8-mixed/M024.stp)  

## Sources
- 21-patents.md (X012)
- US 2021/0263499 A1

## Description

When a mesh is fitted with NURBS patches to produce a B-rep (reverse engineering or scan-to-CAD), boundary mesh edges are constrained to lie on trim curves. Independent fitting of adjacent patches leaves gaps and overlaps along those curves; the resulting B-rep is non-watertight even though visually closed.

## Reproducer recipe

Convert an STL of a scan to STEP via patch fitting; check `ADVANCED_FACE` edges for tolerance-exceeding gaps to neighbour faces.

## Expected kernel behavior

heal — use a single shared trim curve fitted to a tighter tolerance than the patch interior; enforce watertightness as a fitting constraint, not a cleanup step.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**OCC behavior**: the validity-checker does not flag sub-tolerance trim-curve gaps in the mesh-to-BRep result; tier-3 valid-flag should be ignored for this entry. The reader most likely silently fixes or ignores the gap on import.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(18)` |
| `gmsh_autofix_off` | `shape(18)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 8
- **n_vertices_total**: 16
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |
| 1 | plane | 1.0 | 5000001.000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 8`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-10` (rank 5) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(18) ifc=schema_n/a` |
