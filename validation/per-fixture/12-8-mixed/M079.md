# M079 — AP209 boundary condition with no nodes attached

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.8 (`12-8-mixed`)  
**Fixture**: [`step-examples/12-8-mixed/M079.stp`](../../step-examples/12-8-mixed/M079.stp)  
**Sender**: deduced — FEA exporter dropping node-set definitions  

## Sources
- deduced from AP209 §7 constraint association rules

## Description

A `SINGLE_POINT_CONSTRAINT` is declared with a freedoms list and a non-zero `b` value, but the `NODE_GROUP` it points at is empty (or the constrained_node attribute is unresolved). The constraint applies to no degrees of freedom. Bug-reporter language: "AP209 boundary condition has no geometry", "FEM constraint applied to nothing", "SPC node group empty".

## Reproducer recipe

`NODE_GROUP('fixed_nodes', (), $)` (empty) referenced by `SINGLE_POINT_CONSTRAINT(...)`.

## Expected kernel behavior

detect unattached constraints; warn and skip, or reject as malformed; never silently apply to all nodes.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, warn-and-proceed`
- disallowed: `silent-accept`

## Notes

**See also**: M037, M038. **OCC behavior**: silently consumes the AP209 file as a B-rep producer (FEM constraints are ignored entirely on the read path) without surfacing that the empty-node-group constraint was abandoned; kernel mishandling — the catalog above forbids silent application.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [M037](M037.md), [M038](M038.md)
