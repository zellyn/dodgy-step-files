# Os022 — FindContigousEdges: paired LINEs/VECTORs from sewing have unequal parameter lengths (unit vs non-unit DIRECTION) for the same 3D extent

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.8 (`12-8-mixed`)  
**Fixture**: [`step-examples/12-8-mixed/Os022.stp`](../../step-examples/12-8-mixed/Os022.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT BRepOffsetAPI_FindContigousEdges (uncovered class evidence)

## Description

A "find-contiguous-edges" pass identifies free edges that *would* match across faces, but the pairs have different parameter lengths even though their 3D extents are similar. Typical fixture: two LINE entities from the same origin — one with a unit-magnitude VECTOR along a unit DIRECTION, the other with a unit-magnitude VECTOR along a non-unit DIRECTION (e.g., (0.5,0,0)) — covering the same 3D extent but with different parameter lengths. Bug-reporter language: "contiguous edges mismatched length", "free edge pair length differ", "non-unit DIRECTION breaks parameterization".

## Reproducer recipe

Two LINE/VECTOR pairs both running between the same two 3D points, one along a unit DIRECTION and one along a non-unit DIRECTION — so parameterized lengths differ even though 3D extents match.

## Expected kernel behavior

Reparameterize the shorter edge; emit a warning.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `warn-and-proceed`

## Notes

Synonyms: "free edge pair length mismatch".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
