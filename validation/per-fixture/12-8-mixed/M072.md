# M072 — AP238 toolpath_curve open chain claimed as closed contour

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.8 (`12-8-mixed`)  
**Fixture**: [`step-examples/12-8-mixed/M072.stp`](../../step-examples/12-8-mixed/M072.stp)  
**Sender**: deduced — STEP-NC CAM post-processor  

## Sources
- deduced from AP238 §6.2 trajectory and basiccurve invariants

## Description

A `TRAJECTORY` references a `COMPOSITE_CURVE` whose `self_intersect = .F.` and which is consumed as a closed contouring pass, but the underlying `COMPOSITE_CURVE_SEGMENT` chain does not actually close in 3D (the last segment's endpoint != the first segment's startpoint). Bug-reporter language: "toolpath should close but doesn't", "contour pass leaves a gap", "STEP-NC malformed toolpath_curve".

## Reproducer recipe

A `COMPOSITE_CURVE` listing two `COMPOSITE_CURVE_SEGMENT`s that form an "L" (e.g. line p1->p2, line p2->p3), referenced by a `TRAJECTORY` as if it were a closed loop.

## Expected kernel behavior

validate continuity / closure of the segment chain before treating as a closed contour; emit a synthesized closing segment, or reject as malformed, or treat as an open pass.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Synonyms: "non-closed toolpath", "trajectory doesn't loop".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
