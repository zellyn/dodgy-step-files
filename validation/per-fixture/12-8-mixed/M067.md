# M067 — Transformation relation creation failed (AXIS2_PLACEMENT_3D with zero-vector DIRECTION ratios in ITEM_DEFINED_TRANSFORMATION); placement falls back to identity

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.8 (`12-8-mixed`)  
**Fixture**: [`step-examples/12-8-mixed/M067.stp`](../../step-examples/12-8-mixed/M067.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT STEPControl_ActorRead.cxx:2179, 2183 (transformation relation handling) (uncovered class evidence)

## Description

A child component's transformation reference (e.g., `ITEM_DEFINED_TRANSFORMATION`) cannot be resolved to a valid 4×4 matrix; the reader falls back to identity placement. The child appears at the wrong location. Bug-reporter language: "child placement wrong", "STEP transformation creation failed", "components stack at origin".

## Reproducer recipe

An NAUO whose transformation references an `ITEM_DEFINED_TRANSFORMATION` whose `transform_item_2` axis directions are zero vectors.

## Expected kernel behavior

Reject the assembly placement as malformed; do not silently use identity.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

Synonyms: "child at origin bug". **OCC behavior**: silently substitutes the identity placement when the transformation reference cannot be resolved, leaving children at the origin without a diagnostic; kernel mishandling — the catalog above forbids silent identity-substitution.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
