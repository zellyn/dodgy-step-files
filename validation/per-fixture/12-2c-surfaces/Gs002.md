# Gs002 — Degenerate (lemon/apple) torus where minor_radius >= major_radius

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2c (`12-2c-surfaces`)  
**Fixture**: [`step-examples/12-2c-surfaces/Gs002.stp`](../../step-examples/12-2c-surfaces/Gs002.stp)  

## Sources
- G010, G011, G066

## Description

A STEP `TOROIDAL_SURFACE` with minor radius equal to or exceeding major radius collapses the analytic torus into a self-intersecting "lemon" (minor > major, no central hole) or "apple" (minor = major, single pinch point at the centre). Receivers that assume `minor < major` produce overlarge bounding boxes, mis-handle seam insertion, or import a revolved-shape variant inverted.

## Reproducer recipe

`TOROIDAL_SURFACE` with major_radius=2, minor_radius=3; reference from a face with single FACE_OUTER_BOUND covering the full parameter rectangle.

## Expected kernel behavior

accept the analytic surface and treat its parameter range correctly for seam insertion and bbox computation, or reject as malformed (a kernel that requires `minor < major` should refuse rather than silently mis-bound).

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
