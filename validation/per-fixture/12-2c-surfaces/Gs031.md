# Gs031 — `ADVANCED_FACE` with two `FACE_OUTER_BOUND` entries (duplicated outer contour, overlapping pcurves)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2c (`12-2c-surfaces`)  
**Fixture**: [`step-examples/12-2c-surfaces/Gs031.stp`](../../step-examples/12-2c-surfaces/Gs031.stp)  

## Sources
- B004

## Description

A single `ADVANCED_FACE` ends up with two `FACE_OUTER_BOUND` entries — typically both referencing the same `EDGE_LOOP` — when the schema permits only one. Equivalently, the same outer wire is stored twice in the parametric domain so two pcurves trace the same contour. Common cause: rehosting a face onto a new supporting surface and reprojecting all 3D edges, without first deduplicating the wire that already describes the contour.

## Reproducer recipe

Take a trimmed face F1 on surface S1; build F2 on different surface S2 by reprojecting every edge of F1 onto S2 without first deduplicating the outer wire.

## Expected kernel behavior

a face-construction or rehosting pass must ensure exactly one outer wire per face — merge duplicates or reject. A multiply-traced outer wire produces ambiguous parametric trims.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

**Originally also catalogued as**: Gp025. **See also**: Gp014.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Gp014](Gp014.md)

**Originally also catalogued as**: Gp025
