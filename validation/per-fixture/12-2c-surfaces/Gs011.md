# Gs011 — Crossed-trim `EDGE_LOOP` with non-shared diagonals (mesh-derived BRep, STL→STEP)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2c (`12-2c-surfaces`)  
**Fixture**: [`step-examples/12-2c-surfaces/Gs011.stp`](../../step-examples/12-2c-surfaces/Gs011.stp)  
**Sender**: FreeCAD `Part / Create shape from mesh`; other STL→STEP utilities  

## Sources
- J024, X012

## Description

Mesh-to-BRep tools fit `B_SPLINE_SURFACE_WITH_KNOTS` patches per triangle and stitch them; near sharp creases the patches' trim curves self-intersect or overlap. Typical shape: an `EDGE_LOOP` containing two diagonal `EDGE_CURVE`s — for example one running (0,0)→(10,10) and another (0,10)→(10,0) — which cross in UV at the centre and do not share endpoints, so the wire loop is two disjoint crossed diagonals. Output STEP loads but every later Boolean fails.

## Reproducer recipe

STL with creases > ~30°; sew tolerance ≤ 1e-6; resulting `ADVANCED_FACE` contains pcurves that cross.

## Expected kernel behavior

STL→STEP path should emit faceted BRep (`FACETED_BREP` / `OPEN_SHELL`) rather than smooth NURBS; or run self-intersection healing as a final step.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**Originally also catalogued as**: Gn028.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references


**Originally also catalogued as**: Gn028
