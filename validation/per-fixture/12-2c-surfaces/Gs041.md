# Gs041 — `RECTANGULAR_COMPOSITE_SURFACE` with non-uniform patch grid

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2c (`12-2c-surfaces`)  
**Fixture**: [`step-examples/12-2c-surfaces/Gs041.stp`](../../step-examples/12-2c-surfaces/Gs041.stp)  

## Sources
- 16-iso-spec ISO 10303-42 §RECTANGULAR_COMPOSITE_SURFACE

## Description

A RECTANGULAR_COMPOSITE_SURFACE is parameterised as an MxN grid of SURFACE_PATCH children. The U parameter spans across all M patches; V across all N. The spec permits non-uniform parameter intervals (one patch occupies U=0..0.7, the next U=0.7..1.0). Many receivers assume uniform spacing and either pass through with the wrong global parameterisation or trip an internal assertion.

## Reproducer recipe

2x1 RECTANGULAR_COMPOSITE_SURFACE whose left patch occupies U=[0.0, 0.3] and right patch U=[0.3, 1.0] (non-uniform).

## Expected kernel behavior

respect each patch's individual parameter interval; if the kernel internally re-parameterises to uniform, it must record the original mapping so isoparametric queries on the composite return the correct point.

## Notes

Distinct from Gp019 (pcurve-on-RECTANGULAR_COMPOSITE_SURFACE) — this entry is about parameter-grid uniformity, independent of bounding wire.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
