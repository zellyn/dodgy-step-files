# Gs047 — `BLENDED_EDGE_SURFACE` with mismatched fillet radii at a shared vertex

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2c (`12-2c-surfaces`)  
**Fixture**: [`step-examples/12-2c-surfaces/Gs047.stp`](../../step-examples/12-2c-surfaces/Gs047.stp)  

## Sources
- 16-iso-spec AP242 §BLENDED_EDGE_SURFACE

## Description

AP242 introduces BLENDED_EDGE_SURFACE to represent an edge fillet between two adjacent faces. The blend's radius (or radius profile) is one of the entity's defining attributes. A producer that supports variable-radius fillets but exports through a constant-radius schema may emit two BLENDED_EDGE_SURFACE instances for adjacent edges with mismatched radii at their shared vertex; the receiver sees a 0-thickness gap where they meet.

## Reproducer recipe

two CYLINDRICAL_SURFACE blends meeting at the origin: blend A radius 1.0, blend B radius 2.0. At the shared vertex the two fillet surfaces cannot meet smoothly.

## Expected kernel behavior

reconcile the radii by transitioning between them through a third spherical / variable-radius patch, or reject as inconsistent.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

First entry exercising AP242 BLENDED_EDGE_SURFACE.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
