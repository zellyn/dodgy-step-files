# Gp028 — Wire crosses periodic-surface seam without an explicit seam edge (pcurve trim range vs vertex angular position mismatch)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2a (`12-2a-pcurves`)  
**Fixture**: [`step-examples/12-2a-pcurves/Gp028.stp`](../../step-examples/12-2a-pcurves/Gp028.stp)  

## Sources
- 08-occt-gitlog.md G111 (Mantis 0026708)

## Description

A wire bounding a face on a periodic `CYLINDRICAL_SURFACE` (or torus, etc.) traverses the U=0 / U=2π seam, but the input does not include the seam edge that would carry the wire from one side of the seam to the other. Typical signature: pcurve `LINE` trim range (e.g. starts at u=5.5 with length 1.28) corresponds to a different angular interval on the cylinder than the 3D vertex positions imply (vertex angles around -0.78 rad and +0.50 rad), so pcurve and 3D-vertex angular positions disagree. Downstream shell composition sees an open contour in UV even though the wire is closed in 3D.

## Reproducer recipe

STEP face where an outer wire of a cylindrical face crosses the U=0/U=2π seam direction without an explicit seam edge.

## Expected kernel behavior

detect that the wire crosses the seam and insert the seam edge automatically, or reject as malformed.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**See also**: Gp026.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Gp026](Gp026.md)
