# Gp010 — Surface_curve.associated_geometry contains a 3D curve in lieu of pcurve

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2a (`12-2a-pcurves`)  
**Fixture**: [`step-examples/12-2a-pcurves/Gp010.stp`](../../step-examples/12-2a-pcurves/Gp010.stp)  
**Sender**: Parasolid-origin files  

## Sources
- 11-translator-vendors.md W041 (CAD Exchanger v3.24.11)
- 14-brlcad-rhino.md K014 (PCurve.cpp:108)

## Description

A `SURFACE_CURVE`'s `associated_geometry` slot uses a 3D curve (e.g. `LINE`) lying in a `PLANE` rather than a `PCURVE` with 2D parameters. Mathematically valid for flat surfaces, but importers that hardcode the 2D form fail; openNURBS-style consumers cannot evaluate `PointAtStart`/`PointAtEnd` because the path through pcurve→surface is unimplemented.

## Reproducer recipe

`SURFACE_CURVE('',#3dcurve,(#assoc_geom),.PCURVE_S1.)` where `#assoc_geom` is a 3D `LINE` lying in the host `PLANE`, instead of a `PCURVE`.

## Expected kernel behavior

accept — derive 2D parameter curve from 3D curve when the surface is flat; for true pcurves, evaluate in (u,v) and lift via basis surface.

## Notes

**See also**: Gn030, Gp001.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Gn030](Gn030.md), [Gp001](Gp001.md)
