# Gp019 — Edge on a composite-surface face is missing per-patch pcurve

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2a (`12-2a-pcurves`)  
**Fixture**: [`step-examples/12-2a-pcurves/Gp019.stp`](../../step-examples/12-2a-pcurves/Gp019.stp)  

## Sources
- 05-occt-shapefix.md F068 (`ShapeFix_ComposeShell.hxx:127`)

## Description

A face is built on a composite surface (a grid of patches as in `RECTANGULAR_COMPOSITE_SURFACE`), but an edge that bounds a sub-face of the composite does not have a pcurve on the relevant sub-patch. The composite shell builder cannot decompose the face into per-patch sub-faces without the per-patch pcurve.

## Reproducer recipe

A face on a composite surface containing an `EDGE_CURVE` whose `SURFACE_CURVE.associated_geometry` is empty for the relevant sub-patch.

## Expected kernel behavior

synthesise the per-patch pcurves by projecting the 3D curve onto each sub-surface, or reject the face when projection fails. Composite surface decomposition without per-patch pcurves is undefined.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21. **See also**: Gp001, Tfa036.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `signal(11)` |
| `occt_heal_off` | `signal(11)` |
| `gmsh_autofix_on` | `signal(11)` |
| `gmsh_autofix_off` | `signal(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `process_signal` |

**Cross-oracle classification**: Spec-clean Part-21; OCC crashes (kernel-side bug)

## Tier-3 geometric introspection

- **load**: `None`
- **n_faces**: 0
- **n_edges_total**: None
- **n_vertices_total**: None
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=signal(11)/signal(11) gmsh=signal(11) ifc=schema_n/a` |

## Cross-references

**See also**: [Gp001](Gp001.md), [Tfa036](Tfa036.md)
