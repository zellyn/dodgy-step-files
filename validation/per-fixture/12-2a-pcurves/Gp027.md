# Gp027 — Closed-face splitter leaves new pcurves out of sync with 3D curves on `CYLINDRICAL_SURFACE`

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2a (`12-2a-pcurves`)  
**Fixture**: [`step-examples/12-2a-pcurves/Gp027.stp`](../../step-examples/12-2a-pcurves/Gp027.stp)  

## Sources
- 13-quaoar.md B008

## Description

A pass that splits closed periodic faces / solids (full `CYLINDRICAL_SURFACE`, `TOROIDAL_SURFACE`) along their seams produces new edges whose pcurves do not satisfy the same-parameter invariant — pcurve and 3D curve disagree at the same parameter value. Typical pattern: an `EDGE_CURVE` between vertices (10,0,0) and (-10,0,0) on a cylinder traverses the seam from u=0 to u=π in 3D, but the pcurve `LINE` is parameterised over [0, 1] instead of [0, π] — same UV path, different parameterisation, so the seam/branch handling is wrong. A topology checker reports invalid edges. The splitter returns a topologically inconsistent intermediate state.

## Reproducer recipe

Take a full cylinder solid; split along its seam; run a topology checker — invalid edges flagged. Recompute same-parameter on the result — becomes valid.

## Expected kernel behavior

a face-splitter must restore the same-parameter invariant on the new edges before returning, or document that callers must do so. Returning an invalid intermediate burdens every caller.

## Notes

**See also**: Gp022, Twi032.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Gp022](Gp022.md), [Twi032](Twi032.md)
