# A004 — `NEXT_ASSEMBLY_USAGE_OCCURRENCE` references missing `PRODUCT_DEFINITION` (unresolved NAUO target)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A004.stp`](../../step-examples/12-6-assembly/A004.stp)  
**Sender**: any producer that splits assembly into per-component files; PTC Creo with referenced (not converted) components.  

## Sources
- M013 (LOTAR), K022 (Rhino "successfully read" but components missing), V050 (Creo "Reference" mode), V034 (Inventor unresolved external paths).

## Description

A `NEXT_ASSEMBLY_USAGE_OCCURRENCE` whose `related_product_definition` slot points at an entity id (e.g. `#99999`) that doesn't exist in the file — the partner component was lost, the path was unreachable, or the component was filtered out. Many readers report "successfully read" while silently dropping the component.

## Reproducer recipe

Split a multi-product assembly into per-component STEP files; remove one of the partners; reload the parent.

## Expected kernel behavior

reject as invalid 10303-21 reference, or accept and emit a per-product diagnostic with non-zero skipped-count return.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Cross-oracle: pure-Python Part-21 validator rejects (reject(E_UNRESOLVED_REFS)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_UNRESOLVED_REFS)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `occt_diag_off` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `gmsh_diag` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |

**Cross-oracle classification**: OCC silently heals — pure-Python rejects this

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator rejects (reject(E_UNRESOLVED_REFS)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
