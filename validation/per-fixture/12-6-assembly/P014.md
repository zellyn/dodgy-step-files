# P014 — `PCURVE` start point shifted in V from 3D `EDGE_CURVE` lift (UV drift beyond tolerance after Boolean)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/P014.stp`](../../step-examples/12-6-assembly/P014.stp)  
**Sender**: FreeCAD post-Boolean  

## Sources
- FreeCAD #21216

## Description

Faces emitted after a complex Boolean cut carry `SURFACE_CURVE` whose `PCURVE` lies off the surface domain — beyond geometric tolerance. Common signature: a planar `ADVANCED_FACE` whose 3D edge runs from the origin along +X but whose pcurve seed 2D point starts at (u=0.0, v=0.5), a 0.5 mm drift in V between the 2D pcurve and the 3D edge lift. The source shape validates clean inside the producing kernel, but on STEP re-import every face/edge is flagged "invalid curve on surface" — the round-trip introduced UV drift the source did not have.

## Reproducer recipe

`MANIFOLD_SOLID_BREP` where each `ADVANCED_FACE` carries `SURFACE_CURVE` with both 3D `CURVE` and 2D `PCURVE` whose pcurve drift exceeds tolerance.

## Expected kernel behavior

Auto-project pcurve back to surface (within tolerance); or warn and accept.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `warn-and-proceed`

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 13) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
