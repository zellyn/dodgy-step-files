# A072 — Compound with single vertex written as compound-of-two-points

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A072.stp`](../../step-examples/12-6-assembly/A072.stp)  

## Sources
- OCCT MANTIS#0023379
- OCCT MANTIS#0013175

## Description

A document contains a single isolated vertex (e.g., a reference point). The STEP writer emits both a `CARTESIAN_POINT` and a redundant `VERTEX_POINT` at top level, producing a compound-of-two-points on re-import.

## Reproducer recipe

XCAF document with one TopoDS_Vertex at coords `(0,0,0)`. Write to STEP. Reader sees two points.

## Expected kernel behavior

a single vertex is emitted as one `VERTEX_POINT` referenced from one shape representation; never duplicate.

## Notes

Provenance tier: writer-side — writer emits compound-of-two-points instead of single vertex.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `writer-side` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
