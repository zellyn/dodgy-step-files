# A029 — User-Defined Attribute (UDA) target-binding inconsistency

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A029.stp`](../../step-examples/12-6-assembly/A029.stp)  

## Sources
- M014 (CAx-IF UDA v1.8).

## Description

Same UDA template implemented differently across CAD systems — bound to `product`, to `product_definition`, to `next_assembly_usage_occurrence`, or to a shape aspect. Round-trip loses the binding because the receiver expects a different target.

## Reproducer recipe

NX writes UDA on `product_definition`; CATIA reader expects it on `next_assembly_usage_occurrence`; data is ignored on reimport.

## Expected kernel behavior

preserve UDA verbatim using the rec-practice template; do not silently re-bind.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- disallowed: `silent-accept`

## Notes

**See also**: A016. **OCC behavior**: silently drops UDAs whose target binding does not match its expectations rather than preserving them verbatim; kernel mishandling — the catalog above forbids silent re-binding (or silent loss).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [A016](A016.md)
