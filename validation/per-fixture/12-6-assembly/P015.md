# P015 — Non-manifold or open shell exported as `MANIFOLD_SOLID_BREP`

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/P015.stp`](../../step-examples/12-6-assembly/P015.stp)  
**Sender**: FreeCAD (PartDesign with SubShapeBinder offsets)  

## Sources
- FreeCAD #25774, #16292, #26639

## Description

A solid that is closed in the producer is wrapped as `MANIFOLD_SOLID_BREP` but at least one edge appears only once across the shell, or one face's `FACE_OUTER_BOUND` orientation is inverted — so the result is not actually manifold. Re-import yields zero solids; STL still works because tessellation ignores topology.

## Reproducer recipe

STEP file with all faces individually valid but at least one edge appearing only once; or one face with inverted outer-bound orientation. Optionally drop `MANIFOLD_SOLID_BREP` wrapper and emit only `SHELL_BASED_SURFACE_MODEL`.

## Expected kernel behavior

Either heal to solid, or expose explicitly as open shell with warning.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal, warn-and-proceed`

## Notes

**See also**: M052.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(3)` |
| `gmsh_autofix_off` | `shape(3)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(3) ifc=schema_n/a` |

## Cross-references

**See also**: [M052](M052.md)
