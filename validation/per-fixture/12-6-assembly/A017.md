# A017 — Component labels/colors/names lost when subshape carries a transform on round-trip

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A017.stp`](../../step-examples/12-6-assembly/A017.stp)  

## Sources
- I033 (build123d #1014: Pos*Rot drops labels), I036 (build123d #954: nested compound labels via STEPControl_Writer), I006 (OCCT #541: STEP re-export loses XCAF colors), F056/F084/B028 (orphan TopLoc_Location on subshapes), B001 (spurious face-level locations from STEP round-trip).

## Description

A component carries a placement transform attached at the face / edge / vertex level rather than at the assembly node, and the writer's component-attribution path expects identity transforms on leaf subshapes. Names, colors, and other attached metadata bound to the inner subshape are lost in the STEP round-trip, or end up bound to the wrong product instance. Symptom: a translated/rotated component reimports as untitled, uncoloured geometry; or nested compounds (Root → Arm → Cube) drop their inner-level labels.

## Reproducer recipe

face with a non-identity transform reinserted into a shell; export to STEP; reimport and observe a near-identity transform on the leaf face plus missing color/name attributes. Or build a two-level Compound (Root → Arm → Cube) and observe inner-level labels missing from the resulting STEP file.

## Expected kernel behavior

preserve component metadata across round-trip regardless of where the transform is attached. Either absorb the subshape transform into the underlying surface/curve before write, or fold the metadata down to the level where the writer can attach it. Identity-collapsing on read keeps round-trips stable.

## Notes

**See also**: A024.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(1)` |
| `gmsh_autofix_off` | `shape(1)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 8e+100 | 1.0000000000000001e+107 | 0 |

## Tier-3 assertions (machine-checked)

- `face[0].sliver_aspect_max_min > 1e6`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `undetected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(1) ifc=schema_n/a` |

## Cross-references

**See also**: [A024](A024.md)
