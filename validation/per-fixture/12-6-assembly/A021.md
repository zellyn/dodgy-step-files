# A021 — CDORSI WR1 violated by per-component styling in Saved View

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A021.stp`](../../step-examples/12-6-assembly/A021.stp)  

## Sources
- CAx-IF PMI v4.1 §9.4.2.3 (entry from 02-caxif).

## Description

Hiding or restyling a single assembly component within a Saved View uses `context_dependent_over_riding_styled_item` whose `style_context` mixes one `mapped_item` with one or more `representation_relationship`s. This violates CDORSI WR1 ("only one rep_item OR all are mapped_items OR all are representation_relationships"). The recommendation is to do it anyway.

## Reproducer recipe

Two-level assembly; Saved View where one leaf component is hidden by a CDORSI whose `style_context` is `(mapped_item, representation_relationship_1, representation_relationship_2)`.

## Expected kernel behavior

accept this pattern despite the WR1 violation.

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `signal(11)` |
| `occt_heal_off` | `signal(11)` |
| `gmsh_autofix_on` | `signal(11)` |
| `gmsh_autofix_off` | `signal(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `process_signal` |

**Cross-oracle classification**: Spec-clean Part-21; OCC crashes (kernel-side bug)

## Tier-3 geometric introspection

- **load**: `None`
- **n_faces**: 0
- **n_edges_total**: None
- **n_vertices_total**: None
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | `undetected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=signal(11)/signal(11) gmsh=signal(11) ifc=schema_n/a` |
