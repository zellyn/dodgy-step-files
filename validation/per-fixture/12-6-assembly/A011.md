# A011 — Naming collision: same component referenced by colliding `PRODUCT.name` across sub-assemblies

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A011.stp`](../../step-examples/12-6-assembly/A011.stp)  
**Sender**: KiCad STEP writer; large-assembly producers that emit one PRODUCT per use site instead of reusing one PRODUCT with N NAUOs.  

## Sources
- I021/I077 (CadQuery on KiCad mobo-3D), J018 (SolidWorks/AutoCAD "Duplicate Definition of Block"), V050.

## Description

STEP exports either reuse one sub-model for many capacitors etc. (KiCad), or emit the same physical component as multiple distinct `PRODUCT_DEFINITION`s (one per sub-assembly use site). Receivers keyed on label-as-primary-key (CadQuery) raise ValueError; AutoCAD reports "Duplicate Definition of Block 1 ignored". FreeCAD silently appends an index.

## Reproducer recipe

STEP with N NAUO rows referencing one PRODUCT named "Cap"; load via `cadquery.Assembly.load`. Or 1000 distinct PRODUCTs named "Bolt_001" with bit-identical geometry.

## Expected kernel behavior

treat entity IDs (`#NNN`) as primary keys; names are descriptive; disambiguate names on import; never fail on duplicates. Detect by-geometry duplicates; offer to deduplicate on import.

## Notes

**See also**: N044.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [N044](N044.md)
