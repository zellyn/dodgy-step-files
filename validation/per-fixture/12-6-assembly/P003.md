# P003 — Control characters / illegal-XML bytes in name strings corrupt downstream documents

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/P003.stp`](../../step-examples/12-6-assembly/P003.stp)  
**Sender**: KiCad or its STEP add-on (TO-92 footprint package names)  

## Sources
- FreeCAD #22123

## Description

A STEP file whose `PRODUCT` / `NEXT_ASSEMBLY_USAGE_OCCURRENCE` name fields contain bytes that are illegal in downstream serialization (control chars `\x00`–`\x1F`, lone surrogates, unescaped `<`/`&`/`>`, invalid UTF-8 continuation bytes) imports without complaint, but a save-and-reopen of the receiving document fails to parse.

## Reproducer recipe

Produce a STEP file with names containing raw control bytes, lone surrogates, or characters that become illegal XML when serialized.

## Expected kernel behavior

Sanitize/escape names at parse time; reject obviously invalid bytes with a warning.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal, warn-and-proceed`

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
