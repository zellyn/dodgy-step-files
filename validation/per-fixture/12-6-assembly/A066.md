# A066 — `UpdateAssemblies` produces wrong compounds for located roots

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A066.stp`](../../step-examples/12-6-assembly/A066.stp)  

## Sources
- OCCT MANTIS#0029282
- OCCT MANTIS#0031517

## Description

An assembly's `UpdateAssemblies` pass rebuilds top-level compounds from current sub-assembly state. When the root assembly itself carries a non-identity location, the rebuild applies the location once at the leaf level and again at the root, producing parts displaced twice from origin.

## Reproducer recipe

Assembly tree where root has a translation `(100,0,0)` and one child has a translation `(50,0,0)`. After `UpdateAssemblies`, the leaf appears at `(200,0,0)` (sum applied at both levels) instead of `(150,0,0)`.

## Expected kernel behavior

location-chain composition follows the hierarchy strictly once; UpdateAssemblies recomputes from leaves up, honouring identity at each level it climbs.

## Notes

**See also**: U030.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [U030](U030.md)
