# A085 — STEP exporter loses `COMPSOLID` in nonmanifold writes

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A085.stp`](../../step-examples/12-6-assembly/A085.stp)  

## Sources
- OCCT MANTIS#0025092
- OCCT MANTIS#0032914

## Description

A `COMPSOLID` (cellular solid — multiple cells sharing faces) cannot be expressed natively in AP203/AP214 and the writer's fallback is to write each cell as a separate solid. In nonmanifold mode the writer should emit shared faces with proper non-manifold markers but instead duplicates the shared faces, losing the cellular relationship.

## Reproducer recipe

`COMPSOLID` of two cubes sharing one face. Write in nonmanifold mode. Output has each cube as a separate solid; shared face appears twice.

## Expected kernel behavior

nonmanifold export preserves shared faces using `NON_MANIFOLD_SURFACE_SHAPE_REPRESENTATION`; cellular topology recoverable on re-import.

## Notes

Provenance tier: writer-side — writer loses COMPSOLID in non-manifold export.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `writer-side` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
