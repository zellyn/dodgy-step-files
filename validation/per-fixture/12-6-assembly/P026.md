# P026 — `LENGTH_UNIT(.MILLI., .METRE.)` context but `CARTESIAN_POINT` coordinates pre-scaled ×1000 (global OCCT unit setting contaminated across in-process clients)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/P026.stp`](../../step-examples/12-6-assembly/P026.stp)  
**Sender**: FreeCAD ↔ gmsh interop  

## Sources
- FreeCAD #18980

## Description

After gmsh's OCCT instance sets `Geometry.OCCTargetUnit=M`, subsequent FreeCAD STEP exports emit coordinates scaled ×1000 because the OCCT global unit setting was mutated. Common file signature: `GEOMETRIC_REPRESENTATION_CONTEXT` declares `LENGTH_UNIT(SI MILLI METRE)` but `CARTESIAN_POINT` coordinates carry values like 10000.0 where 10 mm was intended — coords scaled by 1000 ignoring the declared unit context. (Not a *file* defect — but a class of "files that came out wrong because of process state" that downstream consumers will see.)

## Reproducer recipe

Process-state contamination scenario; not a file-content recipe directly.

## Expected kernel behavior

STEP export sets unit context per-call without inheriting global state.

## Notes

This entry documents a runtime / process-state interaction defect (gmsh's `Geometry.OCCTargetUnit` setting persists into FreeCAD's STEP export in the same Python process). It is intentionally NOT falsifiable from a single .stp file; the fixture P026.stp is illustrative only. To reproduce, run a Python session that imports both gmsh and FreeCAD/OCCT in sequence with the unit-setting middle step, then export STEP — the export will be 1000× scaled. CI validation marks this entry as PROCESS-STATE — out of scope for static-file oracles.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `beyond-10` (rank 16) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `` |
