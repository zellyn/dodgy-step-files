# A034 — CheckSRRReversesNAUO segfault on SDR with SHAPE_ASPECT instead of PROPERTY_DEFINITION

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A034.stp`](../../step-examples/12-6-assembly/A034.stp)  
**Sender**: CATIA V6 "Fast Export STEP AP214 Mapper" (1088/1267 SDRs in a real 430MB file used the SHAPE_ASPECT variant).  

## Sources
- I001 (OCCT #1202).

## Description

`SHAPE_DEFINITION_REPRESENTATION` whose `Definition` member of the `RepresentedDefinition` SELECT is a `SHAPE_ASPECT` rather than `PROPERTY_DEFINITION`. OCCT's unguarded chain `SDR->Definition().PropertyDefinition()->Definition().ProductDefinition()` returns null on the SHAPE_ASPECT branch and crashes.

## Reproducer recipe

AP214 file with `#27=SHAPE_DEFINITION_REPRESENTATION(#23,#26)` where `#23` is a `SHAPE_ASPECT` instead of `PROPERTY_DEFINITION_SHAPE`.

## Expected kernel behavior

heal — skip SDRs whose Definition is not PropertyDefinition; emit warning; never deref blindly.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal, warn-and-proceed`

## Notes

**See also**: A009. Validation observed: silent-empty rather than crash. Kernel-mishandling-by-silent-acceptance still demonstrates the defect class.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **OCC behavior**: silent-empty rather than crash. Kernel-mishandling-by-silent-acceptance still demonstrates the defect class.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [A009](A009.md)
