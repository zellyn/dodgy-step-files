# A019 — Per-face `STYLED_ITEM` colours collapse to a single hue when multiple `ADVANCED_FACE`s share one supporting `PLANE`

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A019.stp`](../../step-examples/12-6-assembly/A019.stp)  

## Sources
- V047 (CATIA → SolidWorks all white), W024 (HOOPS face-level styling), M032 (CAx-IF styling RP), R063 (OCCT bug33317 reference shape color), G036 (tessellated face style binding), G098 (OCCT non-manifold loses attributes).

## Description

Multiple distinct face colours in the producer become identical white in the consumer. Per-face colours are bound via `STYLED_ITEM` / `PRESENTATION_STYLE_ASSIGNMENT` / `SURFACE_STYLE_FILL_AREA` / `FILL_AREA_STYLE_COLOUR` chains. Common signature: three `ADVANCED_FACE` instances all share the same supporting `PLANE` (e.g. `#64`) but each has its own distinct `STYLED_ITEM` / `COLOUR_RGB` — XCAF risks merging the faces (since they overlap geometrically) and collapsing the per-face colour binding. After re-tessellation or face-merge healing the binding is lost. Non-manifold STEP reader path skips XCAF attribute application.

## Reproducer recipe

Face with full styled_item chain on COLOUR_RGB(0.8,0.1,0.1); subject to sliver-face removal and re-export.

## Expected kernel behavior

honor face-bound colour_assignment without normalizing; re-bind to surviving merged face when possible; bind style to tessellated shape; apply colors/styles on non-manifold path.

## Notes

**See also**: A018, M020, M042, M045, N003.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(3)` |
| `gmsh_autofix_off` | `shape(3)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 3
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 8e+100 | 1.0000000000000001e+107 | 0 |
| 1 | plane | 8e+100 | 1.0000000000000001e+107 | 0 |
| 2 | plane | 8e+100 | 1.0000000000000001e+107 | 0 |

## Tier-3 assertions (machine-checked)

- `face[0].sliver_aspect_max_min > 1e6`
- `face[2].sliver_aspect_max_min > 1e6`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 47) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(3) ifc=schema_n/a` |

## Cross-references

**See also**: [A018](A018.md), [M020](M020.md), [M042](M042.md), [M045](M045.md), [N003](N003.md)
