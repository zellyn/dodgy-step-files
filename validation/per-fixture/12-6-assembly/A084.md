# A084 — STEP exporter writes untrimmed curve where trimmed expected

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A084.stp`](../../step-examples/12-6-assembly/A084.stp)  

## Sources
- OCCT MANTIS#0032817

## Description

An edge backed by a trimmed curve (e.g., a half-circle arc) is exported with the full underlying parent curve plus separate trim markers — but the exporter omits the trim markers entirely. The re-imported edge spans the full parent curve, giving wrong arc length.

## Reproducer recipe

Edge on a circle from parameter `t=0` to `t=π`. After export, file emits `CIRCLE` plus `EDGE_CURVE` with full-parameter range `[0, 2π]` instead of `[0, π]`.

## Expected kernel behavior

every edge writes its trim parameters; receivers see the intended sub-arc.

## Notes

Provenance tier: writer-side — writer emits untrimmed where trimmed expected.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `writer-side` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
