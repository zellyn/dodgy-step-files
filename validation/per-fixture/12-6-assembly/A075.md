# A075 — Free-shape-after-import is empty

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A075.stp`](../../step-examples/12-6-assembly/A075.stp)  

## Sources
- OCCT MANTIS#0031786
- OCCT MANTIS#0031191
- OCCT MANTIS#0033261
- OCCT MANTIS#0029241
- OCCT MANTIS#0022680

## Description

A STEP file from SolidWorks 2018 (or similar producers) is imported but the resulting document contains no shapes — only an empty XCAF root. Diagnostics report success. Common cause: the file's `PRODUCT_DEFINITION` chain is rooted in a representation type the reader silently skips (e.g., `MANIFOLD_SURFACE_SHAPE_REPRESENTATION` instead of `ADVANCED_BREP_SHAPE_REPRESENTATION`).

## Reproducer recipe

STEP file whose `PRODUCT_DEFINITION` references `MANIFOLD_SURFACE_SHAPE_REPRESENTATION` carrying real geometry; reader expects only `ADVANCED_BREP_SHAPE_REPRESENTATION`.

## Expected kernel behavior

reader recognises every representation subtype that is geometry-bearing; if a subtype is unsupported, emit a diagnostic listing the entity type rather than silently empty result.

## Notes

Provenance tier: runtime-only — kernel reports no free-shape after import; defect is reader behavior.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `runtime-only` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
