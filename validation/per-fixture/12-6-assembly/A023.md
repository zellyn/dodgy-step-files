# A023 — Mapped_item / representation_map identity transform requirement (Saved Views)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A023.stp`](../../step-examples/12-6-assembly/A023.stp)  

## Sources
- CAx-IF PMI v4.1 §9.4.2.1 (entry from 02-caxif), Q031 (MAPPED_ITEM unrecognized location), Q084 (mapped_item with scaling).

## Description

To include part geometry in a Saved View's `draughting_model`, a `mapped_item` plus a `representation_map` must be used. The `axis2_placement_3d` referenced as `mapping_source`/`mapping_origin` MUST be a unit identity transform. Files that put a non-identity transform inadvertently move the geometry only in that view. Variants: MAPPED_ITEM with unrecognized location (location dropped); writer skips MAPPED_ITEM with non-rigid (scaling) transform.

## Reproducer recipe

Saved view's mapped_item has `mapping_source = AXIS2_PLACEMENT_3D(P0=(10,0,0), Z=(0,0,1), X=(1,0,0))` instead of unit identity.

## Expected kernel behavior

validate identity transforms on saved-view mapped items; reject scaling on assembly mapped items.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**Originally also catalogued as**: U030, Pmi016.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(1)` |
| `gmsh_autofix_off` | `shape(1)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 8e+100 | 1.0000000000000001e+107 | 0 |

## Tier-3 assertions (machine-checked)

- `face[0].sliver_aspect_max_min > 1e6`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(1) ifc=schema_n/a` |

## Cross-references


**Originally also catalogued as**: U030, Pmi016
