# P016 — Pre-Boolean operands shipped alongside result solid

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/P016.stp`](../../step-examples/12-6-assembly/P016.stp)  
**Sender**: FreeCAD with OpenSCAD workbench  

## Sources
- FreeCAD #8476

## Description

An OpenSCAD-produced model with `difference(){ outer; inner; }` exports STEP containing the difference result PLUS the original argument cylinders as separate solids. On re-import the user sees the union of the operands instead of the hollow.

## Reproducer recipe

STEP file with multiple `MANIFOLD_SOLID_BREP` instances representing pre-Boolean operands, no indication that they were CSG inputs.

## Expected kernel behavior

Producers should emit only final result solid; receivers can't fix this without provenance hints.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
