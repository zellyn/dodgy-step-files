# P002 — Non-UTF-8 (GB18030 / locale code page) in string literals

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/P002.stp`](../../step-examples/12-6-assembly/P002.stp)  
**Sender**: SolidWorks (Chinese-locale install) and any non-Western Windows export  

## Sources
- FreeCAD #7987, #22123

## Description

STEP-21 lines like `SHAPE_REPRESENTATION ('海绵2', ...)` appear in files encoded as GB18030 / Shift-JIS / CP1252 because exporters write the OS code page directly. Readers that assume UTF-8 produce mojibake in object labels and attributes; round-tripping then corrupts the saved project document.

## Reproducer recipe

Generate a STEP file containing `PRODUCT`/`SHAPE_REPRESENTATION` name strings with high-bit bytes interpretable in GB18030 but not UTF-8 (or with the standard `\X2\…\X0\` Unicode escape sequences that exporters rarely emit).

## Expected kernel behavior

Auto-detect or honor STEP-21 `\X2\` / `\X4\` escape sequences; expose a per-file encoding override; warn if bytes don't decode as declared encoding.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_X2_NO_TERM)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: OCC silently heals — pure-Python rejects this

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-10` (rank 4) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
