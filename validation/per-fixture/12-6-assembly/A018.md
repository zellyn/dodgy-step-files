# A018 — STYLED_ITEM at wrong scope: assembly-level color override lost or misbound

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A018.stp`](../../step-examples/12-6-assembly/A018.stp)  

## Sources
- V046 (SolidWorks assembly-level color), V047 (per-face color → white), V048 (Inventor strips appearances), G097 (OCCT root-label color), J008 (Onshape Apr-2024 pipeline), Y004 (KiCad nested STYLED_ITEM lost on flatten), W024 (HOOPS material merging).

## Description

When color is overridden at assembly node level (not part level), STEP writers emit per-part STYLED_ITEM rather than per-instance, so receivers see all instances colored alike. Or per-face STYLED_ITEM chains land at the wrong scope (top-level shape-rep instead of per-`SOLID_MODEL`). Multiple `MECHANICAL_DESIGN_GEOMETRIC_PRESENTATION_REPRESENTATION` chains may contradict on the same STYLED_ITEM. When nested compound is flattened/copied, inner STYLED_ITEM is dropped entirely.

## Reproducer recipe

assembly with a top-level color override; export. Verify whether STYLED_ITEM attaches to MAPPED_ITEM-level instances or only to SHAPE_REPRESENTATION. Or build a one-level vs two-level wrapped product and flatten via `XCAFDoc_ShapeTool::CopyShape`.

## Expected kernel behavior

STYLED_ITEM must attach to MAPPED_ITEM-level instances; prefer most-specific styled-item; fall back to next ancestor; never inherit from sibling sub-assembly; propagate STYLED_ITEM through every level of NAUO during flatten.

## Notes

**Originally also catalogued as**: P020. **See also**: A019, A020, M039, M040, M042.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(2)` |
| `gmsh_autofix_off` | `shape(2)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 8e+100 | 1.0000000000000001e+107 | 0 |
| 1 | plane | 8e+100 | 1.0000000000000001e+107 | 0 |

## Tier-3 assertions (machine-checked)

- `face[0].sliver_aspect_max_min > 1e6`
- `face[1].sliver_aspect_max_min > 1e6`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(2) ifc=schema_n/a` |

## Cross-references

**See also**: [A019](A019.md), [A020](A020.md), [M039](M039.md), [M040](M040.md), [M042](M042.md)

**Originally also catalogued as**: P020
