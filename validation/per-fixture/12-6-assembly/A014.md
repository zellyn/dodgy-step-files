# A014 — EXTERNAL_ANCHOR uniqueness (1:1 anchor↔entity) violated; orphan EER source

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.6 (`12-6-assembly`)  
**Fixture**: [`step-examples/12-6-assembly/A014.stp`](../../step-examples/12-6-assembly/A014.stp)  
**Sender**: per-level split assembly writers; tools that copy anchors when re-exporting.  

## Sources
- CAx-IF EER §7.3.1 (entry from 02-caxif: "EXTERNAL_ANCHOR uniqueness rule"), CAx-IF EER §7.3.2 (entry from 02-caxif: "EER source file must include placeholder representation"), CAx-IF EER §6.4 ("Nested external references").

## Description

For each entity instance there must be at most one EXTERNAL_ANCHOR; every EXTERNAL_ANCHOR must reference exactly one data-section entity. Writers that copy anchors duplicate them. Source files containing `externally_defined_representation_item` must wrap them in a placeholder `externally_defined_representation`. When the assembly tree is split per-level, the same leaf part may be referenced from multiple sub-assembly files with divergent `product.id`.

## Reproducer recipe

`!EXTERNAL_ANCHOR('AAA', #100); !EXTERNAL_ANCHOR('BBB', #100);`. Or `externally_defined_representation_item` whose `source` is set but is not contained inside any `externally_defined_representation`.

## Expected kernel behavior

reject duplicate anchors; reject orphan externally-defined items; surface PDM mismatch when same product diverges across files.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**Originally also catalogued as**: A015.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references


**Originally also catalogued as**: A015
