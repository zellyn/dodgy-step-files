# Le038 — Bare `\PE\` directive at end-of-string with no operand letter

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1a (`12-1a-encoding`)  
**Fixture**: [`step-examples/12-1a-encoding/Le038.stp`](../../step-examples/12-1a-encoding/Le038.stp)  

## Sources
- 16-iso-spec ISO 10303-21 Ed.3 §6.4.3 (`\PE\` alphabet extension)

## Description

Edition-3 `\PE\` requires a single ASCII letter (the page selector A..I) immediately following the slashes. A producer that truncates a string at the directive boundary or copy/pastes incorrectly emits a bare `\PE\` with no follow-byte before the closing apostrophe. A naive parser may consume the closing `'` as the page operand and lose string termination, walking off into the rest of the entity.

## Reproducer recipe

`PRODUCT.name='text\PE\'` — closing apostrophe immediately follows `\PE\`. The next entity definition becomes part of the apparent string body if the parser eats the apostrophe as the operand.

## Expected kernel behavior

reject the malformed directive with a precise diagnostic citing the string offset; never let the closing apostrophe be consumed as a directive operand.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**See also**: Le037, Le039.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Le037](Le037.md), [Le039](Le039.md)
