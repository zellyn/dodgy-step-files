# Le023 — Locale-dependent decimal separator inside numeric attribute

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1a (`12-1a-encoding`)  
**Fixture**: [`step-examples/12-1a-encoding/Le023.stp`](../../step-examples/12-1a-encoding/Le023.stp)  

## Sources
- A038/numeric-parser pitfall
- N019/ISO 10303-21 Ed.3 REAL grammar

## Description

The REAL grammar uses `.` only. A parser using `strtod` without `setlocale("C")` on a host with `LC_NUMERIC=de_DE` reads `1,5` as `1234`-style integers separated by a comma, or worse, two integer tokens. Producers using locale-aware `printf` on European locales emit `1,5` for one-and-a-half. Geometry then has wildly wrong coordinates; downstream tessellation can crash on degenerate triangles.

## Reproducer recipe

`#1=CARTESIAN_POINT('',(1,5,0.,0.));`

## Expected kernel behavior

use `strtod_l` with the `"C"` locale; reject anything `strtod` does not consume entirely. Tolerant readers should diagnose locale corruption rather than silently re-parse.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**Originally also catalogued as**: Ls007, Ad034.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references


**Originally also catalogued as**: Ls007, Ad034
