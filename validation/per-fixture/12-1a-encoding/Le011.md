# Le011 — `\N\` notation directive misused as C-style newline escape

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1a (`12-1a-encoding`)  
**Fixture**: [`step-examples/12-1a-encoding/Le011.stp`](../../step-examples/12-1a-encoding/Le011.stp)  

## Sources
- N010/ISO 10303-21 Ed.3 §6.4.3 Table 4
- W032/CAD Exchanger CHANGES v3.24.12

## Description

`\N\` toggles a notation context. Generators emit it without the spec-required following digit, or assume it means "newline" (it does not — newlines must be encoded as `\X\0A`). The same defect can also span a physical line break in the source.

## Reproducer recipe

`'line1\N\line2'` (writer assumed `\N\` == LF).

## Expected kernel behavior

spec semantics differ from C `\n`; tolerant readers should at least not crash and should warn that `\N\` is not a newline escape.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- disallowed: `crash`

## Notes

Validation observed: silent-empty rather than the cited crash. Specific crash code path may not be reached at fixture scale; the kernel-mishandling-by-silent-acceptance still demonstrates the defect class.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Byte assertions (machine-checked)

- `contains(b'\\N\\')`

## Provenance / kernel-behavior annotations

- **OCC behavior**: silent-empty rather than the cited crash. Specific crash code path may not be reached at fixture scale; the kernel-mishandling-by-silent-acceptance still demonstrates the defect class.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
