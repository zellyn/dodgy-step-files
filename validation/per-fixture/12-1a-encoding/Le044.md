# Le044 — `\X2\` payload containing UTF-16 surrogate halves

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1a (`12-1a-encoding`)  
**Fixture**: [`step-examples/12-1a-encoding/Le044.stp`](../../step-examples/12-1a-encoding/Le044.stp)  

## Sources
- 16-iso-spec ISO 10303-21 Ed.3 §6.4.3 (`\X2\..\X0\` 4-hex groups)

## Description

`\X2\..\X0\` wraps 4-hex-digit groups each denoting one UCS-2 (BMP) scalar. Surrogate halves U+D800..U+DFFF are not BMP characters — they exist only as paired surrogates of UTF-16. A Java-vintage exporter that internally holds Java `char[]` (allowing lone surrogates) and writes them out one-by-one as `\X2\DXXX\X0\` produces a malformed but parseable token. A surrogate pair encoded as two `\X2\` groups (CESU-8-like) round-trips by accident on tools that decode UTF-16 surrogates.

## Reproducer recipe

`'lone=\X2\D801\X0\'` — D801 is a high-surrogate half. `'pair=\X2\D801DC37\X0\'` — high+low surrogate pair.

## Expected kernel behavior

reject lone surrogates with a precise diagnostic; either reject paired surrogates (strict) or transparently combine them into the matching supplementary-plane scalar (lenient, with a warning).

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, warn-and-proceed`

## Notes

**See also**: Le006, Le041. Distinct from Le006 (which exercises hex run length, not surrogate semantics).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `process_signal` |
| `part21_strict` | `reject(E_X2_NO_TERM)` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: OCC silently heals — pure-Python rejects this

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `undetected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=process_signal` |

## Cross-references

**See also**: [Le006](Le006.md), [Le041](Le041.md)
