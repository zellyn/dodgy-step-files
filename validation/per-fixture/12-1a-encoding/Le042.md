# Le042 — `\Q\` payload contains non-decimal characters

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1a (`12-1a-encoding`)  
**Fixture**: [`step-examples/12-1a-encoding/Le042.stp`](../../step-examples/12-1a-encoding/Le042.stp)  

## Sources
- 16-iso-spec ISO 10303-21 Ed.3 §6.4.3 (decimal-only payload)

## Description

`\Q\NNN\Q\` requires the bracketed payload to be a decimal integer. Producers occasionally emit hex (`\Q\1F600\Q\`), an `0x` prefix (`\Q\0x1F600\Q\`), or whitespace around the number (`\Q\ 65 \Q\`). A strict parser rejects; a lenient parser may silently coerce, producing the wrong character (e.g. parsing `F60` as decimal 0 then F-A-A garbage).

## Reproducer recipe

`PRODUCT.name='hex=\Q\F60\Q\'`; `'px=\Q\0x41\Q\'`; `'ws=\Q\ 65 \Q\'`.

## Expected kernel behavior

reject any non-decimal payload with a precise diagnostic citing the offending characters; never silently substitute 0 for a malformed payload.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

**See also**: Le030, Le040. **OCC behavior**: silently accepts non-decimal `\Q\` payloads and either substitutes 0 or coerces via leading digits; kernel mishandling — the catalog above forbids silent substitution.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `process_signal` |
| `part21_strict` | `warn(W_BARE_BACKSLASH)` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=process_signal` |

## Cross-references

**See also**: [Le030](Le030.md), [Le040](Le040.md)
