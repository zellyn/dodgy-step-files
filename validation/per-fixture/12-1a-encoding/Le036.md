# Le036 — Round-trip loss of `\X2\` content through XML / DB layer

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1a (`12-1a-encoding`)  
**Fixture**: [`step-examples/12-1a-encoding/Le036.stp`](../../step-examples/12-1a-encoding/Le036.stp)  

## Sources
- W031/CAD Exchanger CHANGES v3.24.18
- G045/OCCT 475da0f

## Description

A toolchain that decodes `\X2\…\X0\` into UTF-8 internally and re-encodes for export must round-trip the original Unicode characters. Several vendors lost backslashes (OCCT 7.5 regression) or replaced rare code points with `?` when the storage layer (XML, SQL VARCHAR(...)) silently truncated.

## Reproducer recipe

PRODUCT name containing literal backslashes (`'C:\path\file'`) or rare CJK code points; export and re-import.

## Expected kernel behavior

preserve exact Unicode through encode/decode/storage; emit `\X2\…\X0\` only when the Ed.2 conformance class requires it.

## Notes

**See also**: Le022. Provenance tier: requires-sibling-pair — bytes alone cannot demonstrate round-trip loss; the defect is the differential between the input file and the writer's output. Demonstrating this requires both an `<id>.input.stp` fixture and the corrupted `<id>.stp` produced by re-export through the lossy XML/DB layer.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_BARE_BACKSLASH)` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `requires-sibling-pair` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Le022](Le022.md)
