# Le017 — Raw control character (U+0000..U+001F) in string body

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1a (`12-1a-encoding`)  
**Fixture**: [`step-examples/12-1a-encoding/Le017.stp`](../../step-examples/12-1a-encoding/Le017.stp)  

## Sources
- N014/ISO 10303-21 Ed.3 §6.4.3.4
- A029/lexer CR/LF pitfall
- R053/OCCT bug 33815
- W031/CAD Exchanger CHANGES v3.24.13

## Description

Control characters (tab, LF, CR, NUL, etc.) must be encoded as `\X\hh` (e.g. tab = `\X\09`); raw bytes in a string literal are illegal even under the Ed.3 UTF-8 default. Files written by tools using "binary write" of attribute strings often contain raw `\t`, `\n`, or `\0`. Some parsers tolerate raw `\n` and then index past the line buffer when computing line/column for diagnostics. Round-tripping names with quotes, backslashes, and control chars must be preserved (per OCCT bug 33815).

## Reproducer recipe

`'first line<RAW LF>second'`; `#1=PERSON('line1<RAW LF>line2');`

## Expected kernel behavior

reject raw control characters in literals. Tolerant readers may translate raw `\n`/`\t` to space-equivalents and warn. Identifiers carrying control chars must round-trip without corruption when the user opted in.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**Originally also catalogued as**: Ad029. **See also**: Le018, Le028.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Byte assertions (machine-checked)

- `matches(rb"'[^'\\n\\r]*[\\x00-\\x08\\x0b\\x0c\\x0e-\\x1f][^']*'")`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Le018](Le018.md), [Le028](Le028.md)

**Originally also catalogued as**: Ad029
