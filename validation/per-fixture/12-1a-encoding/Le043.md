# Le043 — `\X4\` with a valid supplementary-plane code point followed by a short trailing hex run

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1a (`12-1a-encoding`)  
**Fixture**: [`step-examples/12-1a-encoding/Le043.stp`](../../step-examples/12-1a-encoding/Le043.stp)  

## Sources
- 16-iso-spec ISO 10303-21 Ed.3 §6.4.3 (`\X4\..\X0\` 8-hex groups)

## Description

`\X4\..\X0\` wraps a sequence of 8-hex-digit groups, each encoding one UCS-4 (32-bit) scalar. A producer concatenating variable-length output may emit a hex run not divisible by 8 (e.g. 4 or 7 digits), which is malformed. Distinct from Le006 in that the *valid* leading group is a supplementary-plane scalar (U+1F600 Grinning Face) and only the trailing group is short, so a tolerant parser may accept the first character and silently drop the second.

## Reproducer recipe

`PRODUCT.name='mix=\X4\0001F60000041\X0\'` — 8 hex digits (U+1F600, valid SMP) followed by 4 hex digits (short, illegal).

## Expected kernel behavior

reject the whole `\X4\..\X0\` block as malformed, or reject only the trailing short group with a diagnostic; never silently truncate.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

**See also**: Le006. **OCC behavior**: silently accepts the malformed `\X4\..\X0\` block and either truncates the trailing short group or interprets it incorrectly; kernel mishandling — the catalog above forbids silent truncation.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `process_signal` |
| `part21_strict` | `reject(E_X4_NO_TERM)` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: OCC silently heals — pure-Python rejects this

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `undetected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=process_signal` |

## Cross-references

**See also**: [Le006](Le006.md)
