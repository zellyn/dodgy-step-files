# Le041 — `\Q\` encoding a code point in the UTF-16 surrogate range (U+D800..U+DFFF)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1a (`12-1a-encoding`)  
**Fixture**: [`step-examples/12-1a-encoding/Le041.stp`](../../step-examples/12-1a-encoding/Le041.stp)  

## Sources
- 16-iso-spec ISO 10303-21 Ed.3 §6.4.3
- Unicode standard §3.8 (surrogate code points are not legal scalars)

## Description

`\Q\NNN\Q\` accepts decimal Unicode scalars, but values 55296..57343 (the UTF-16 surrogate halves) are not legal Unicode scalars — they exist only as paired surrogates in the UTF-16 transformation. A kernel that decodes `\Q\55296\Q\` into a UTF-8 stream produces an ill-formed sequence (CESU-8 / WTF-8 territory) that downstream tooling may reject. The producer may have meant a non-BMP character but failed to emit the surrogate pair, or may be re-encoding a Java/JavaScript string that allowed lone surrogates internally.

## Reproducer recipe

`PRODUCT.name='lone=\Q\55296\Q\'` — value 55296 is U+D800, a high-surrogate half.

## Expected kernel behavior

reject `\Q\..\Q\` payloads in the surrogate range (55296..57343); a tolerant mode may pass them through as a degraded encoding with a warning, but conforming Unicode output must not contain lone surrogates.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, warn-and-proceed`

## Notes

**See also**: Le044 (the matching `\X2\` defect).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `process_signal` |
| `part21_strict` | `warn(W_BARE_BACKSLASH)` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=process_signal` |

## Cross-references

**See also**: [Le044](Le044.md), [X2](X2.md)
