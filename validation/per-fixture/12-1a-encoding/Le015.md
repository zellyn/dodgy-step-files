# Le015 — Unterminated string literal

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1a (`12-1a-encoding`)  
**Fixture**: [`step-examples/12-1a-encoding/Le015.stp`](../../step-examples/12-1a-encoding/Le015.stp)  

## Sources
- T009/stepcode Str.cc:162-169, expscan.l:240-244
- A008/CVE-2024-1847-style OOB-read pattern

## Description

A string with an opening apostrophe but no matching close advances the scanner past the end of the intended attribute and may swallow the rest of the file or read sensitive memory until a lucky `'` byte is found.

## Reproducer recipe

`#1=IFCPERSON($,$,'unterminated,$);\n#2=...` or `#1=PERSON('unclosed_string_then_eof`

## Expected kernel behavior

bound the scan to remaining buffer size; emit `E_UNTERMINATED_STRING` with offset; do not silently re-synchronise on a later `'`.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- disallowed: `silent-accept`

## Notes

Often pairs with subsequent OOB write when the caller allocates `(end - start)` bytes. **Originally also catalogued as**: Ad008. **See also**: Ad001, Ls029.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `reject` |
| `occt_heal_off` | `reject` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_NO_CLOSE)` |
| `manifold` | `step_read_failed` |
| `occt_diag_on` | 1:**** ERR StepFile : Undefined Parsing: Line 23: Incorrect... |
| `occt_diag_off` | 1:**** ERR StepFile : Undefined Parsing: Line 23: Incorrect... |
| `gmsh_diag` | 1:**** ERR StepFile : Undefined Parsing: Line 23: Incorrect... |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Byte assertions (machine-checked)

- `matches(rb"PERSON\\('[^']*\\Z")`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | `no-target-byte` (? attempts) |
| Status | `pending` |
| Expected validation | `occt=reject/reject gmsh=reject ifc=schema_n/a` |

## Cross-references

**See also**: [Ad001](Ad001.md), [Ls029](Ls029.md)

**Originally also catalogued as**: Ad008
