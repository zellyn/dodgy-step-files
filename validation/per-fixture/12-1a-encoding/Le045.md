# Le045 — String literal at the Edition-3 length limit (32768 characters)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1a (`12-1a-encoding`)  
**Fixture**: [`step-examples/12-1a-encoding/Le045.stp`](../../step-examples/12-1a-encoding/Le045.stp)  

## Sources
- 16-iso-spec ISO 10303-21 Ed.3 §6.4.4 (string literal max length 32768)

## Description

Edition-3 raises the maximum string-literal length to 32768 characters (Edition 1 was 256). A reader that statically allocates a 256-char buffer truncates silently or faults; a reader that allocates dynamically but checks against an off-by-one threshold may reject one too many or one too few. The boundary character itself becomes load-bearing.

## Reproducer recipe

`PRODUCT.name` whose string literal is exactly 32768 characters of `'A'`.

## Expected kernel behavior

accept literals up to 32768 characters; reject longer with a length diagnostic; never silently truncate. Reading should be O(N) in literal length, not quadratic.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT rejects (`reject`). OCC enforces stricter framing than the spec text requires.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `reject` |
| `occt_heal_off` | `reject` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `step_read_failed` |
| `occt_diag_on` | 2:... Exception Raised while reading Step File : '/Users/ze... |
| `occt_diag_off` | 2:... Exception Raised while reading Step File : '/Users/ze... |
| `gmsh_diag` | 2:... Exception Raised while reading Step File : '/Users/ze... |

**Cross-oracle classification**: OCC stricter than spec — pure-Python accepts

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT rejects (`reject`). OCC enforces stricter framing than the spec text requires.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=reject/reject gmsh=reject ifc=schema_n/a` |
