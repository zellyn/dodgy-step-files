# Pf007 — `ADVANCED_FACE` with many circular inner `FACE_BOUND` holes triggers eager UV-bounds wire-walk on every type-only surface query

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.10 (`12-10-perf`)  
**Fixture**: [`step-examples/12-10-perf/Pf007.stp`](../../step-examples/12-10-perf/Pf007.stp)  

## Sources
- 13-quaoar.md B003

## Description

Querying the supporting surface of a face eagerly walks every edge of every bounding wire to compute UV bounds, even when the caller only needs the surface type. Common shape: an `ADVANCED_FACE` with one outer bound plus many circular inner `FACE_BOUND` holes on a single plane (perforated sheet) — the cost multiplies by hole count, producing quadratic-in-feature-count writer time.

## Reproducer recipe

time the STEP writer's transfer pass on a sheet with thousands of holes.

## Expected kernel behavior

lazy UV-bound computation; cheap

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 12) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
