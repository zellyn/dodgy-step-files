# Pf021 — Floating / non-deterministic crash in shape healing on near-apex `CONICAL_SURFACE` edge

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.10 (`12-10-perf`)  
**Fixture**: [`step-examples/12-10-perf/Pf021.stp`](../../step-examples/12-10-perf/Pf021.stp)  

## Sources
- 11-translator-vendors.md W049 (CAD Exchanger CHANGES v3.24.8)

## Description

Healing pipeline crashes intermittently on the same input; symptoms are uninitialized memory, threading races, or floating-point exceptions in projection routines. Whole class, not a single defect — every vendor fixes it repeatedly. Common trigger: a `CONICAL_SURFACE` whose `EDGE_CURVE` passes through the cone apex, with a near-zero-length edge between the apex and a 1e-12-offset `VERTEX_POINT` — singular projection in healing produces an FP exception or uninitialised-memory read.

## Reproducer recipe

run healing on the same large STEP 1000 times; detect non-deterministic crashes / divergent outputs.

## Expected kernel behavior

deterministic same-input-same-output

## Notes

Cross-oracle: pure-Python Part-21 validator rejects (reject(E_REAL_NO_DOT)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_REAL_NO_DOT)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: OCC silently heals — pure-Python rejects this

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator rejects (reject(E_REAL_NO_DOT)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
