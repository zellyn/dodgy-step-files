# Pf015 — Pathological B-rep from mesh conversion: `OPEN_SHELL` from OpenSCAD/STL→STEP

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.10 (`12-10-perf`)  
**Fixture**: [`step-examples/12-10-perf/Pf015.stp`](../../step-examples/12-10-perf/Pf015.stp)  
**Sender**: OpenSCAD → STL → FreeCAD "Convert to Shape/Solid" → STEP  

## Sources
- 22-niche-forums.md Y024
- 14-brlcad-rhino.md K028

## Description

Mesh-derived B-reps emerge as huge `OPEN_SHELL`s with one tiny `ADVANCED_FACE` per triangle. Stitching pairs fail, Boolean cuts fail with builder-arg check errors, joining more than ~10k faces takes quadratic time. Rhino exposes "do not join polysurfaces with more than ___ faces" as an explicit scale escape hatch.

## Reproducer recipe

OpenSCAD design with `hull()` or `minkowski()`; export STL; FreeCAD: Mesh → Shape → Solid → STEP. Try a Boolean cut on the result.

## Expected kernel behavior

reject with explicit "non-watertight,

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**See also**: M023, Pf027. Provenance tier: runtime-only — bytes alone cannot demonstrate this defect; the catalogued symptoms (quadratic-time stitching, Boolean failures, resource exhaustion at ~10k+ faces) are runtime resource consumption that scales with mesh size. A fixture-scale `OPEN_SHELL` does not embody the production-scale exhaustion; a behavioral test on the stitching/Boolean pipeline at scale is the appropriate venue.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `runtime-only` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-10` (rank 6) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [M023](M023.md), [Pf027](Pf027.md)
