# Pf020 — OCAF modification-delta leak on long-running healing test suites

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.10 (`12-10-perf`)  
**Fixture**: [`step-examples/12-10-perf/Pf020.stp`](../../step-examples/12-10-perf/Pf020.stp)  

## Sources
- 13-quaoar.md B013

## Description

OCAF's modification-delta mechanism for array-style attributes (Active Data Mesh Attribute) skips a destructor on the previous state; long-running suites that perform thousands of modify/commit cycles accumulate memory. Hides as "test-suite OOM".

## Reproducer recipe

regression suite performing thousands of modify/commit cycles on a mesh attribute; observe RSS growth proportional to iteration count.

## Expected kernel behavior

`Backup`/`Delta` lifetime in OCAF must

## Notes

Provenance tier: cross-file-state — bytes alone cannot demonstrate this defect; the symptom is monotonic memory growth across thousands of modify/commit cycles in long-running kernel state. A single fixture cannot embody the leak; the kernel's cross-transaction state cache must retain (and fail to release) snapshots over many iterations. An external memory measurement against a long-running test loop is the appropriate venue.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_COMPLEX_ORDER)` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 3
- **n_vertices_total**: 27
- **brepcheck.valid**: `True`

## Tier-3 assertions (machine-checked)

- `load == "ok"`
- `n_edges_total >= 3`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `cross-file-state` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
