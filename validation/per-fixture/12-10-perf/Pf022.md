# Pf022 — Non-deterministic empty-export from in-memory state leakage

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.10 (`12-10-perf`)  
**Fixture**: [`step-examples/12-10-perf/Pf022.stp`](../../step-examples/12-10-perf/Pf022.stp)  
**Sender**: gmsh writing STEP from a once-populated, now-cleared model; OCCT Draw `stepread` followed by `stepwrite`  

## Sources
- 22-niche-forums.md Y020 (gmsh #3332)
- 13-quaoar.md B011

## Description

After import-then-partial-clear, the writer emits "No suitable CAD data found" non-deterministically — depends on past actions, not on whether the document currently contains shapes. In OCCT Draw, a `stepread` populates a transient model that the next `stepwrite` re-uses, doubling the output and producing a file twice the expected size.

## Reproducer recipe

import STEP, `gmsh.model.remove()` some entities, then `gmsh.write("out.step")`; or `stepread file.stp sh *` followed by `stepwrite a sh_1 file.stp`.

## Expected kernel behavior

writer state must be a pure function of

## Notes

**See also**: U014.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(3)` |
| `gmsh_autofix_off` | `shape(3)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_COMPLEX_ORDER)` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 1
- **n_vertices_total**: 2
- **brepcheck.valid**: `True`

## Tier-3 assertions (machine-checked)

- `load == "ok"`
- `n_edges_total >= 1`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `top-3` (rank 3) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(3) ifc=schema_n/a` |

## Cross-references

**See also**: [U014](U014.md)
