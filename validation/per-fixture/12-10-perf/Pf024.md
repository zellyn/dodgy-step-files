# Pf024 — Self-intersection-healing tool enters infinite loop on `EDGE_LOOP` with crossed diagonals (out-of-range split index)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.10 (`12-10-perf`)  
**Fixture**: [`step-examples/12-10-perf/Pf024.stp`](../../step-examples/12-10-perf/Pf024.stp)  

## Sources
- 08-occt-gitlog.md G077 (Mantis 0029695
- OCCT ea1114e)

## Description

Intersection-healing algorithm could infinite-loop on certain edges because a split-index range check was missing. Common trigger: an `EDGE_LOOP` with self-intersecting diagonals — for example four edges A→C, B→D, A→B, C→D in cross order forming an X-shape — exercises the missing bound on the split index.

## Reproducer recipe

face whose intersection-tool encounters out-of-range split.

## Expected kernel behavior

bounded iteration; index range guard.

## Notes

Validation observed: this is a deliberately scaled-down representative; the structural pattern is present but the production-scale resource exhaustion / catastrophic timing is not exercised at fixture size. To trigger the documented kernel-crash, multiply the entity-replication factor by ~10^N as noted in the reproducer recipe.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **OCC behavior**: this is a deliberately scaled-down representative; the structural pattern is present but the production-scale resource exhaustion / catastrophic timing is not exercised at fixture size. To trigger the documented kernel-crash, multiply the entity-replication factor by ~10^N as noted in the reproducer recipe.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 12) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
