# Pf011 — `EntityCluster` infinite recursion / leak on pathological deep chain

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.10 (`12-10-perf`)  
**Fixture**: [`step-examples/12-10-perf/Pf011.stp`](../../step-examples/12-10-perf/Pf011.stp)  

## Sources
- 08-occt-gitlog.md G031 (Mantis 0031435
- OCCT e00b8ed)

## Description

Internal `StepData_EntityCluster` chain allows cyclic links and recursion in `Value`/`SetValue`/`Append`; pathological inputs crash via stack overflow and leak memory through a missing destructor.

## Reproducer recipe

hand-construct entity cluster chain with cyclic next-pointer.

## Expected kernel behavior

iterative traversal; cycle detection;

## Notes

**See also**: Pf010. Validation observed: this is a deliberately scaled-down representative; the structural pattern is present but the production-scale resource exhaustion / catastrophic timing is not exercised at fixture size. To trigger the documented kernel-crash, multiply the entity-replication factor by ~10^N as noted in the reproducer recipe.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_COMPLEX_ORDER)` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **OCC behavior**: this is a deliberately scaled-down representative; the structural pattern is present but the production-scale resource exhaustion / catastrophic timing is not exercised at fixture size. To trigger the documented kernel-crash, multiply the entity-replication factor by ~10^N as noted in the reproducer recipe.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Pf010](Pf010.md)
