# U008 — NX subassemblies with mixed inch + mm — inconsistent per-part scaling

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.5 (`12-5-units`)  
**Fixture**: [`step-examples/12-5-units/U008.stp`](../../step-examples/12-5-units/U008.stp)  
**Sender**: any sender mixing inch and mm subassemblies  

## Sources
- 10-vendor-kbs.md V056 (eng-tips threads/step-file-import-scaling-problem.395939)
- 10-vendor-kbs.md V049 (assembly STEP scale mismatch)

## Description

STEP files with mixed inch and mm contexts in subassemblies cause inconsistent scaling: inch parts multiplied by 25.4, metric parts not, leading to mis-scaled assembly.

## Reproducer recipe

AP242 assembly with one component's `GEOMETRIC_REPRESENTATION_CONTEXT` declaring inch (via `CONVERSION_BASED_UNIT('INCH',...)`) and another declaring `SI_UNIT(.MILLI.,.METRE.)`, joined via `MAPPED_ITEM` whose `mapping_source` carries no scale.

## Expected kernel behavior

apply unit context per child sub-representation; never assume a document-wide unit; scale at `MAPPED_ITEM` / sub-assembly boundary using each child's own `GEOMETRIC_REPRESENTATION_CONTEXT` global units.

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `signal(11)` |
| `occt_heal_off` | `signal(11)` |
| `gmsh_autofix_on` | `signal(11)` |
| `gmsh_autofix_off` | `signal(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_COMPLEX_ORDER)` |
| `manifold` | `process_signal` |

**Cross-oracle classification**: Spec-clean Part-21; OCC crashes (kernel-side bug)

## Tier-3 geometric introspection

- **load**: `None`
- **n_faces**: 0
- **n_edges_total**: None
- **n_vertices_total**: None
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=signal(11)/signal(11) gmsh=signal(11) ifc=schema_n/a` |
