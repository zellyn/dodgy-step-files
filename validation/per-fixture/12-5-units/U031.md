# U031 — `CONVERSION_BASED_UNIT('DEG',#9)` / DEG vs RADIAN encoding via complex entity

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.5 (`12-5-units`)  
**Fixture**: [`step-examples/12-5-units/U031.stp`](../../step-examples/12-5-units/U031.stp)  

## Sources
- 07-stepcode-ifcopenshell.md T025 (complex entity `( CONVERSION_BASED_UNIT('DEG',#9) NAMED_UNIT(*) PLANE_ANGLE_UNIT() )`)
- ISO 10303-21 Ed.3 §12.2.5

## Description

Plane-angle units are typically encoded as a complex entity combining `CONVERSION_BASED_UNIT('DEG',#refToRadianMeasure)`, `NAMED_UNIT(*)`, and `PLANE_ANGLE_UNIT()`. Defects: members in non-alphabetical order; conversion factor wrong (not `pi/180`); name spelled `'DEGREES'` / `'deg'`; missing `PLANE_ANGLE_UNIT()` member entirely so kernel mistakes it for length.

## Reproducer recipe

`#10=( CONVERSION_BASED_UNIT('DEG',#9) NAMED_UNIT(*) PLANE_ANGLE_UNIT() );` with `#9 = PLANE_ANGLE_MEASURE_WITH_UNIT(PLANE_ANGLE_MEASURE(0.0174533),#radian)` — and a variant with factor `1.0` (treats degrees as radians).

## Expected kernel behavior

accept space-separated complex record; verify factor ≈ pi/180 within tolerance; resolve via `PLANE_ANGLE_UNIT` membership not the `name` string.

## Notes

**See also**: U004.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_COMPLEX_ORDER)` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [U004](U004.md)
