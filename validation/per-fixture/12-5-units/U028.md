# U028 — Two `GEOMETRIC_REPRESENTATION_CONTEXT` instances with different `LENGTH_UNIT` (mm BRep vs metre PMI/CGR)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.5 (`12-5-units`)  
**Fixture**: [`step-examples/12-5-units/U028.stp`](../../step-examples/12-5-units/U028.stp)  

## Sources
- 02-caxif.md "shape_representation/draughting_model/CGR must share the same geometric_representation_context" (PMI v4.1 §9.4.2
- Supplemental Geometry §5.4)
- 17-standards-bodies.md M020 (PrePoMax forum
- OCCT default-mm vs CONVERSION_BASED chain)

## Description

A single STEP file carries two `GEOMETRIC_REPRESENTATION_CONTEXT` instances with different `LENGTH_UNIT`s — for example one named 'mm_brep' in millimetres and another 'm_pmi' in metres — where a `SHAPE_REPRESENTATION` (B-rep) uses the mm context and a `CONSTRUCTIVE_GEOMETRY_REPRESENTATION` (CGR) or `DRAUGHTING_MODEL` (PMI) uses the metre context. Part shape, supplemental geometry CGR, and PMI draughting models are required to share the same context; when they don't, units / uncertainty / coordinate space differ between sub-representations. Receivers see Cartesian points apparently in mixed scale; OCCT-based readers default to mm and silently mis-scale the non-mm ones.

## Reproducer recipe

part with two GRCs — one in mm for the BREP, another in m for the CGR; PMI references supplemental elements from the second CGR.

## Expected kernel behavior

reject or propagate units consistently; resolve full unit-conversion chain to a single SI scale factor per sub-representation; refuse to silently default.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**Originally also catalogued as**: U029, M009. **See also**: N016.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 30) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [N016](N016.md)

**Originally also catalogued as**: U029, M009
