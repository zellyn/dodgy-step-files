# U033 — Unit-default flag silently overridden by file's first unit-record

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.5 (`12-5-units`)  
**Fixture**: [`step-examples/12-5-units/U033.stp`](../../step-examples/12-5-units/U033.stp)  

## Sources
- OCCT MANTIS#0033564
- OCCT MANTIS#0032748

## Description

A reader is configured with a default unit (`xstep.cascade.unit`). The file emits an explicit `LENGTH_UNIT` record. Reader assumes the configured default but the file's record overrides without warning; conversely, when the configured unit and the file's unit disagree, the reader may apply the conversion factor twice.

## Reproducer recipe

STEP file with explicit `LENGTH_UNIT` of inches; reader configured with default `xstep.cascade.unit = MM`. Behaviour varies across versions (some apply file unit, some apply default, some apply both as conversion).

## Expected kernel behavior

file-declared units take precedence; the configured default applies only when the file omits the unit record; emit a diagnostic when the two disagree.

## Notes

**See also**: U025, U028.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_COMPLEX_ORDER)` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [U025](U025.md), [U028](U028.md)
