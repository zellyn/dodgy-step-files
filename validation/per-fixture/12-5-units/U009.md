# U009 — KiCad assembly: board mm + component model in inch, naive merge breaks scale

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.5 (`12-5-units`)  
**Fixture**: [`step-examples/12-5-units/U009.stp`](../../step-examples/12-5-units/U009.stp)  
**Sender**: vendor-supplied component STEP (US-origin, inch-based)  

## Sources
- 22-niche-forums.md Y011 (gitlab.com/kicad/code/kicad/-/issues/22139)

## Description

Component vendor STEP has `LENGTH_UNIT() NAMED_UNIT(*) SI_UNIT(.INCH.)` (or `CONVERSION_BASED_UNIT` to inch); board file has `SI_UNIT(.MILLI.,.METRE.)`. Naive merger that doesn't apply per-context conversion places components 25.4× too large or too small relative to the board.

## Reproducer recipe

synthesize component STEP with explicit `CONVERSION_BASED_UNIT('INCH', ..., 25.4·MILLIMETRE)`; merge into mm board without unit-aware transformer.

## Expected kernel behavior

heal — apply unit conversion at `MAPPED_ITEM` / sub-assembly boundary using each component's own `GEOMETRIC_REPRESENTATION_CONTEXT` global units.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `signal(11)` |
| `occt_heal_off` | `signal(11)` |
| `gmsh_autofix_on` | `signal(11)` |
| `gmsh_autofix_off` | `signal(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_COMPLEX_ORDER)` |
| `manifold` | `process_signal` |

**Cross-oracle classification**: Spec-clean Part-21; OCC crashes (kernel-side bug)

## Tier-3 geometric introspection

- **load**: `None`
- **n_faces**: 0
- **n_edges_total**: None
- **n_vertices_total**: None
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=signal(11)/signal(11) gmsh=signal(11) ifc=schema_n/a` |
