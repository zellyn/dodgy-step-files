# Wr027 — Unit context emitted twice with different units (HEADER vs DATA mismatch)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr027.stp`](../../step-examples/12-13-writer-pathology/Wr027.stp)  
**Sender**: Writers where the HEADER is composed by a templating subsystem and the DATA-section unit context is emitted by a separate geometry serialiser, with no cross-check  

## Sources
- OCCT MANTIS#0029xxx-class "unit mismatch in HEADER vs DATA"
- bug-reporter language: "STEP file has two different units"

## Description

`FILE_NAME` or `FILE_DESCRIPTION` lists `'mm'` as the unit (informally, in the description string), while the DATA-section `LENGTH_UNIT` resolves to `INCH` (or vice versa). The geometry is correct in DATA-section units; the HEADER claim is decorative but misleading. Bug-reporter language: "header says mm, geometry is inches", "STEP file has two different units", "unit declaration contradicts geometry".

## Reproducer recipe

`FILE_DESCRIPTION(('part in mm'),'2;1')` paired with a `LENGTH_UNIT` resolving to `INCH` (`#20=(LENGTH_UNIT() NAMED_UNIT(*) CONVERSION_BASED_UNIT('INCH',#21))`).

## Expected kernel behavior

trust the DATA-section unit context (the schema-meaningful one) and treat HEADER prose as informational. Optionally warn on mismatch.

## Notes

§12.5 covers unit defects; Wr027 documents producer-side root cause.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
