# Wr031 — Schema downgrade on export (AP242 input emitted as AP203, entities lost)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr031.stp`](../../step-examples/12-13-writer-pathology/Wr031.stp)  
**Sender**: Writers configured to emit AP203 because a downstream consumer doesn't speak AP242, with no warning that AP242-specific content (PMI, kinematics, composite materials) is dropped silently  

## Sources
- prostep ivip "AP242→AP203 downgrade"
- bug-reporter language: "STEP file lost PMI on AP203 export"

## Description

The input declared `FILE_SCHEMA(('AP242_MANAGED_MODEL_BASED_3D_ENGINEERING'))` and contained `GEOMETRIC_TOLERANCE`, `DRAUGHTING_ANNOTATION_OCCURRENCE`, and `KINEMATIC_LINK` entities. The re-emitted file declares `FILE_SCHEMA(('AUTOMOTIVE_DESIGN'))` and contains only the geometry — every AP242-specific entity is gone. Bug-reporter language: "schema downgrade lost data", "AP203 export drops PMI", "STEP went from 242 to 203".

## Reproducer recipe

a fixture with `FILE_SCHEMA(('AUTOMOTIVE_DESIGN { 1 0 10303 214 3 1 1 }'))` and only AP203-compatible entities, with a comment noting the input was AP242 with PMI and kinematics.

## Expected kernel behavior

preserve the input schema on round-trip when possible; warn loudly on any schema downgrade that drops entities.

## Notes

§12.7 covers PMI loss; Wr031 covers the schema-level root cause. Bytes alone are insufficient to demonstrate this defect — needs sibling input fixture. Tagged provenance_tier: requires-sibling-pair.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
