# Wr013 — Forward references where sequential numbering was expected

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr013.stp`](../../step-examples/12-13-writer-pathology/Wr013.stp)  
**Sender**: Writers that emit entities in topological-construction order without a topological sort; observed in some open-source exporters  

## Sources
- OCCT bug "kernel chokes on forward refs"
- bug-reporter language: "ref to undefined #N", "forward declaration"

## Description

The producer emits `#5 = ORIENTED_EDGE(...,#10,...)` before `#10` has been defined. Spec-conforming readers must do a two-pass parse (or a deferred-resolution mechanism), but a class of single-pass kernels reports "reference to undefined instance #10" and rejects. Bug-reporter language: "forward references in STEP", "ref before def", "kernel doesn't support out-of-order STEP".

## Reproducer recipe

`#5 = CARTESIAN_POINT('p',(0.,0.,0.));` followed later by `#1 = AXIS2_PLACEMENT_3D('a',#5,$,$);` — but `#5` appears first; or, more pathologically, `#1 = ... (#5,...);` first, then `#5 = ...;` second (forward).

## Expected kernel behavior

implement two-pass parsing or deferred-resolution; the spec permits forward references. Reject only if a reference remains unresolved after the full file is parsed.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Lh006-class issue but writer-attributable.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
