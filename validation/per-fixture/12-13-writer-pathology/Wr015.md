# Wr015 — Duplicate `#N` instance numbers in the same DATA section

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr015.stp`](../../step-examples/12-13-writer-pathology/Wr015.stp)  
**Sender**: Writers with broken counter logic when emitting multi-section or multi-pass files (collision between sections or passes)  

## Sources
- OCCT MANTIS#0028xxx-class
- bug-reporter language: "duplicate instance number", "two entities with same #N"

## Description

Two entities in the same DATA section share the same `#N`. Spec violation. Receivers diverge: first-wins, last-wins, both-loaded-and-randomly-resolved, reject. Bug-reporter language: "duplicate #5", "same instance number twice", "STEP file has collision".

## Reproducer recipe

`#10 = CARTESIAN_POINT('a',(0.,0.,0.));` and later `#10 = CARTESIAN_POINT('b',(1.,1.,1.));` in the same DATA section.

## Expected kernel behavior

reject as malformed; spec mandates unique instance numbers per DATA section. Document the resolution policy if the kernel chooses tolerant first-wins / last-wins.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Lh010-class.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_DUP_REF)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `occt_diag_off` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `gmsh_diag` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
