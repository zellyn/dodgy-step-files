# Wr014 — Sparse instance numbering with huge gaps

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr014.stp`](../../step-examples/12-13-writer-pathology/Wr014.stp)  
**Sender**: Writers that hash entity identity to a wide integer space and use the hash as the instance number directly  

## Sources
- OCCT forum "instance numbers run to 10 million for 50 entities"
- bug-reporter language: "huge instance numbers", "memory blows up loading STEP"

## Description

A file with 50 entities uses instance numbers like `#42`, `#9876543`, `#21000000` — the maximum number is many orders of magnitude larger than the entity count. Spec-conforming, but a class of receivers naïvely pre-allocates an array of size `max(#N)`, blowing up memory. Bug-reporter language: "STEP file with huge instance numbers", "out of memory loading 50-entity file", "instance #N overflow".

## Reproducer recipe

a 5-entity file where the highest instance number is `#999999999`.

## Expected kernel behavior

use a sparse map (hashtable) for instance lookup; never assume `#N` is a dense array index. Reject only if `#N` exceeds 32-bit signed integer range and the kernel uses int32 indices internally.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Spec does not bound `#N`; ISO 10303-21 Ed.3 §5 defines it as an unbounded positive integer.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
