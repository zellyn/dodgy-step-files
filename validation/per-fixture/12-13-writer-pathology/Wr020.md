# Wr020 — Re-export drops feature labels and product names

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr020.stp`](../../step-examples/12-13-writer-pathology/Wr020.stp)  
**Sender**: Writers without a label/name propagation layer  

## Sources
- OCCT name-round-trip tests
- CAx-IF round-trip notes
- bug-reporter language: "feature names disappeared"

## Description

User-assigned feature labels — e.g. `PRODUCT.name = 'left_bracket'`, face labels via `PRESENTATION_LAYER_ASSIGNMENT`, descriptive `name` strings on `MANIFOLD_SOLID_BREP` — are replaced with empty strings or auto-generated identifiers (`'BREP_001'`, `''`). The geometry is preserved but the human-readable labelling is lost. Bug-reporter language: "feature names dropped", "names replaced with auto-IDs", "STEP file forgot the part name".

## Reproducer recipe

a STEP fixture where every `name` attribute is `''` and `PRODUCT.name = 'BREP_001'` despite a comment indicating the input had named features.

## Expected kernel behavior

preserve `name` attributes and `PRESENTATION_LAYER_ASSIGNMENT` content end-to-end.

## Notes

Subset of the broader "metadata stripped on re-export" pattern.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
