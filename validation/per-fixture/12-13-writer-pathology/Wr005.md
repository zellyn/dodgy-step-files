# Wr005 — Floating-point format inconsistency within one file

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr005.stp`](../../step-examples/12-13-writer-pathology/Wr005.stp)  
**Sender**: Multi-source writer pipelines that compose entities from different subsystems (e.g. older Pro/E, multi-pass NX export)  

## Sources
- OCCT MANTIS#0028xxx-class
- FreeCAD forum "round-trip changes every coordinate"
- bug-reporter language: "every number formatted differently"

## Description

Floating-point values appear in three or more distinct formats within a single DATA section: `1.0E-7`, `1e-07`, `0.0000001`, and `1.0e-007` (extra exponent digit) all coexist. The spec permits any valid REAL formatting, so each value parses correctly, but tools that hash REAL streams to detect change disagree on equality, regex-based extractors fail, and human reviewers find the file unreadable. Bug-reporter language: "STEP file has weird number formatting", "every coordinate looks different", "scientific notation inconsistent".

## Reproducer recipe

one CARTESIAN_POINT with coordinates `(1.0E-7, 1e-07, 0.0000001)` and a second with `(1.0e-007, 1.0, 1.)`.

## Expected kernel behavior

parse all valid REAL formats; canonicalise on re-emit if the kernel's policy is normalised output.

## Notes

ISO 10303-21 §6 allows any of these forms; producer is sloppy but conforming.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_REAL_NO_DOT)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: OCC silently heals — pure-Python rejects this

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
