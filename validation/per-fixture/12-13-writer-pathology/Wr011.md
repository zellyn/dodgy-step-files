# Wr011 — Empty parameter list `()` where the schema requires at least one element

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr011.stp`](../../step-examples/12-13-writer-pathology/Wr011.stp)  
**Sender**: Writers that prune the model and forget to suppress now-empty container entities  

## Sources
- OCCT MANTIS#0027xxx-class
- bug-reporter language: "empty list, expected at least one face"

## Description

A `CLOSED_SHELL` entity with an empty face-list `()` appears in the file because the producer pruned all faces but left the shell wrapper. Schema requires the face-list to be non-empty. Some readers reject; some accept and produce a vacuous shell that flows through the rest of the import as an empty solid. Bug-reporter language: "empty shell", "STEP closed_shell with no faces", "writer left a stub entity".

## Reproducer recipe

`CLOSED_SHELL('hollow',())` referenced by a `MANIFOLD_SOLID_BREP`.

## Expected kernel behavior

reject the empty-shell case as schema-violating; do not silently accept the placeholder.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

Strongly related to Wr020 (empty SHAPE_DEFINITION_REPRESENTATION chains). **OCC behavior**: silently accepts the empty face-list and produces a vacuous shell with no diagnostic; the kernel mishandling is the documented signal here.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
