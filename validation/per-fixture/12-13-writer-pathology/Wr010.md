# Wr010 — Re-emitted `*` (overridden) where schema does not allow it

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr010.stp`](../../step-examples/12-13-writer-pathology/Wr010.stp)  
**Sender**: Buggy writers that mis-handle the inheritance-override mechanism for complex (subtype) entities and apply it to non-inherited attributes  

## Sources
- OCCT forum "writer emitted asterisk in CARTESIAN_POINT coords"
- bug-reporter language: "asterisk in geometry"

## Description

The `*` token denotes an attribute inherited from a supertype and overridden in a subtype context — valid only inside complex (subtype-stack) records. The producer emits `*` in a simple-entity instance where no supertype exists, producing `CARTESIAN_POINT('p',(*,0.0,0.0))` or similar. Receivers diverge: some treat `*` as an error, some treat it as zero, some treat it as `$`. Bug-reporter language: "asterisk where number expected", "STEP file has stars in coordinates", "writer used override-marker incorrectly".

## Reproducer recipe

`CARTESIAN_POINT('p',(*,0.0,0.0))` in a non-subtype context.

## Expected kernel behavior

reject as malformed; `*` is meaningful only in complex-instance subtype-override position.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

ISO 10303-21 §10 defines `*` semantics narrowly.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
