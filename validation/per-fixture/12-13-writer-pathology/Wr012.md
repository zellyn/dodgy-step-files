# Wr012 — Random `#N` numbering with no monotonic structure

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr012.stp`](../../step-examples/12-13-writer-pathology/Wr012.stp)  
**Sender**: Writers that allocate instance numbers from a hashed pool keyed on entity-identity rather than emit-order; observed in some Java-based exporters  

## Sources
- OCCT forum "STEP file has random instance numbers"
- bug-reporter language: "instance numbers jump around"

## Description

Instance numbers in the DATA section follow no monotonic pattern: `#1`, then `#1000`, then `#5`, then `#1003`, then `#7`. The spec does not mandate monotonic numbering, so the file is well-formed; but human reviewers, line-based diff tools, and forward-reference-detecting linters all stumble. Bug-reporter language: "STEP instance numbers are random", "file numbering jumps", "can't follow refs visually".

## Reproducer recipe

a Part-21 file in which the order of `#NNN` in the DATA section is `#1, #1000, #5, #1003, #7, #1006`.

## Expected kernel behavior

parse normally; numbering is content-addressable, not order-bearing. A re-emit policy may renumber sequentially.

## Notes

Discoverability is the main cost; correctness is unaffected.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
