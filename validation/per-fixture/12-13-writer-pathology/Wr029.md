# Wr029 — `FILE_DESCRIPTION` strings containing newlines or unescaped control characters

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr029.stp`](../../step-examples/12-13-writer-pathology/Wr029.stp)  
**Sender**: Writers that paste user-entered description text into `FILE_DESCRIPTION` without escaping line breaks  

## Sources
- OCCT forum "header description has line break"
- bug-reporter language: "string in STEP wraps mid-line"

## Description

A `FILE_DESCRIPTION(('line one\nline two'),'2;1')` where the literal `\n` is an embedded newline (not the two-character escape — the actual 0x0A byte) terminates the string for crude line-based parsers. Spec violation per ISO 10303-21 §6.4 (control characters in string literals). Bug-reporter language: "STEP file string has line break", "FILE_DESCRIPTION corrupt".

## Reproducer recipe

literal `FILE_DESCRIPTION(('first line` (newline) `second line'),'2;1')`.

## Expected kernel behavior

reject as malformed per spec; do not silently splice the broken string back together because the splice point is undefined.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

Le017 covers from read side; Wr029 attributes to writer. **OCC behavior**: silently accepts the broken-string file (no diagnostic, empty result); kernel mishandling — not what kernels should do.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
