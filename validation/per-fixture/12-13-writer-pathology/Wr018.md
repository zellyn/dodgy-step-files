# Wr018 — Empty `SHAPE_DEFINITION_REPRESENTATION` chain (placeholder structure with no geometry)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr018.stp`](../../step-examples/12-13-writer-pathology/Wr018.stp)  
**Sender**: Writers that initialise the product structure tree before geometry is computed and forget to suppress empty trees on export when computation fails or is cancelled  

## Sources
- OCCT MANTIS#0027xxx-class
- bug-reporter language: "STEP loaded but model is empty"

## Description

The file contains a complete product-structure scaffold (`APPLICATION_CONTEXT`, `PRODUCT`, `PRODUCT_DEFINITION`, `SHAPE_DEFINITION_REPRESENTATION`, `SHAPE_REPRESENTATION`) with all the right cross-references, but the `SHAPE_REPRESENTATION.items` list is empty or contains only placement axes — no `MANIFOLD_SOLID_BREP`, no `SHELL_BASED_SURFACE_MODEL`, no `TESSELLATED_SHELL_REPRESENTATION`. The receiver loads "successfully" but reports zero solids. Bug-reporter language: "STEP file loaded but nothing visible", "empty model", "product tree but no geometry", "ghost STEP file".

## Reproducer recipe

full scaffold ending with `SHAPE_REPRESENTATION('main',(#13),#24)` where `#13` is just an `AXIS2_PLACEMENT_3D` and there is no shape.

## Expected kernel behavior

warn the user that the file describes a product but contains no geometry. Do not raise an error (well-formed empty product is permitted).

## Notes

A "successful empty load" is the most confusing failure mode for end users.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
