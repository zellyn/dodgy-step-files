# Wr034 — Coordinate-system axis swap without notification (Y-up vs Z-up)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr034.stp`](../../step-examples/12-13-writer-pathology/Wr034.stp)  
**Sender**: Web-CAD and game-engine-derived writers whose internal convention is Y-up, exporting to STEP without applying the Z-up transform  

## Sources
- OCCT forum "STEP from web-CAD has wrong axis"
- bug-reporter language: "model is on its side after import", "Y up vs Z up confusion"

## Description

The geometry coordinates were authored in a Y-up coordinate system (the engine convention), but the writer emitted them as-is into STEP without applying the rotation that makes Z the up-axis (STEP / CAD convention). The receiver loads the geometry rotated 90° around X. Bug-reporter language: "model lying on its side", "Y-up vs Z-up", "STEP imported sideways", "coordinate axes swapped".

## Reproducer recipe

a `MANIFOLD_SOLID_BREP` whose vertices imply a vertical extent along Y rather than Z, with a comment noting the input was authored Y-up and emitted without rotation.

## Expected kernel behavior

trust the file's coordinate system (DATA-section AXIS2_PLACEMENT_3D); if the application expects Z-up, surface a "model appears Y-up" diagnostic when geometry's bounding box has inverted aspect.

## Notes

Symptom-match search-term: "STEP imported on its side".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
