# Wr035 — Coordinate scale-factor applied twice (model 1000× larger or smaller than expected)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr035.stp`](../../step-examples/12-13-writer-pathology/Wr035.stp)  
**Sender**: Writers with bugs in unit-conversion: applying the m-to-mm factor in both the coordinate transform and the unit declaration  

## Sources
- OCCT MANTIS#0030xxx-class
- bug-reporter language: "model 1000× too small / too large"

## Description

Numeric coordinates are scaled by 1000 (input was in metres, internal store is in millimetres, output applied the m-to-mm factor *and* declared the unit as `MILLI METRE` while the coordinates already reflect the mm scale, so the receiver applies the factor a second time). The result: the receiver sees a model 1000× larger or smaller than intended. Bug-reporter language: "model 1000× too small after import", "STEP file scaled by 1000", "tiny model", "huge model", "millimetre-vs-metre mistake".

## Reproducer recipe

a CARTESIAN_POINT at `(1000.0, 1000.0, 1000.0)` with `LENGTH_UNIT` declared as `MILLI METRE`, comment noting input was a 1.0-metre cube.

## Expected kernel behavior

trust the DATA-section unit declaration. If a receiver application has a "model size sanity check" it can warn when the bounding box exceeds plausible bounds, but the kernel itself should not second-guess.

## Notes

§12.5 covers unit defects from receiver perspective.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
