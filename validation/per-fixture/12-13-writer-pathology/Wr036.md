# Wr036 — Re-export inverts solid orientation (inside becomes outside)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr036.stp`](../../step-examples/12-13-writer-pathology/Wr036.stp)  
**Sender**: Writers whose internal half-space convention differs from STEP's and that do not apply the conversion on export  

## Sources
- OCCT MANTIS#0028xxx
- bug-reporter language: "STEP solid is inside-out after re-export", "normals flipped"

## Description

The input had a `MANIFOLD_SOLID_BREP` whose face normals point outward (kernel convention: solid material is on the negative side of the face normals, exterior is positive). The re-emitted file has all face normals reversed: the solid is topologically intact but represents an infinite material region with a single hole (the original solid's volume). Bug-reporter language: "STEP solid inside out", "normals flipped on re-export", "solid is inverted".

## Reproducer recipe

a unit cube `MANIFOLD_SOLID_BREP` with all `ADVANCED_FACE.same_sense` flags set to `.F.` and corresponding `FACE_OUTER_BOUND.orientation` flipped — geometry is the cube but material is "outside the cube".

## Expected kernel behavior

detect orientation by computing signed volume of the closed shell; if negative, flip and warn (or reject under strict policy).

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Common in writers from a half-space-CSG kernel.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
