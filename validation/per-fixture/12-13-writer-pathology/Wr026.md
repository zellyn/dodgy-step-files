# Wr026 — Vendor-specific FILE_DESCRIPTION strings that downstream readers special-case

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr026.stp`](../../step-examples/12-13-writer-pathology/Wr026.stp)  
**Sender**: Writers that include vendor-version strings in `FILE_DESCRIPTION.description` such that downstream readers branch on them  

## Sources
- OCCT `STEPCAFControl_Reader` source
- CAx-IF interop "vendor sniffing"
- bug-reporter language: "STEP from vendor X behaves weirdly"

## Description

The producer writes `FILE_DESCRIPTION(('CAD-System-X version 12.4.1 build 2026-04-15'),'2;1')`. Some receivers contain heuristic special-casing keyed on vendor strings — they apply a "fix-up X model" filter only if the vendor string matches. This couples the receiver's behaviour to the producer's exact wording, so a minor wording change ("CAD-System X" with a space versus "CAD-System-X" with a hyphen) silently disables the fix-up. Bug-reporter language: "STEP file behaves differently after vendor renamed product", "kernel detects wrong vendor", "vendor sniffing failed".

## Reproducer recipe

a fixture with `FILE_DESCRIPTION(('CAD-System-X v12.4.1'),'2;1')` and a comment noting the receiver special-cases the exact spelling.

## Expected kernel behavior

avoid behaviour-altering vendor sniffing; if vendor-specific compatibility is needed, expose it as an explicit user opt-in.

## Notes

Sender-attribution in `FILE_DESCRIPTION` is a long-standing interop hazard. Bytes alone are insufficient to demonstrate this defect — needs sibling input fixture. Tagged provenance_tier: requires-sibling-pair.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
