# Wr009 — Spurious `$` for required parameter (writer omits required value)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr009.stp`](../../step-examples/12-13-writer-pathology/Wr009.stp)  
**Sender**: Older Pro/E and some lightweight web-CAD STEP exporters  

## Sources
- OCCT MANTIS#0026xxx-class "missing direction in AXIS2_PLACEMENT_3D from kernel X"
- bug-reporter language: "writer skipped required attribute"

## Description

The producer emits `$` (omitted) where the EXPRESS schema declares the attribute as required (non-OPTIONAL). Example: `AXIS2_PLACEMENT_3D('placement',#10,$,$)` where the schema specifies `axis` and `ref_direction` as effectively required when both are present (one cannot be `$` if the other is non-`$` and the Z-axis convention is to be applied). Some kernels invent a default direction; others reject. Bug-reporter language: "STEP file has dollar signs for required fields", "missing axis direction", "writer omitted required attribute".

## Reproducer recipe

`AXIS2_PLACEMENT_3D('p',#10,$,$)` paired with a `MANIFOLD_SOLID_BREP` that depends on the placement.

## Expected kernel behavior

reject as malformed when a required attribute is `$`; do not silently substitute Z+X defaults because the producer's intent is unrecoverable.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

AP242 marks `axis` and `ref_direction` as OPTIONAL in the EXPRESS — but most receivers expect both populated when the placement is referenced from a non-trivial geometry context. **OCC behavior**: silently substitutes the Z+X default convention rather than rejecting; this is the kernel mishandling the catalog above forbids.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
