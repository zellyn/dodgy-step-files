# Wr028 — `FILE_NAME.author` and `originating_system` fields blank or auto-filled with placeholder

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr028.stp`](../../step-examples/12-13-writer-pathology/Wr028.stp)  
**Sender**: Writers that do not capture user identity / origin and emit empty strings  

## Sources
- prostep ivip CAx-IF
- bug-reporter language: "STEP file has no provenance"

## Description

`FILE_NAME` records `author = ('')`, `organization = ('')`, `originating_system = ''`, `authorisation = ''`. The file is well-formed; provenance and audit-trail are lost. Bug-reporter language: "STEP has no author", "can't tell who created this STEP", "lost provenance".

## Reproducer recipe

standard `FILE_NAME` with all author/org/system fields as empty strings.

## Expected kernel behavior

parse normally; if the kernel re-emits, populate at least `originating_system` with the kernel's identity.

## Notes

Audit-trail loss but no functional consequence.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
