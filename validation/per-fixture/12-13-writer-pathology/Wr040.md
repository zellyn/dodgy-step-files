# Wr040 — Empty `name` strings on every entity (writer pre-fills `''` instead of omitting)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.13 (`12-13-writer-pathology`)  
**Fixture**: [`step-examples/12-13-writer-pathology/Wr040.stp`](../../step-examples/12-13-writer-pathology/Wr040.stp)  
**Sender**: Writers that always populate the `name` attribute (mandatory in EXPRESS) with `''` rather than carrying through the original `name` if any  

## Sources
- OCCT name-round-trip tests
- bug-reporter language: "every entity name is empty"

## Description

Every entity in DATA (`CARTESIAN_POINT`, `DIRECTION`, `EDGE_CURVE`, `ADVANCED_FACE`, etc.) has `name = ''`. The schema requires `name`, so empty string is well-formed; but `PRESENTATION_LAYER_ASSIGNMENT` and other display-name-bearing references find empty strings everywhere and the assembly tree displays as a forest of unnamed nodes. Bug-reporter language: "every entity name is empty", "STEP file has no labels", "names lost on re-export".

## Reproducer recipe

a complete fixture where every entity's `name` first attribute is `''`.

## Expected kernel behavior

preserve any non-empty input names on round-trip; emit `''` only for entities with no name in input. This is a subset of Wr020.

## Notes

Most-frequent name-round-trip complaint.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `n/a` |
| Reverse self-evidence | `n/a` |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
