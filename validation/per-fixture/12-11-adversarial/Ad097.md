# Ad097 — Comment string inside STEP file breaks parser

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.11 (`12-11-adversarial`)  
**Fixture**: [`step-examples/12-11-adversarial/Ad097.stp`](../../step-examples/12-11-adversarial/Ad097.stp)  

## Sources
- OCCT MANTIS#0030053
- OCCT MANTIS#0030280

## Description

A STEP file comment `/* ... */` contains characters that resemble entity-record terminators or section markers. Lexer that uses regex-based comment stripping can be tricked into ending the comment early, leaving the comment's tail interpreted as records.

## Reproducer recipe

Comment containing `*/ENDSEC;` — naive regex strips up to first `*/`, leaving `ENDSEC;` interpreted as section terminator.

## Expected kernel behavior

comment-stripping is character-by-character with explicit `/*` and `*/` matching; never use greedy regex.

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT rejects (`reject`). OCC enforces stricter framing than the spec text requires.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `reject` |
| `occt_heal_off` | `reject` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `reject` |
| `part21_strict` | `accept` |
| `manifold` | `step_read_failed` |
| `occt_diag_on` | 1:**** ERR StepFile : Undefined Parsing: Line 4: Incorrect ... |
| `occt_diag_off` | 1:**** ERR StepFile : Undefined Parsing: Line 4: Incorrect ... |
| `gmsh_diag` | 1:**** ERR StepFile : Undefined Parsing: Line 4: Incorrect ... |

**Cross-oracle classification**: OCC stricter than spec — pure-Python accepts

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT rejects (`reject`). OCC enforces stricter framing than the spec text requires.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=reject/reject gmsh=reject ifc=reject` |
