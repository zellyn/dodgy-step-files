# Ad043 — STEP read raises uncaught exception on null / invalid entity reference

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.11 (`12-11-adversarial`)  
**Fixture**: [`step-examples/12-11-adversarial/Ad043.stp`](../../step-examples/12-11-adversarial/Ad043.stp)  

## Sources
- 03-occt-tests.md R043 (`bug22871`, `bug16351`, `bug23157`, `bug24383`, `bug25912`, `bug133_2/3/9`)
- R057 (material missing name `bug23251`)
- R045 (empty `EDGE_LOOP` `bug33307`)
- R067 (writer exception `bug24990`)
- 04-occt-translation.md Q078
- 08-occt-gitlog.md G027 (`STYLED_ITEM.item` NULL), G029 (NULL vertex name/id), G034, G035, G050, G063, G083, G085, G086, G109 (GD&T null-deref family)
- 18-other-issues.md I001, I003, I004, I044

## Description

Reader paths assume non-null entity references and throw an uncaught exception (access violation, out-of-range, or similar) when a STEP file references a non-existent entity, supplies a null `swept_curve`, has an empty edge loop, a `STYLED_ITEM` with a null `item` reference, a vertex with null name/id, etc. The exception escapes across the reader's API boundary and aborts the import.

## Reproducer recipe

any of the above patterns; e.g. `STYLED_ITEM('',(...),#null);` where `#null` is unresolved.

## Expected kernel behavior

every reader path must be defensive against null and dangling references — warn-and-skip the offending instance, return an empty/partial result, or signal failure via a status flag. Never let an unhandled exception escape across the parser/importer ABI.

## Notes

Cross-oracle: pure-Python Part-21 validator rejects (reject(E_UNRESOLVED_REFS)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_UNRESOLVED_REFS)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `occt_diag_off` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `gmsh_diag` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |

**Cross-oracle classification**: OCC silently heals — pure-Python rejects this

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator rejects (reject(E_UNRESOLVED_REFS)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-10` (rank 6) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
