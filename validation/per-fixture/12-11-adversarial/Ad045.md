# Ad045 — Healing pipeline raises exception on real input with `EDGE_CURVE` vertices off the `LINE` by sub-confusion offset ("must not throw")

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.11 (`12-11-adversarial`)  
**Fixture**: [`step-examples/12-11-adversarial/Ad045.stp`](../../step-examples/12-11-adversarial/Ad045.stp)  

## Sources
- 03-occt-tests.md R083 (`bug24126`, `bug24249_*`, `bug24934`, `bug7570`)
- R038, R075, R078, R079
- 22-niche-forums.md Y017 (gmsh ≥4.12 fatal "Could not fix wire" regression)

## Description

A healing pass over a real-world input shape throws an exception across the kernel ABI rather than producing a result, a warning, or an empty shape. Common trigger: an `EDGE_CURVE` whose `VERTEX_POINT`s do not lie on the edge's 3D `LINE` — a sub-confusion vertex/curve mismatch (e.g. ±1e-7 Y-offset from an X-axis `LINE`) — exposes a healing-path edge case. Reported failure modes include out-of-range index access on a wire-ordering pass, exceptions while evaluating an offset surface, healing reporting a negative face area, and fatal errors emerging from a wire-fix pass after a kernel upgrade.

## Reproducer recipe

any of pinned test inputs; or wire whose `EDGE_CURVE` start/end disagree by approximately the kernel's confusion tolerance when traced against the host surface.

## Expected kernel behavior

a healing pipeline run on any real-world input must terminate without throwing across the ABI. Kernels may report failure as a status flag, return an empty/partial shape, or downgrade to a warning; throwing an unhandled exception is not acceptable.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `warn-and-proceed`

## Notes

**See also**: Ad043. Validation observed: silent-empty rather than the cited crash. The structural attack pattern (e.g., billion-laughs / cyclic-reference / very-large-string) is demonstrated; production-scale exploitation requires inflating the size factor as described. Kernel-mishandling-by-silent-acceptance is itself a defect signal at fixture scale.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **OCC behavior**: silent-empty rather than the cited crash. The structural attack pattern (e.g., billion-laughs / cyclic-reference / very-large-string) is demonstrated; production-scale exploitation requires inflating the size factor as described. Kernel-mishandling-by-silent-acceptance is itself a defect signal at fixture scale.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Ad043](Ad043.md)
