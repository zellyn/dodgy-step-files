# Ad042 — Reference to entity-of-wrong-type in attribute slot

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.11 (`12-11-adversarial`)  
**Fixture**: [`step-examples/12-11-adversarial/Ad042.stp`](../../step-examples/12-11-adversarial/Ad042.stp)  

## Sources
- 06-nist-sfa.md S007
- 18-other-issues.md I001 (CATIA V6 SDR with `SHAPE_ASPECT` instead of `PROPERTY_DEFINITION` — null-handle deref)
- 08-occt-gitlog.md G002 (Datum_System mixed array → crash on multi-body), G016 (NAUO with PDS instead of PD)

## Description

Schema SELECT type validated by structural shape only; attribute type is wrong subtype, leading downstream chained calls (`x->A()->B()`) to deref a null handle.

## Reproducer recipe

minimum AP214 with `#27=SHAPE_DEFINITION_REPRESENTATION(#23,#26)` where `#23=SHAPE_ASPECT(...)` instead of `PROPERTY_DEFINITION_SHAPE`.

## Expected kernel behavior

schema-aware select-narrowing that distinguishes branches and emits `E_SELECT_BRANCH` instead of dereffing.

## Notes

Documented as 1088/1267 SDRs broken in a 430 MB CATIA V6 file. **See also**: Ad030. Validation observed: silent-empty rather than the cited crash. The structural attack pattern (e.g., billion-laughs / cyclic-reference / very-large-string) is demonstrated; production-scale exploitation requires inflating the size factor as described. Kernel-mishandling-by-silent-acceptance is itself a defect signal at fixture scale.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **OCC behavior**: silent-empty rather than the cited crash. The structural attack pattern (e.g., billion-laughs / cyclic-reference / very-large-string) is demonstrated; production-scale exploitation requires inflating the size factor as described. Kernel-mishandling-by-silent-acceptance is itself a defect signal at fixture scale.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Ad030](Ad030.md)
