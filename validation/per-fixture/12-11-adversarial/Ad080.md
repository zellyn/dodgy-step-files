# Ad080 — Two `DATA` sections with colliding `#NNN` id space in one file (token-boundary attack)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.11 (`12-11-adversarial`)  
**Fixture**: [`step-examples/12-11-adversarial/Ad080.stp`](../../step-examples/12-11-adversarial/Ad080.stp)  

## Sources
- 12-adversarial.md A025

## Description

Two `DATA;…ENDSEC;` sections appear in one file (illegal in Ed.2), often with concatenated section markers. Common attack shape: both DATA sections reuse the identical entity-id range `#1`..`#N` so any reader that builds a single `#`-keyed map silently overwrites the first section's entities with the second. Greedy regex parsers may match across the section boundary.

## Reproducer recipe

`…ENDSEC;DATA;…ENDSEC;DATA;…ENDSEC;`

## Expected kernel behavior

reject second `DATA` section unless Ed.3 multi-section; one DATA per file otherwise.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**See also**: Lh024, Ls024.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_DUP_REF)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 2:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 2:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 2:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 13) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Lh024](Lh024.md), [Ls024](Ls024.md)
