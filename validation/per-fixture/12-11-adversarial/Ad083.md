# Ad083 — Multiple `PRODUCT`s share the same name; only `NEXT_ASSEMBLY_USAGE_OCCURRENCE` id disambiguates (assembly reader flat-load label collision)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.11 (`12-11-adversarial`)  
**Fixture**: [`step-examples/12-11-adversarial/Ad083.stp`](../../step-examples/12-11-adversarial/Ad083.stp)  

## Sources
- 18-other-issues.md I021 (CadQuery #1962, KiCad), I077
- 03-occt-tests.md R057

## Description

Multiple child `PRODUCT` instances share the same `name` attribute across distinct `PRODUCT_DEFINITION` chains — for example three boards each named 'R0402' — and only the `NEXT_ASSEMBLY_USAGE_OCCURRENCE` `id` slot ('R1', 'R2', 'R3') disambiguates them. KiCad-style sources reuse one PRODUCT for many NAUOs; receiver dictionaries keyed on `name` instead of NAUO id collide and drop siblings.

## Reproducer recipe

`cq.Assembly.load("mobo-3D.step")`.

## Expected kernel behavior

treat IDs (entity #) as primary keys; disambiguate names; do not fail on duplicates.

## Notes

**See also**: Ad043. Validation observed: silent-empty rather than the cited crash. The structural attack pattern (e.g., billion-laughs / cyclic-reference / very-large-string) is demonstrated; production-scale exploitation requires inflating the size factor as described. Kernel-mishandling-by-silent-acceptance is itself a defect signal at fixture scale.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **OCC behavior**: silent-empty rather than the cited crash. The structural attack pattern (e.g., billion-laughs / cyclic-reference / very-large-string) is demonstrated; production-scale exploitation requires inflating the size factor as described. Kernel-mishandling-by-silent-acceptance is itself a defect signal at fixture scale.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 19) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Ad043](Ad043.md)
