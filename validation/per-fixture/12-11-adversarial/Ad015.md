# Ad015 — Empty aggregate where schema requires at least one element

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.11 (`12-11-adversarial`)  
**Fixture**: [`step-examples/12-11-adversarial/Ad015.stp`](../../step-examples/12-11-adversarial/Ad015.stp)  

## Sources
- 12-adversarial.md A015
- 18-other-issues.md I004 (`TESSELLATED_SHELL((), $)` crash)
- 03-occt-tests.md R045 (empty `EDGE_LOOP`), R037 (empty wire), R050 (empty `APPLIED_GROUP_ASSIGNMENT`)
- 08-occt-gitlog.md G023 (BSpline with zero poles), G035 (empty entity iterator)

## Description

Schema declares `LIST [1:?]`; producer emits empty list; downstream code dereferences `list[0]`. Or weight-array length mismatches pole-count.

## Reproducer recipe

`#1=ADVANCED_FACE('',(),#42,.T.);` (empty bound list); `#1=TESSELLATED_SHELL('',(),$);`.

## Expected kernel behavior

validate `LIST` bounds against schema lower bound at parse time; reject with `E_AGGREGATE_BOUNDS`.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `signal(11)` |
| `occt_heal_off` | `signal(11)` |
| `gmsh_autofix_on` | `signal(11)` |
| `gmsh_autofix_off` | `signal(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `process_signal` |

**Cross-oracle classification**: Spec-clean Part-21; OCC crashes (kernel-side bug)

## Tier-3 geometric introspection

- **load**: `None`
- **n_faces**: 0
- **n_edges_total**: None
- **n_vertices_total**: None
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=signal(11)/signal(11) gmsh=signal(11) ifc=schema_n/a` |
