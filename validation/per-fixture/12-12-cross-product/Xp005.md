# Xp005 — NURBS knot vector × control-point weight × tolerance-boundary cusp

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.12 (`12-12-cross-product`)  
**Fixture**: [`step-examples/12-12-cross-product/Xp005.stp`](../../step-examples/12-12-cross-product/Xp005.stp)  

## Sources
- synthesized cross-product (Pass E).

## Description

A `RATIONAL_B_SPLINE_CURVE` has a knot vector with two consecutive equal knots not justified by multiplicity; one of its weights is zero (rendering the rational evaluation indeterminate at that pole); and the control polygon has a cusp where two consecutive control points are separated by ~1e-7 — exactly at the OCCT default `Precision:: Confusion()` boundary. Three independent NURBS-quality invariants are violated.

## Reproducer recipe

Cubic `(BOUNDED_CURVE() B_SPLINE_CURVE(...) RATIONAL_B_SPLINE_CURVE(...) ...)` with five control points, knot vector `(0,0,0,0,0.5,0.5,1,1,1,1)` (the inner pair is duplicated without justification), weights `(1.0,0.0,1.0,1.0,1.0)`, and CPs `(0,0,0)`, `(1e-7,0,0)`, `(1,0,0)`, `(2,1,0)`, `(3,0,0)`.

## Expected kernel behavior

Each NURBS invariant inspected independently; the cusp-at-tolerance-boundary case is decided by the kernel's working precision policy.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
