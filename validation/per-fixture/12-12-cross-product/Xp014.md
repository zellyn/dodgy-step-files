# Xp014 — Open shell as MANIFOLD_SOLID_BREP.outer × tolerance-violation × unit mismatch

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.12 (`12-12-cross-product`)  
**Fixture**: [`step-examples/12-12-cross-product/Xp014.stp`](../../step-examples/12-12-cross-product/Xp014.stp)  

## Sources
- synthesized cross-product (Pass E).

## Description

A `MANIFOLD_SOLID_BREP` whose `outer` slot references an `OPEN_SHELL` (schema requires `CLOSED_SHELL`). The shell's vertex tolerances exceed the edge tolerances (hierarchy inverted). The containing context declares mm but coordinates are 1000× too small (i.e., authored in metres). Three orthogonal invariants violated.

## Reproducer recipe

`MANIFOLD_SOLID_BREP('',#OPEN_SHELL_REF);` with `VERTEX_POINT.tolerance > EDGE_CURVE.tolerance`; coordinates in the range 0.001 with mm declared.

## Expected kernel behavior

schema-class invariant; tolerance-hierarchy invariant; unit-vs-magnitude policy. Each addressed independently.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
