# Xp006 — Schema-mismatch × forward-reference × unresolved entity

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.12 (`12-12-cross-product`)  
**Fixture**: [`step-examples/12-12-cross-product/Xp006.stp`](../../step-examples/12-12-cross-product/Xp006.stp)  

## Sources
- synthesized cross-product (Pass E).

## Description

`FILE_SCHEMA` declares `CONFIG_CONTROL_DESIGN { 1 0 10303 203 1 1 1 1 }` (AP203). The DATA section contains AP242-only entities (`TESSELLATED_FACE`, `COMPLEX_TRIANGULATED_ SURFACE_SET`). One `ADVANCED_FACE` references `#999` for its `face_geometry` — but `#999` is never defined in the file (forward reference that never resolves). All three are independent failures of the reference-/schema-consistency invariant.

## Reproducer recipe

AP203 schema in header; DATA contains a `TRIANGULATED_FACE`, an `ADVANCED_FACE` whose `face_geometry` slot is `#999`, and no `#999` definition. Other entities resolve normally.

## Expected kernel behavior

schema/entity-set mismatch detected and resolved (load anyway, reject, warn); unresolved `#999` flagged or the containing face dropped.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Cross-oracle: pure-Python Part-21 validator rejects (reject(E_UNRESOLVED_REFS)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_UNRESOLVED_REFS)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `occt_diag_off` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |
| `gmsh_diag` | 1:*** ERR StepReaderData : Unresolved Reference : Fails Cou... |

**Cross-oracle classification**: OCC silently heals — pure-Python rejects this

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator rejects (reject(E_UNRESOLVED_REFS)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
