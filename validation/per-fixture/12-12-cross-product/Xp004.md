# Xp004 — PMI annotation × tessellation-vs-BRep mix in a single AP242 file

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.12 (`12-12-cross-product`)  
**Fixture**: [`step-examples/12-12-cross-product/Xp004.stp`](../../step-examples/12-12-cross-product/Xp004.stp)  

## Sources
- synthesized cross-product (Pass E).

## Description

An AP242 file declares one `tessellated_shape_representation` whose items list mixes a `TESSELLATED_FACE` and an `ADVANCED_FACE` (Ed.1 forbids the latter). A PMI `dimensional_size` is attached via a `geometric_item_specific_usage` to the BRep face, while the saved-view `draughting_model.items` list is missing the required `mapped_item`. The PMI association traverses both geometry kinds and depends on a saved-view linkage that is broken.

## Reproducer recipe

One `ADVANCED_FACE` on a planar surface and one `TRIANGULATED_FACE` referencing a `COORDINATES_LIST`, both placed in a single `tessellated_shape_representation`. A PMI `dimensional_size` (a diameter) cites a `shape_aspect` referencing the BRep face. A `draughting_model` exists with `items` list lacking the required `mapped_item`.

## Expected kernel behavior

Each defect addressed independently — the representation-items invariant checked, the PMI-association resolved (or flagged), the saved-view linkage repaired or warned about.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
