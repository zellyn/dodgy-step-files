# Xp008 — Empty EDGE_LOOP × empty FACE_OUTER_BOUND × otherwise-valid solid

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.12 (`12-12-cross-product`)  
**Fixture**: [`step-examples/12-12-cross-product/Xp008.stp`](../../step-examples/12-12-cross-product/Xp008.stp)  

## Sources
- synthesized cross-product (Pass E).

## Description

A `MANIFOLD_SOLID_BREP` containing 7 faces of a cube. One face's `EDGE_LOOP` has an empty edge aggregate; another face's `face_geometry` slot is `$`. The remaining 5 faces are valid. A robust kernel must skip both malformed faces and decide whether the result (an open shell) can be repaired into a solid.

## Reproducer recipe

6 cube faces but #5 has `EDGE_LOOP('',())` and #6 has `face_geometry = $`.

## Expected kernel behavior

each malformed sub-shape rejected/skipped independently; the kernel decides whether to surface the resulting open shell or attempt repair.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 3) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
