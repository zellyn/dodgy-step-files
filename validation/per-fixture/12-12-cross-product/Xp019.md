# Xp019 — Multiple DATA sections × duplicated entity ID × cross-section reference

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.12 (`12-12-cross-product`)  
**Fixture**: [`step-examples/12-12-cross-product/Xp019.stp`](../../step-examples/12-12-cross-product/Xp019.stp)  

## Sources
- synthesized cross-product (Pass E).

## Description

The file has two `DATA` sections. Both define `#10` (with different bodies). One section references `@otherSection#10` (cross- section reference syntax). The behavior under the spec is ambiguous — multi-DATA is permitted in Edition 3 with explicit naming, but anonymous DATA blocks are not, and duplicated IDs across blocks must be resolved.

## Reproducer recipe

two `DATA;`/`ENDSEC;` blocks, both defining `#10`, with one entity citing `@s2#10`.

## Expected kernel behavior

multi-DATA policy, duplicate-ID resolution, cross-section-ref handling. Each independent.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `warn(W_DUP_REF)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 2:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |
| `occt_diag_off` | 2:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |
| `gmsh_diag` | 2:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
