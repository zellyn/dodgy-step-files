# Xp022 — Time-bomb tolerance × negative-radius torus × cyclic seam edge

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.12 (`12-12-cross-product`)  
**Fixture**: [`step-examples/12-12-cross-product/Xp022.stp`](../../step-examples/12-12-cross-product/Xp022.stp)  

## Sources
- synthesized cross-product (Pass E).

## Description

A `TOROIDAL_SURFACE` has `major_radius = -10.0` (vendor flip) and `minor_radius = 1.0`. A face on it has a seam edge whose 3D-curve identical-vertex check is exactly at the kernel's working precision (the two endpoint coordinates differ by exactly 1e-7). The seam edge appears in the loop twice (once forward, once reverse), forming a cycle. Three time-bomb / vendor / topology defects.

## Reproducer recipe

torus with negative major radius; seam edge with 3D length 1e-7; loop = `(seam_fwd, other, seam_rev, other2)` such that the seam edge appears doubled.

## Expected kernel behavior

vendor-radius policy, tolerance-boundary policy, repeated-seam invariant — each independently.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
