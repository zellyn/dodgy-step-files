# Xp002 — Periodic-surface seam gap × pcurve missing × unit-context mismatch

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.12 (`12-12-cross-product`)  
**Fixture**: [`step-examples/12-12-cross-product/Xp002.stp`](../../step-examples/12-12-cross-product/Xp002.stp)  

## Sources
- synthesized cross-product (Pass E).

## Description

A face on a closed `CYLINDRICAL_SURFACE` is bounded by a wire whose 3D endpoints meet but whose 2D pcurves are missing entirely (`SURFACE_CURVE.associated_geometry` carries no `PCURVE` entry). The containing `GEOMETRIC_REPRESENTATION_CONTEXT` declares `SI_UNIT(.MILLI., .METRE.)` while coordinate magnitudes are 25.4× too large (i.e., authored in inches but tagged as mm). Three independent invariants are violated and three healing strategies (synthesize pcurve, close UV seam, scale consistently with declared unit) interact.

## Reproducer recipe

`CYLINDRICAL_SURFACE` of radius 1.0 on Z-axis, bounded by an `EDGE_LOOP` of two `EDGE_CURVE`s whose 3D endpoints share vertices but whose `SURFACE_CURVE` has empty `associated_geometry`. `GEOMETRIC_REPRESENTATION_CONTEXT` units = mm; coordinates suggest inch authorship (radius emitted as 25.4).

## Expected kernel behavior

Each invariant addressed independently. A kernel may synthesize pcurves, close the UV seam, and elect a unit-policy (honor declaration, infer from magnitudes, or warn).

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `signal(11)` |
| `occt_heal_off` | `signal(11)` |
| `gmsh_autofix_on` | `signal(11)` |
| `gmsh_autofix_off` | `signal(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `process_signal` |

**Cross-oracle classification**: Spec-clean Part-21; OCC crashes (kernel-side bug)

## Tier-3 geometric introspection

- **load**: `None`
- **n_faces**: 0
- **n_edges_total**: None
- **n_vertices_total**: None
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=signal(11)/signal(11) gmsh=signal(11) ifc=schema_n/a` |
