# Gn023 — STEP→BREP silently injects new `B_SPLINE_CURVE_WITH_KNOTS` (analytic `CIRCLE` cylinder cap edges replaced)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2b (`12-2b-nurbs`)  
**Fixture**: [`step-examples/12-2b-nurbs/Gn023.stp`](../../step-examples/12-2b-nurbs/Gn023.stp)  

## Sources
- 18-other-issues.md (I045 — pythonocc-core #661)

## Description

After STEP→BREP via OCC `BRepTools` write, `B_SPLINE_CURVE_WITH_KNOTS` instances appear that were never in the source — analytic `CIRCLE` edges bounding a `CYLINDRICAL_SURFACE` are replaced with NURBS approximations. Source signature: a cylinder authored with two `CIRCLE` cap edges where each `EDGE_CURVE` uses the same `VERTEX_POINT` for start and end (a closed circular edge with one self-paired vertex per cap).

## Reproducer recipe

Read a STEP with `CYLINDRICAL_SURFACE` faces, write BRep, compare edge curve types.

## Expected kernel behavior

preserve analytic curve types through internal serialization; only convert to NURBS on explicit request.

## Notes

**See also**: Gp031.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Gp031](Gp031.md)
