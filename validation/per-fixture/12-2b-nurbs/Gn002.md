# Gn002 — RATIONAL_BSPLINE_CURVE / SURFACE with `NbWeights ≠ NbControlPoints`

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2b (`12-2b-nurbs`)  
**Fixture**: [`step-examples/12-2b-nurbs/Gn002.stp`](../../step-examples/12-2b-nurbs/Gn002.stp)  

## Sources
- 04-occt-translation.md (Q060 `RWStepGeom_RWRationalBSplineCurve.cxx:193-199`)
- 08-occt-gitlog.md (G024, Mantis 0030921, OCCT 3940436/40858cc)

## Description

A `RATIONAL_B_SPLINE_CURVE` (or surface) has a weights array whose length differs from the control-point list length, breaking reader contracts. OCCT's writer historically also emitted mismatched lengths.

## Reproducer recipe

Emit `RATIONAL_B_SPLINE_CURVE(...,(w0,w1,w2))` paired with a 2D/3D pole list of length 4; or surface analogue with rectangular weight grid having a missing row.

## Expected kernel behavior

heal — pad weights with 1.0 / truncate to match pole count; warn. Do not crash. Writers must always emit len(weights) == len(poles).

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`
- disallowed: `crash`

## Notes

Validation observed: silent-empty (the malformed complex-instance entity is dropped at parse without a diagnostic) rather than the cited heal-or-crash. Specific reader-contract code path returns a quiet false at fixture scale; the kernel-mishandling-by-silent-acceptance still demonstrates the defect class. **See also**: Ad059.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **OCC behavior**: silent-empty (the malformed complex-instance entity is dropped at parse without a diagnostic) rather than the cited heal-or-crash. Specific reader-contract code path returns a quiet false at fixture scale; the kernel-mishandling-by-silent-acceptance still demonstrates the defect class.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 3) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Ad059](Ad059.md)
