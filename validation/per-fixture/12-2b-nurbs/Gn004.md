# Gn004 — Complex BSpline surface entity with empty knots/multiplicities

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2b (`12-2b-nurbs`)  
**Fixture**: [`step-examples/12-2b-nurbs/Gn004.stp`](../../step-examples/12-2b-nurbs/Gn004.stp)  

## Sources
- 08-occt-gitlog.md (G022 / G039, Mantis 0029478, OCCT 5dd92c3)

## Description

Complex entity `(BOUNDED_SURFACE() B_SPLINE_SURFACE() B_SPLINE_SURFACE_WITH_KNOTS() … RATIONAL_B_SPLINE_SURFACE())` carries empty `KNOTS()` / `KNOT_MULTIPLICITIES()` lists or empty `name`. Reader null-derefs.

## Reproducer recipe

A complex rational BSpline surface entity with `''` name and empty `KNOTS(())` aggregates.

## Expected kernel behavior

reject the surface and skip the host face; emit a per-entity diagnostic; never crash the read.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `crash`

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21. **OCC behavior**: crashes (signal 11) on the empty knots/multiplicities — exactly the kernel mishandling the catalog above forbids; documented here so kernel authors know this is what OCC currently does.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `signal(11)` |
| `occt_heal_off` | `signal(11)` |
| `gmsh_autofix_on` | `signal(11)` |
| `gmsh_autofix_off` | `signal(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `process_signal` |

**Cross-oracle classification**: Spec-clean Part-21; OCC crashes (kernel-side bug)

## Tier-3 geometric introspection

- **load**: `None`
- **n_faces**: 0
- **n_edges_total**: None
- **n_vertices_total**: None
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=signal(11)/signal(11) gmsh=signal(11) ifc=schema_n/a` |
