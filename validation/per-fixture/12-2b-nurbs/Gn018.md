# Gn018 — Bezier surface and rational Bezier surface unimplemented (BRL-CAD step-g)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2b (`12-2b-nurbs`)  
**Fixture**: [`step-examples/12-2b-nurbs/Gn018.stp`](../../step-examples/12-2b-nurbs/Gn018.stp)  
**Sender**: rare CATIA exports  

## Sources
- 14-brlcad-rhino.md (K006, `OpenNurbsInterfaces.cpp:554, :800`)

## Description

`BEZIER_SURFACE` and `RATIONAL_BEZIER_SURFACE` cause step-g to print `LoadONBrep() not implemented for <entity>` and produce no geometry, although they are algebraically degree-d NURBS patches with knot multiplicity = degree+1.

## Reproducer recipe

STEP entity `#N=BEZIER_SURFACE(...)` or `#N=RATIONAL_BEZIER_SURFACE(...)` referenced from an `ADVANCED_FACE`.

## Expected kernel behavior

convert Bezier→NURBS internally (algebraic identity); never silently drop face geometry due to unsupported but trivially-convertible surface form.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- disallowed: `silent-accept`

## Notes

**OCC behavior**: silently drops the Bezier surface and produces empty geometry without a diagnostic; kernel mishandling — the catalog above forbids silent face-geometry loss.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
