# Gn019 — Degenerate zero-length `B_SPLINE` pcurve (coincident control points / sample count <2)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.2b (`12-2b-nurbs`)  
**Fixture**: [`step-examples/12-2b-nurbs/Gn019.stp`](../../step-examples/12-2b-nurbs/Gn019.stp)  

## Sources
- 14-brlcad-rhino.md (K007, `OpenNurbsInterfaces.cpp:1596`)

## Description

A pcurve on a `CYLINDRICAL_SURFACE` (or other periodic surface) is a degenerate `B_SPLINE_CURVE_WITH_KNOTS` of degree 1 whose two control points are both at the same UV (e.g. both (0,0)) — a zero-length pcurve typically representing a seam. When sampling such a pcurve for an `ON_BrepTrim`, step-g gets fewer than 2 distinct sample points, yielding a degenerate trim. Likely from `seam_curve` or near-singular pcurve evaluation on closed surfaces.

## Reproducer recipe

cylinder/cone with seam edge whose `pcurve_or_surface.pcurve` evaluates to a single point at the apex/seam.

## Expected kernel behavior

enforce minimum sample count >= 2; fall back to endpoint+midpoint+endpoint and refine adaptively.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 15) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
