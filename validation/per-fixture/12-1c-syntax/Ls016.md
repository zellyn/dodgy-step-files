# Ls016 — Double comma / empty parameter slot

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1c (`12-1c-syntax`)  
**Fixture**: [`step-examples/12-1c-syntax/Ls016.stp`](../../step-examples/12-1c-syntax/Ls016.stp)  

## Sources
- T020, A016

## Description

`(a,,b)` leaves an attribute slot empty. Correct null is `$`, derived is `*`. Empty slot is a generator bug. Parsers that map positional slots without re-checking arity may construct the entity with one fewer attribute, leading to uninitialised fields.

## Reproducer recipe

`#1=IFCPERSON($,$,'',,$,$,$,$);` or `#1=CARTESIAN_POINT('',(0.,,1.));`

## Expected kernel behavior

Reject with "expected parameter, found ','"; do not silently treat empty slot as `$`.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`
- disallowed: `silent-accept`

## Notes

**Originally also catalogued as**: Ad016. **OCC behavior**: silently treats the empty slot as `$` (omitted) and proceeds; kernel mishandling — the catalog above forbids silent slot-equivalence.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 3 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 3 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 3 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references


**Originally also catalogued as**: Ad016
