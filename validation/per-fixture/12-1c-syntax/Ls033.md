# Ls033 — Whitespace handling: tab/CR/LF/FF as token separators or ignored chars

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1c (`12-1c-syntax`)  
**Fixture**: [`step-examples/12-1c-syntax/Ls033.stp`](../../step-examples/12-1c-syntax/Ls033.stp)  

## Sources
- T031, T032, T037, N030, N031

## Description

ISO 10303-21 §5.2 says LF, CR, TAB, FF are ignored characters; §5.6 says only SPACE, comments, and print directives separate tokens. Strict reading: `#1\t=\tCARTESIAN_POINT(...)` after tab-removal becomes `#1=CARTESIAN_POINT(...)`, but `#1<TAB>2` would become `#12`. Real-world parsers tolerate any ASCII whitespace as a separator. Defects: split keyword `FILE _SCHEMA`, internal whitespace inside `#NNN` references, files with `\r`-only line endings, `\f` between attributes, long-line wrapping inside an instance.

## Reproducer recipe

`FILE _SCHEMA(('AUTOMOTIVE_DESIGN'));`, `# 12 = IFCPERSON(...);`, file with CR-only line endings, `#1=IFCPOINT(\F\(0.,0.,0.));`

## Expected kernel behavior

Treat any of `\t \f\r\n` as whitespace separator (matches user expectation); preserve line/column counting across line-ending conventions; warn on TAB inside an identifier; accept print-control directives `\F\` and `\N\` as zero-width separators where token separator is allowed; reject embedded whitespace inside `#N` (between `#` and digits).

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**See also**: Le028.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Le028](Le028.md)
