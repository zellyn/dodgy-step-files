# Ls029 — Comment terminator absent — runs through EOF

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1c (`12-1c-syntax`)  
**Fixture**: [`step-examples/12-1c-syntax/Ls029.stp`](../../step-examples/12-1c-syntax/Ls029.stp)  

## Sources
- A007, N028

## Description

`/* ... ` opens a comment that is never closed; lexer scans past EOF if loop only terminates on `*/`. One-byte read past mmap'd file boundary; weaponizable for info leak.

## Reproducer recipe

`ISO-10303-21; HEADER; ... ENDSEC; DATA; /* unterminated`

## Expected kernel behavior

Lexer must check `pos < end` on every advance and emit `E_UNTERMINATED_COMMENT` at EOF.

## Notes

Cross-oracle: pure-Python Part-21 validator rejects (reject(E_NO_CLOSE)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_NO_CLOSE)` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: OCC silently heals — pure-Python rejects this

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator rejects (reject(E_NO_CLOSE)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
