# In012 — Transfer watchdog fires: dependency cycle in entity references

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1c (`12-1c-syntax`)  
**Fixture**: [`step-examples/12-1c-syntax/In012.stp`](../../step-examples/12-1c-syntax/In012.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT Transfer_TransferDeadLoop.hxx (uncovered class evidence)

## Description

The transfer engine detects a dependency cycle (entity A references B references C references A) and raises a dead-loop exception. Bug-reporter language: "STEP file has cycle", "circular reference", "kernel detects loop".

## Reproducer recipe

A `MAPPED_ITEM` whose `mapping_source` resolves through a chain that eventually re-references the original entity.

## Expected kernel behavior

Detect cycle; reject the file; emit the cycle path in the diagnostic.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Synonyms: "circular dependency", "STEP cycle". Provenance tier: runtime-only — the bytes can encode a reference cycle, but the catalogued defect is the kernel's transfer-time watchdog firing (or failing to fire). Whether the kernel produces the dead-loop diagnostic vs. recursing forever is a runtime behavior, not a property of the file content. A behavioral test against a transfer engine is the appropriate venue.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `runtime-only` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
