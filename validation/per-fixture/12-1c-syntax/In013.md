# In013 — Transfer Status remains Void after Defined was expected: unknown entity type in DATA section silently skipped (e.g., made-up future-schema entity)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1c (`12-1c-syntax`)  
**Fixture**: [`step-examples/12-1c-syntax/In013.stp`](../../step-examples/12-1c-syntax/In013.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT Transfer_StatusResult Void/Defined transitions (uncovered class evidence)

## Description

A transfer process expected to produce a Defined result for an entity, but the result remains Void because no actor handled the entity type. The transfer log doesn't flag the void as an error. Typical fixture: the DATA section contains an entity of a made-up or future type (e.g., `HYPOTHETICAL_AP242_ED2_FUTURE_ENTITY`) that no standard schema defines and no kernel actor recognizes — the reader silently skips it. Bug-reporter language: "entity type not transferred", "kernel skipped entity silently", "unknown entity type ignored".

## Reproducer recipe

An entity of a type the actor pool doesn't recognize (e.g., a made-up `HYPOTHETICAL_AP242_ED2_FUTURE_ENTITY` or an AP242 ed.2 entity that the reader wasn't compiled with).

## Expected kernel behavior

Emit "no actor for entity type X" warning; track unrecognized types in a per-file summary.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `warn-and-proceed`

## Notes

Synonyms: "STEP entity skipped". Provenance tier: runtime-only — bytes alone cannot demonstrate this defect; the symptom is the transfer-status field staying Void with no diagnostic emitted. The static fixture encodes only the trigger (an unknown entity type); observing the silent-Void requires a kernel runtime trace.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Provenance tier**: `runtime-only` (defect not fully embodied in fixture bytes)

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
