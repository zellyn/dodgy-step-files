# Ls031 — Forward-slash inside `/* ... */` comment treated as nested-comment open

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1c (`12-1c-syntax`)  
**Fixture**: [`step-examples/12-1c-syntax/Ls031.stp`](../../step-examples/12-1c-syntax/Ls031.stp)  

## Sources
- T003, N029

## Description

ISO 10303-21 comments do not nest. A naive scanner that re-fires on inner `/*` (or fails to require both `*` and `/` consecutively for close) desynchronizes when comments contain Windows paths or URLs.

## Reproducer recipe

`/* path C:/foo/bar */` and `/* outer /* inner */ should still be comment */`

## Expected kernel behavior

Only `*/` ends a comment; `/*` within a comment is literal text. After the first `*/` everything else is code.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
