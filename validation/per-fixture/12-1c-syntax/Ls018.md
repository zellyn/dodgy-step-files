# Ls018 — Two consecutive semicolons (`;;`) after instance

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.1c (`12-1c-syntax`)  
**Fixture**: [`step-examples/12-1c-syntax/Ls018.stp`](../../step-examples/12-1c-syntax/Ls018.stp)  

## Sources
- T019, A018

## Description

Trailing extra `;` confuses parsers expecting another `#` definition or `ENDSEC`. Second `;` may be tokenized as start of an empty entity, leading to NULL entity-class lookup.

## Reproducer recipe

`#20=IFCPROJECT('xyz',#5,'',$,$,$,$,(#11),#19);;`

## Expected kernel behavior

Reject by default; lenient mode silently drop empty statements with `W_EMPTY_STATEMENT` diagnostic; never construct an empty/NULL entity.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Cross-oracle: pure-Python Part-21 validator rejects (reject(E_UNRESOLVED_REFS)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `reject(E_UNRESOLVED_REFS)` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 2:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |
| `occt_diag_off` | 2:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |
| `gmsh_diag` | 2:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |

**Cross-oracle classification**: OCC silently heals — pure-Python rejects this

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator rejects (reject(E_UNRESOLVED_REFS)); OCCT silently accepts (load is `empty`). OCC auto-heals a spec-level violation.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
