# Twi017 — Closed 3D curve with two distinct VERTEX_POINTs (single-edge closed)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi017.stp`](../../step-examples/12-3b-wires/Twi017.stp)  

## Sources
- 04-occt-translation.md Q007

## Description

An EDGE_CURVE whose 3D curve is closed (full circle/ellipse) but cites two distinct VERTEX_POINTs as endpoints; per STEP/OCCT, a closed curve must reuse the same vertex at both ends.

## Reproducer recipe

`EDGE_CURVE('',#v1,#v2,#circle_geom,.T.);` with `#circle_geom` a full `CIRCLE` and `#v1`, `#v2` distinct entities.

## Expected kernel behavior

heal — translator reuses the first vertex on both ends.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

The translator's edge-transfer pass must detect the closed-curve case and reuse the same vertex at both ends, regardless of how many distinct vertex entities the file references. **See also**: Twi002, Twi018.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(3)` |
| `gmsh_autofix_off` | `shape(3)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 1
- **n_vertices_total**: 2
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 78.53981633974483 | 50000001.00000001 | 1 |

## Tier-3 assertions (machine-checked)

- `n_edges_total == 1`
- `n_vertices_total == 2`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(3) ifc=schema_n/a` |

## Cross-references

**See also**: [Twi002](Twi002.md), [Twi018](Twi018.md)
