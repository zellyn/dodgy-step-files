# Twi034 — `EDGE_LOOP` missing the closing edge (open near-closed wire treated as intentionally open)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi034.stp`](../../step-examples/12-3b-wires/Twi034.stp)  

## Sources
- 05-occt-shapefix.md F074 (ShapeFix_Wire.hxx:184-186 ClosedWireMode)

## Description

An `EDGE_LOOP` is missing the edge that would close it back to its starting vertex — for example three `ORIENTED_EDGE`s forming an L-shape (bottom, right, top, with no closing left edge), so the wire never returns to its start vertex. The fixer's default `ClosedWireMode` mis-matches the caller's expectation: a closed wire missing one edge gets treated as intentionally open and never repaired.

## Reproducer recipe

STEP face whose outer wire has a small gap > tol; pass through `ShapeFix_Wire` with default `ClosedWireMode=Standard_False`.

## Expected kernel behavior

callers explicitly set `ClosedWireMode` per topological context; healer respects it.

## Notes

**See also**: Twi003.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 3
- **n_vertices_total**: 6
- **brepcheck.valid**: `False`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 24.999999999999996 | 25000001.000000004 | 3 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 3`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 14) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=reject ifc=schema_n/a` |

## Cross-references

**See also**: [Twi003](Twi003.md)
