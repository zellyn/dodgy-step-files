# Twi049 — Wire on a face self-intersects in the host surface's parameter space

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi049.stp`](../../step-examples/12-3b-wires/Twi049.stp)  

## Sources
- OCCT `ShapeFix_IntersectionTool::FixSelfIntersectWire` (`ShapeFix_IntersectionTool.hxx:69`)

## Description

A single closed wire's edges, when projected to the host face's UV parameter space, cross each other (figure-eight shape). The wire is topologically closed but geometrically self-intersecting; it cannot bound a simple region.

## Reproducer recipe

An EDGE_LOOP of four edges forming a figure-eight: edges from `(0,0)→(10,10)`, `(10,10)→(10,0)`, `(10,0)→(0,10)`, `(0,10)→(0,0)`. The diagonals cross at `(5,5)`.

## Expected kernel behavior

detect the self-intersection point(s) in UV; either split the wire at the intersection into two simple loops, remove the inner sub-loop, or reject the face as malformed. A diagnostic citing the offending edge pair must be available.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

3D self-intersection (independent of any host surface) is a separate class. **See also**: existing Twi entries on self-intersection. **OCC behavior**: the validity-checker does not flag the UV-space figure-eight wire self-intersection; tier-3 valid-flag should be ignored for this entry. The reader most likely silently fixes or ignores the self-intersection on import.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(13)` |
| `gmsh_autofix_off` | `shape(13)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 6
- **n_vertices_total**: 12
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 25.0 | 50000000.99999999 | 3 |
| 1 | plane | 24.999999999999996 | 50000000.99999999 | 3 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 6`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(13) ifc=schema_n/a` |
