# Twi050 — Two distinct wires share a VERTEX_POINT instance that needs to be split

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi050.stp`](../../step-examples/12-3b-wires/Twi050.stp)  

## Sources
- OCCT `ShapeFix_SplitCommonVertex::Perform` (`ShapeFix_SplitCommonVertex.hxx:37`)

## Description

Two unrelated wires (in two different faces, or in two unrelated regions of one face) accidentally reference the same VERTEX_POINT instance because the sender deduplicated vertices too aggressively. Topology operators that assume vertices are owned by exactly one connected component misbehave.

## Reproducer recipe

Two faces whose outer wires both list a VERTEX_POINT at `(0,0,0)` via the same instance reference.

## Expected kernel behavior

detect vertex over-sharing; split the common vertex into independent copies (one per wire) so each wire's connectivity is explicit. Optional: reject as malformed if the share crosses topological boundaries that the kernel cannot resolve.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Search terms: "shared vertex", "vertex over-sharing", "common vertex split".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(13)` |
| `gmsh_autofix_off` | `shape(13)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 6
- **n_vertices_total**: 12
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 24.999999999999996 | 50000001.00000001 | 3 |
| 1 | plane | 25.000000000000007 | 50000001.00000001 | 3 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 6`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(13) ifc=schema_n/a` |
