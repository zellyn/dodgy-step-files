# Twi004 — ORIENTED_EDGE wrapping another ORIENTED_EDGE

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi004.stp`](../../step-examples/12-3b-wires/Twi004.stp)  

## Sources
- 04-occt-translation.md Q004
- 08-occt-gitlog.md G012 (Mantis 0029979)

## Description

An `ORIENTED_EDGE` whose `edge_element` is itself an `ORIENTED_EDGE` instead of an `EDGE_CURVE`. Technically illegal but observed in the wild.

## Reproducer recipe

`#1=ORIENTED_EDGE('',*,*,#2,.T.); #2=ORIENTED_EDGE(...,#3,.T.); #3=EDGE_CURVE(...);`

## Expected kernel behavior

heal-by-traversal — chase ORIENTED_EDGE chains until an EDGE_CURVE is found, composing the orientation flags; never crash.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`
- disallowed: `crash`

## Notes

The translator's edge-loop transfer pass must chase wrapper chains until it finds the underlying `EDGE_CURVE`, composing orientation flags as it descends. Validation observed: silent-empty rather than a crash. Without a PRODUCT chain the transfer never invokes the edge-loop translator; with a wrap chain the receiver silently heals (chases the wrapper). The kernel-mishandling-by-silent-acceptance still demonstrates the defect class — the bare reader's malformed-reference handling is not exercised at fixture scale. **See also**: Twi005, Twi006.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(3)` |
| `gmsh_autofix_off` | `shape(3)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 1
- **n_vertices_total**: 2
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 3.1415926535897922 | 10000001.000000002 | 1 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 1`

## Provenance / kernel-behavior annotations

- **OCC behavior**: silent-empty rather than a crash. Without a PRODUCT chain the transfer never invokes the edge-loop translator; with a wrap chain the receiver silently heals (chases the wrapper). The kernel-mishandling-by-silent-acceptance still demonstrates the defect class — the bare reader's malformed-reference handling is not exercised at fixture scale.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(3) ifc=schema_n/a` |

## Cross-references

**See also**: [Twi005](Twi005.md), [Twi006](Twi006.md)
