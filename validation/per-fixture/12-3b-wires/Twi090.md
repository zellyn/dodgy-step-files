# Twi090 — Cached EDGE_LOOP closed-flag not refreshed after one ORIENTED_EDGE replaced by two collinear halves at a midpoint vertex

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi090.stp`](../../step-examples/12-3b-wires/Twi090.stp)  

## Sources
- OCCT MANTIS#0030927

## Description

A wire's `closed` flag was computed at construction. Subsequent healing replaces one of the wire's edges (e.g., to fix a SameRange flag). The new edge connects the same vertices as the old one, so the wire is still closed — but the flag is not recomputed and propagates `closed=false` to downstream consumers (face validity check, STEP exporter).

## Reproducer recipe

A four-edge rectangular wire where one straight edge has been replaced by a polyline of two collinear edges joined at a vertex in the middle of the original edge. Wire is still closed; cached `closed_flag` says open.

## Expected kernel behavior

any edge insertion / replacement on a wire invalidates cached topological flags (closed, simple, ordered); recompute on demand or eagerly.

## Notes

**See also**: Twi053, Twi064.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 18) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |

## Cross-references

**See also**: [Twi053](Twi053.md), [Twi064](Twi064.md)
