# Twi092 — Wire with degenerated edge dropped silently on export

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi092.stp`](../../step-examples/12-3b-wires/Twi092.stp)  

## Sources
- OCCT MANTIS#0025843

## Description

A face has a wire containing a degenerate edge (a seam pole on a cone or a degenerate edge at the apex of a sphere wedge). The STEP exporter, given a degenerated edge, drops it from the wire entirely rather than emitting it as `EDGE_CURVE` with a degenerated marker. The re-imported wire is incomplete and the face fails validity.

## Reproducer recipe

Cone face whose apex is reached by a degenerate edge (zero-length segment between two coincident vertex points at the apex). Exporter writes the wire with three edges instead of four.

## Expected kernel behavior

degenerate edges are part of the topology and must be preserved on export, marked via `EDGE_CURVE` of zero parametric length or via dedicated degenerated-edge representation.

## Notes

**See also**: Twi053.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(8)` |
| `gmsh_autofix_off` | `shape(8)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `False`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | cone | 8.719671245021581e-17 | 2500000.999234221 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].surface_type == "cone"`
- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-10` (rank 7) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(8) ifc=schema_n/a` |

## Cross-references

**See also**: [Twi053](Twi053.md)
