# Twi040 — `EDGE_CURVE` `LINE` slightly off `CYLINDRICAL_SURFACE` (~`Precision::Confusion()` deviation): `Could not fix wire in surface` regression after OCCT version bump

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi040.stp`](../../step-examples/12-3b-wires/Twi040.stp)  
**Sender**: any STEP with wires whose pcurve / 3D-curve disagree by `Precision::Confusion()`  

## Sources
- 22-niche-forums.md Y017 (gmsh #3090)

## Description

An `EDGE_CURVE` `LINE` on a `CYLINDRICAL_SURFACE` whose start point is offset from the surface by `Precision::Confusion()`-scale (e.g. line starting at (10.0000001, 0, 0) on a R=10 cylinder while the endpoint `VERTEX_POINT`s lie exactly on R=10), so the edge does not quite lie on the host surface. Until gmsh 4.11 this logged a "Recomputing incorrect OpenCASCADE wire" warning and fixed the wire; from 4.12 the same input throws fatal exceptions on identical surfaces — a tolerance-handling regression.

## Reproducer recipe

STEP with `EDGE_LOOP` whose `EDGE_CURVE` start/end points disagree by ~`Precision::Confusion()` from the surface parametric trace.

## Expected kernel behavior

heal — degrade to warning + accept the file with a fudged wire when fix fails, matching pre-4.12 behavior.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal, warn-and-proceed`

## Notes

**See also**: Gs009.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 1
- **n_vertices_total**: 2
- **brepcheck.valid**: `False`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | cylinder | 0.0 | 3.9999998800000043 | 1 |

## Tier-3 assertions (machine-checked)

- `face[0].surface_type == "cylinder"`
- `n_edges_total >= 1`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=reject ifc=schema_n/a` |

## Cross-references

**See also**: [Gs009](Gs009.md)
