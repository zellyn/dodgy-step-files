# Twi064 — Wire-analysis pipeline collects all sub-check status flags without short-circuit (e.g., inner FACE_BOUND triangle with near-coincident VERTEX_POINTs and EDGE_LOOP ORIENTED_EDGEs out of head-to-tail order)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi064.stp`](../../step-examples/12-3b-wires/Twi064.stp)  

## Sources
- OCCT `ShapeAnalysis_Wire::Perform` (`ShapeAnalysis_Wire.hxx:176`)

## Description

A diagnostic-only pass over a wire (no healing). The pipeline must run CheckOrder, CheckSmall, CheckConnected, CheckEdgeCurves, CheckDegenerated, CheckSelfIntersection, CheckLacking, CheckClosed and aggregate all status flags — short-circuiting on the first defect leaves the user without a complete picture. Typical fixture: an inner FACE_BOUND triangle with three near-coincident VERTEX_POINTs and an EDGE_LOOP whose ORIENTED_EDGEs are listed out of head-to-tail order — multiple defects in one EDGE_LOOP.

## Reproducer recipe

An EDGE_LOOP that fails at least three sub-checks: out-of-order ORIENTED_EDGEs, sub-tolerance edges between near-coincident vertices, not closed.

## Expected kernel behavior

run each sub-check independently of the others' outcomes, return a status object with one entry per check. Callers can later filter by severity.

## Notes

Umbrella for diagnostic-only pipeline; separate from Twi051 which is the healer pipeline.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
