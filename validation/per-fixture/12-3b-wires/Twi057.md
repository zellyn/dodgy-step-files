# Twi057 — Two edges of one face are confused along their full length (strip pair)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi057.stp`](../../step-examples/12-3b-wires/Twi057.stp)  

## Sources
- OCCT `ShapeAnalysis_CheckSmallFace::CheckStripEdges` (`ShapeAnalysis_CheckSmallFace.hxx:85`)

## Description

Two long edges of a wire are within tolerance distance of each other along the full parameter range of both. The pair-of-edges check operates without knowing the host face — it is purely about whether two edges describe approximately the same curve.

## Reproducer recipe

A wire with two parallel edges from `(0,0,0)→(100,0,0)` and `(0,1e-6,0)→(100,1e-6,0)`; the gap is 1e-6 over a 100-unit length.

## Expected kernel behavior

detect the strip pair and report which two edges form it, with the maximum gap distance; downstream healing can merge the edges. Detector must be independent of host face (so pairs across faces are also caught).

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

Related to Tfa042 (strip face) but at edge-pair granularity.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 1
- **n_vertices_total**: 2
- **brepcheck.valid**: `False`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 0.0 | 500000000.99999994 | 1 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 1`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=reject ifc=schema_n/a` |
