# Twi082 — Edge `same_range` flag claims parametric agreement that does not hold

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi082.stp`](../../step-examples/12-3b-wires/Twi082.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT BRepCheck_Edge.cxx:321, 360 (InvalidSameRangeFlag) (uncovered class evidence)

## Description

An edge declares its 3D-curve parameter range and pcurve parameter range to be identical (the `same_range` flag is set), but evaluating both at the boundary yields parameter pairs that differ by more than the edge's tolerance. Bug-reporter language: "parameter range mismatch", "same_range lies", "edge ranges disagree".

## Reproducer recipe

Build an edge whose 3D `LINE` runs `t∈[0,10]` while the pcurve in UV runs `u∈[0,5]` (different parameterizations). Set the `same_range` flag true anyway.

## Expected kernel behavior

Detect by comparing ranges; reset the flag, or recompute one parameterization to match.

## Notes

Bug-reporter synonym: "edge parameterization mismatch".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 3) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
