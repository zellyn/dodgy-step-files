# Twi048 — Vertex tolerance smaller than the edge endpoint discrepancy it must absorb

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi048.stp`](../../step-examples/12-3b-wires/Twi048.stp)  

## Sources
- OCCT `ShapeFix_Edge::FixVertexTolerance` (`ShapeFix_Edge.hxx:131`)

## Description

A VERTEX_POINT carries (or inherits) a tolerance of 1e-6, but the edge curve's evaluation at the endpoint is 5e-3 away from the vertex. The vertex's tolerance ball does not reach the curve, so any "is this point on this edge?" query fails. Often produced by senders that compute vertex tolerance globally rather than per-incident-edge.

## Reproducer recipe

Square face where one corner vertex is positioned at `(10.005, 10, 0)` while the adjoining edges' 3D LINE curves run between `(10,0)` and `(10,10)`; global UNCERTAINTY_MEASURE_WITH_UNIT declared at 1e-6 mm.

## Expected kernel behavior

detect the tolerance shortfall; either inflate the vertex tolerance to cover the discrepancy, snap the vertex to the curve, or reject the edge as not same-parameter. Excessive tolerance inflation should be flagged as a quality regression.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Cascading inflation is a known anti-pattern (see N008).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 3 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 3 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 3 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
