# Twi093 — Synthesised seam edge is inserted through existing wire path on cylinder

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi093.stp`](../../step-examples/12-3b-wires/Twi093.stp)  

## Sources
- OCCT MANTIS#0023451

## Description

A face on a closed surface (cylinder, torus) has a wire that crosses the seam but does not include the seam edge. A seam-reconstruction pass synthesises the missing seam edge — but inserts it through the existing wire path, producing a wire that intersects itself at the synthesised seam.

## Reproducer recipe

Cylindrical face, single closed wire that runs from `(U=0, V=0)` to `(U=2π, V=h)` directly. Healer inserts a seam edge at `U=0, V=[0,h]` without reordering the existing wire, creating a figure-eight.

## Expected kernel behavior

seam reconstruction must check whether the inserted seam intersects the existing wire and, if so, split the existing wire at the seam crossing before inserting.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: Twi049, Tsh047.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(3)` |
| `gmsh_autofix_off` | `shape(3)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 1
- **n_vertices_total**: 2
- **brepcheck.valid**: `False`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | cylinder | 6.283185307179583 | 1.0000000000000002 | 1 |

## Tier-3 assertions (machine-checked)

- `face[0].surface_type == "cylinder"`
- `n_edges_total >= 1`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-10` (rank 7) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(3) ifc=schema_n/a` |

## Cross-references

**See also**: [Twi049](Twi049.md), [Tsh047](Tsh047.md)
