# Twi076 — EDGE_LOOP self-intersects (figure-eight): edges crisscross inside the wire (e.g., diagonal-like edges between rectangle corners)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi076.stp`](../../step-examples/12-3b-wires/Twi076.stp)  

## Sources
- OCCT `ShapeAnalysis_Wire::CheckLoop` (`ShapeAnalysis_Wire.hxx:512`)

## Description

A vertex referenced by more than two oriented-edges in a single wire creates a figure-eight or pinched topology. The wire is technically traversable but cannot bound a face without splitting at the offending vertex.

## Reproducer recipe

As Twi049 (self-intersecting figure-eight) but framed as a vertex over-visit problem.

## Expected kernel behavior

identify "loop vertices" (those with degree > 2 in the wire's edge graph); split the wire at those vertices into multiple simple wires, or reject as malformed.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Related to Twi049 but vertex-centric rather than edge-intersection-centric. **OCC behavior**: the validity-checker does not flag a wire vertex with edge-graph degree > 2; tier-3 valid-flag should be ignored for this entry. The reader most likely silently fixes or ignores the figure-eight wire on import.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(13)` |
| `gmsh_autofix_off` | `shape(13)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 6
- **n_vertices_total**: 12
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 25.0 | 50000000.99999999 | 3 |
| 1 | plane | 24.999999999999996 | 50000000.99999999 | 3 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 6`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 32) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(13) ifc=schema_n/a` |
