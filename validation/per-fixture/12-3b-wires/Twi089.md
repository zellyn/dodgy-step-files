# Twi089 — Two collinear edges meeting at a degree-2 vertex should be fused into one edge

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi089.stp`](../../step-examples/12-3b-wires/Twi089.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT BRepLib::FuseEdges (uncovered class evidence)

## Description

Two edges with collinear (or co-curve) geometry share a vertex of degree 2, and that vertex is not topologically necessary (no other face uses it). The expected post-condition: fuse the two edges into one, removing the spurious vertex. Bug-reporter language: "extra vertex on edge", "edge unnecessarily split", "kernel didn't merge collinear edges".

## Reproducer recipe

A wire of two `LINE`-edges (0,0,0)→(1,0,0) and (1,0,0)→(2,0,0) joined at `#V` (1,0,0), where `#V` is not used by any other wire/face.

## Expected kernel behavior

Fuse the two edges; or leave as-is if downstream needs the split.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

Synonyms: "spurious vertex", "non-essential vertex split".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
