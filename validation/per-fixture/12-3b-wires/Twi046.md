# Twi046 — Edge 3D curve evaluation does not match its declared end vertices

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi046.stp`](../../step-examples/12-3b-wires/Twi046.stp)  

## Sources
- OCCT `ShapeFix_Edge::FixRemoveCurve3d` (`ShapeFix_Edge.hxx:75`)

## Description

An EDGE_CURVE's 3D representation, evaluated at the edge's parameter range, returns points that are not the same as the edge's start/end VERTEX_POINTs — the curve goes somewhere else entirely. Senders that regenerated the curve without coordinating with vertices, or that copied the wrong curve handle, produce this.

## Reproducer recipe

An EDGE_CURVE whose vertices are at `(0,0,0)` and `(2,0,0)` but whose 3D curve is a LINE in the +Z direction starting at `(0,0,0)` — the curve ends at `(0,0,1)`, far from the second vertex.

## Expected kernel behavior

detect the curve-vertex mismatch; choose to drop the bad 3D curve (and either rebuild it from vertices or rely on a pcurve), snap vertices to the curve, or reject the edge as inconsistent. The chosen response should be reported.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Often paired with Twi047 (no 3D curve at all) when senders strip the bad curve and forget to add a replacement.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
