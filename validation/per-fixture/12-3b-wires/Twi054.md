# Twi054 — EDGE_LOOP wire has a notch: short detour edge between two near-collinear long edges (e.g., 5-edge rectangle wire with one VERTEX_POINT offset ~1e-3 forming a sub-mm triangular bump)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi054.stp`](../../step-examples/12-3b-wires/Twi054.stp)  

## Sources
- OCCT `ShapeFix_Wire::FixNotchedEdges` (`ShapeFix_Wire.hxx:400`)

## Description

A wire contains three consecutive edges where the middle is a short edge that forms a sharp triangular notch between two long edges that should otherwise be near-collinear. The notch is not a feature; it's the residue of a faulty Boolean or a sub-feature that should have been absorbed.

## Reproducer recipe

A five-edge wire forming a unit rectangle with one edge replaced by a 3-edge "bump out" of width 0.001 — the middle edge of the bump is the notch.

## Expected kernel behavior

detect notch-shaped triplets; flatten by removing the short edge and extending the two long neighbors to meet, or leave the notch and report it as a quality metric. Threshold for "short" must be kernel-policy.

## Notes

Search terms: "notch", "spike", "snag", "tail" (Twi077 covers a different sub-case).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(11)` |
| `gmsh_autofix_off` | `shape(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 5
- **n_vertices_total**: 10
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 5.000500950094991 | 25005001.000000007 | 5 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 5`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 41) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(11) ifc=schema_n/a` |
