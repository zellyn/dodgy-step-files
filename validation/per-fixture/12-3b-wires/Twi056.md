# Twi056 — Wire vertices that should share a coordinate differ by sub-precision amounts

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi056.stp`](../../step-examples/12-3b-wires/Twi056.stp)  

## Sources
- OCCT `ShapeFix_WireVertex::Fix` (`ShapeFix_WireVertex.hxx:71`)

## Description

Two adjacent edges of a wire reference distinct VERTEX_POINTs whose 3D coordinates agree only within 1e-7 (or some other sub-precision distance). The edges should share a single vertex; they appear independent because the sender forgot to deduplicate.

## Reproducer recipe

A square wire where the "first" vertex `(0,0,0)` and the "last" vertex `(1e-7, 0, 0)` are intended to be the same but are emitted as two distinct VERTEX_POINTs. The closing edge is omitted, expecting the consumer to merge.

## Expected kernel behavior

detect near-coincident vertex pairs; merge them and re-stitch incident edges, or reject the wire as not-actually-closed. The merge tolerance is kernel policy.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Related to Twi050 (vertex over-sharing — opposite defect). Search terms: "duplicate vertex", "vertices not merged", "near-coincident vertex".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
