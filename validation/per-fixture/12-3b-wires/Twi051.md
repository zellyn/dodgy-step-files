# Twi051 — Wire-level healing pipeline must order reorder/connect/lacking/closed fixes (e.g., inner FACE_BOUND triangle with near-coincident vertices and EDGE_LOOP ORIENTED_EDGEs out of head-to-tail order)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi051.stp`](../../step-examples/12-3b-wires/Twi051.stp)  

## Sources
- OCCT `ShapeFix_Wire::Perform` (`ShapeFix_Wire.hxx:271`)

## Description

A wire simultaneously has out-of-order edges, a 3D gap between two of its edges, and a missing edge between two non-coincident vertices. Fixing in the wrong order (e.g., reorder before connect) produces a different end-state than fixing in the right order. The wire-level orchestrator must apply fixes in a sequence that converges on a closed wire.

## Reproducer recipe

A face with one outer wire whose edges are listed in scrambled order plus an inner wire with a sub-tolerance hole and disordered edges.

## Expected kernel behavior

an iterative healing pipeline that begins with reorder, then connection, then degenerate-edge handling, then self-intersection, then lacking-edge insertion, then closure verification; each fix re-evaluates the wire's status before the next.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

Umbrella entry over the per-defect wire-healing rows (reorder, small-edge removal, connection, edge-curve fix, degenerate-edge insertion, self-intersection resolution, lacking-edge insertion, closure verification). This row captures the pipeline contract that ties them together.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 18) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
