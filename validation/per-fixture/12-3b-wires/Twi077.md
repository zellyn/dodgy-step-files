# Twi077 — Wire tail / sliver: two adjacent EDGE_CURVEs form a sharp 180° fold over sub-tolerance width (one edge nearly retraces the other in opposite direction)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi077.stp`](../../step-examples/12-3b-wires/Twi077.stp)  

## Sources
- OCCT `ShapeAnalysis_Wire::CheckTail` (`ShapeAnalysis_Wire.hxx:519`)

## Description

Two consecutive edges of a wire fold back on themselves at near-180° over a width below tolerance. The "tail" is a degenerate geometric feature (a stick poking into the face). The check identifies the four sub-edges produced when the tail's two long sides are split at their crossing point.

## Reproducer recipe

A wire with two long edges meeting at a sharp angle, the second edge nearly retracing the first within 1e-6.

## Expected kernel behavior

detect the tail, report the four sub-edges, and either remove the tail (collapse to a single corner) or reject.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Distinct from a notch (Twi054): a tail is a fold-back; a notch is a small detour.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `beyond-10` (rank 27) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
