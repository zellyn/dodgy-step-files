# Twi038 — Scrambled `EDGE_LOOP` requiring only reorder: wire-only-reorder healing doesn't update the parent face

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi038.stp`](../../step-examples/12-3b-wires/Twi038.stp)  

## Sources
- 08-occt-gitlog.md G104 (sub-class of G080)
- 05-occt-shapefix.md F001

## Description

An `EDGE_LOOP` whose `edge_list` is ordered out of cyclic head-to-tail (e.g. (#71, #70, #73, #72) — square corners visited in scrambled order so consecutive `ORIENTED_EDGE`s do not share endpoints) needs only a reorder, no edge replacement. When a wire-healing pass performs only a wire reorder and no other modification, the parent face is not replaced with the new (reordered) wire. The pass reports success, but the output shape still references the unhealed wire — the fix is silently lost.

## Reproducer recipe

BRep face requiring only wire-reorder; run a healing pass; observe original unfixed face still present in the output.

## Expected kernel behavior

a healing pass that produces a modified sub-shape must propagate the replacement up the parent chain unconditionally — any sub-fix that succeeds must result in the parent referring to the new sub-shape.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: Twi003.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 23) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |

## Cross-references

**See also**: [Twi003](Twi003.md)
