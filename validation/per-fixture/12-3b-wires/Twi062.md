# Twi062 — 3D curve and pcurve traverse the same edge in opposite parameter senses

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi062.stp`](../../step-examples/12-3b-wires/Twi062.stp)  

## Sources
- OCCT `ShapeAnalysis_Edge::CheckCurve3dWithPCurve` (`ShapeAnalysis_Edge.hxx:193`)

## Description

At the edge's first parameter `t=0`, the 3D curve evaluates to the start vertex but the pcurve, lifted through the host surface, evaluates to the end vertex (and vice versa at `t=1`). The two representations disagree about which way the edge runs. Reprojection-based imports often produce this when the projector picks the opposite sweep.

## Reproducer recipe

A square face where one edge's pcurve runs `(10,0)→(10,10)` in UV but the corresponding 3D curve was emitted with reversed `LINE` direction.

## Expected kernel behavior

detect the directional mismatch; flip the pcurve sense (or the 3D curve's parameter direction), re-validate same-parameter, and continue. Alternative: reject as malformed.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

FixReversed2d (existing entry) is the corresponding healer.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
