# Twi063 — Two edges describe overlapping segments of the same curve

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi063.stp`](../../step-examples/12-3b-wires/Twi063.stp)  

## Sources
- OCCT `ShapeAnalysis_Edge::CheckOverlapping` (`ShapeAnalysis_Edge.hxx:237`)

## Description

Two distinct EDGE_CURVEs share a finite-length segment of the same underlying curve — distance between them stays below tolerance over a non-zero parameter interval. The edges are not duplicates (their endpoints differ), but their interiors collide.

## Reproducer recipe

Edge A from `(0,0,0)→(5,0,0)` along the X-axis; edge B from `(2,0,0)→(7,0,0)` along the same axis. Overlap span: `[2,5]`.

## Expected kernel behavior

report the overlap interval and the offending edge pair; either trim one edge, merge them, or reject. The "domain distance" parameter lets the caller filter overlaps below a length threshold.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Distinct from coincident edges (which would overlap fully).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 1
- **n_vertices_total**: 2
- **brepcheck.valid**: `False`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 0.0 | 25000001.000000004 | 1 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 1`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=reject ifc=schema_n/a` |
