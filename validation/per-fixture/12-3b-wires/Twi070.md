# Twi070 — Curve gap within a single edge: 3D curve and pcurve drift mid-edge

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi070.stp`](../../step-examples/12-3b-wires/Twi070.stp)  

## Sources
- OCCT `ShapeAnalysis_Wire::CheckCurveGaps` (`ShapeAnalysis_Wire.hxx:242`)

## Description

Along the interior of one edge, the 3D curve and the lifted pcurve diverge by more than tolerance even though the endpoints agree. Senders that store both representations and recompute one without re-projecting introduce this. The defect is invisible at endpoints; only mid-curve sampling reveals it.

## Reproducer recipe

Edge whose 3D LINE goes from `(0,0,0)→(10,0,0)` straight, but whose pcurve traces a slight arc through `(5, 0.01)` in UV — endpoints match, mid-edge does not.

## Expected kernel behavior

per-edge sample-and-compare; report max divergence and parameter where it occurs. Reproject one representation onto the other or reject.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Search terms: "pcurve drift", "curve sag", "mid-edge mismatch".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
