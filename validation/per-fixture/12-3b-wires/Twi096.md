# Twi096 — Wire with internal cross-loop translates incorrectly

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi096.stp`](../../step-examples/12-3b-wires/Twi096.stp)  

## Sources
- OCCT MANTIS#0005027

## Description

A STEP wire encodes a self-crossing path: edges visit a shared vertex twice, forming a figure-eight or a small detour loop. The reader builds a wire object that conflates the two visits, dropping the second occurrence — geometry is lost.

## Reproducer recipe

Eight-edge wire whose vertex sequence is `A → B → C → D → B → E → F → A`. Vertex B appears twice (figure-eight cross). Reader collapses to a six-edge wire `A → B → C → D → E → F → A`.

## Expected kernel behavior

wire-builder allows the same vertex to appear more than once in the vertex sequence of an open or closed wire; the topology is figure-eight, not a simple cycle.

## Notes

**See also**: Twi050, Twi076.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Twi050](Twi050.md), [Twi076](Twi076.md)
