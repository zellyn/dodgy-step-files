# Twi007 — Disordered edges in wire — listed out of traversal order but reorderable

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi007.stp`](../../step-examples/12-3b-wires/Twi007.stp)  

## Sources
- 05-occt-shapefix.md F001 (FixReorder)
- 04-occt-translation.md Q024 (composite-curve r0301_db.stp)
- 08-occt-gitlog.md G080/G104 (bug 0021317, torus)
- 09-healing-menus.md

## Description

The edges in an `EDGE_LOOP` are emitted in arbitrary order — the head/tail vertices of consecutive entries do not match, but the edges as a set can be reordered into a valid head-to-tail traversal. Common with Pro/E composite-curve segments where the exporter writes edges in arbitrary slot order.

## Reproducer recipe

STEP face whose `EDGE_LOOP` lists edges in non-traversal order; vertices common but not adjacent.

## Expected kernel behavior

reorder the edges by vertex matching to recover a head-to-tail traversal; if no consistent ordering exists (some edges need flipping or no permutation works), reject as malformed.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

For STEP-disordered loops where reordering alone is insufficient and orientation flipping is also required, see Twi038. **See also**: Twi003.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000001.000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 2) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |

## Cross-references

**See also**: [Twi003](Twi003.md)
