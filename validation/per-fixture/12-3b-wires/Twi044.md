# Twi044 — Internal `FACE_BOUND` hole-wire with sub-tolerance enclosed area (degenerate tiny inner wire)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi044.stp`](../../step-examples/12-3b-wires/Twi044.stp)  

## Sources
- 05-occt-shapefix.md F054 (ShapeUpgrade_RemoveInternalWires)
- 08-occt-gitlog.md G091/G092 (FixSmallAreaWire)
- 09-healing-menus.md H060 (Suppress Internal Wires)
- 15-academic.md L001

## Description

A face carries one or more inner `FACE_BOUND` ("hole") wires whose enclosed area is below a configurable threshold relative to the face's overall area — a tiny wire that almost-encloses-nothing, typically left behind by imprecise reverse-engineering or Boolean fallout rather than describing a genuine hole. Common shape: a triangle inner wire with vertices like (5,5,0), (5.001,5,0), (5.0,5.001,0) — enclosed area ~5e-7 mm² well below typical working tolerance — sitting inside a 100 mm² planar face.

## Reproducer recipe

Planar face with a small inner wire enclosing area < 1e-6 mm² inside a 100 mm² face.

## Expected kernel behavior

drop the spurious inner wire and fill the would-be hole, keep it but accept the noise, or reject as malformed; choice is kernel-policy-dependent and may be exposed via a healing flag.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Cross-oracle: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `signal(11)` |
| `occt_heal_off` | `signal(11)` |
| `gmsh_autofix_on` | `signal(11)` |
| `gmsh_autofix_off` | `signal(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `process_signal` |

**Cross-oracle classification**: Spec-clean Part-21; OCC crashes (kernel-side bug)

## Tier-3 geometric introspection

- **load**: `None`
- **n_faces**: 0
- **n_edges_total**: None
- **n_vertices_total**: None
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **Cross-oracle**: pure-Python Part-21 validator accepts (`accept`); OCCT crashes (`signal(11)`). The crash is on the kernel side — the file is spec-conformant Part-21.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 13) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=signal(11)/signal(11) gmsh=signal(11) ifc=schema_n/a` |
