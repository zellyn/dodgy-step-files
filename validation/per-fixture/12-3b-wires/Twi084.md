# Twi084 — Closed-curve edge has different start and end vertex points

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi084.stp`](../../step-examples/12-3b-wires/Twi084.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT BRepLib_MakeEdge EdgeError.DifferentPointsOnClosedCurve (uncovered class evidence)

## Description

An `EDGE_CURVE` whose underlying `edge_geometry` is a closed curve (a circle, an ellipse, a closed B-spline) declares its start vertex and end vertex to be at *different* coordinates. The expected invariant — a closed curve's two endpoints coincide in 3D — is violated. Bug-reporter language: "closed curve has gap", "circle's endpoints don't meet", "circle edge open".

## Reproducer recipe

`EDGE_CURVE` referencing a `CIRCLE` of radius 1.0 with `edge_start = #V1` (at (1,0,0)) and `edge_end = #V2` (at (1.5,0,0)); the curve geometry is closed but the topology says otherwise.

## Expected kernel behavior

Detect via the curve's `closed` predicate vs vertex equality test; coerce vertices to coincide, or reject.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

Bug-reporter synonyms: "non-coincident closed-curve endpoints".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
