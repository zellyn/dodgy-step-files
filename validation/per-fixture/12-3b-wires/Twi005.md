# Twi005 — ORIENTED_EDGE.edge_element references non-EDGE_CURVE

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi005.stp`](../../step-examples/12-3b-wires/Twi005.stp)  

## Sources
- 04-occt-translation.md Q005

## Description

`ORIENTED_EDGE.edge_element` resolves to something other than `EDGE_CURVE` (e.g. `VERTEX_POINT`, an unrelated geometric_representation_item, or unresolved id). Dereferencing assumes EDGE_CURVE and crashes.

## Reproducer recipe

`ORIENTED_EDGE('',*,*,#5,.T.);` where `#5` is a `VERTEX_POINT` or `GEOMETRIC_CURVE_SET`.

## Expected kernel behavior

skip the oriented-edge with warning; continue translation of remaining edges.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `warn-and-proceed`

## Notes

The translator's edge-loop transfer pass must validate the dereferenced entity's type before treating it as an `EDGE_CURVE`. Validation observed: silent-empty rather than the cited crash. Without a PRODUCT chain the transfer never reaches the dereference site; with a wrap chain the receiver silently skips the bad oriented-edge. The kernel-mishandling-by-silent-acceptance still demonstrates the defect class — the bare-reader crash path is not exercised at fixture scale. **See also**: Twi004, Twi006.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Provenance / kernel-behavior annotations

- **OCC behavior**: silent-empty rather than the cited crash. Without a PRODUCT chain the transfer never reaches the dereference site; with a wrap chain the receiver silently skips the bad oriented-edge. The kernel-mishandling-by-silent-acceptance still demonstrates the defect class — the bare-reader crash path is not exercised at fixture scale.

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Twi004](Twi004.md), [Twi006](Twi006.md)
