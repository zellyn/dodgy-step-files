# Twi067 — Lacking edge: 2D gap not absorbable by vertex tolerance

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi067.stp`](../../step-examples/12-3b-wires/Twi067.stp)  

## Sources
- OCCT `ShapeAnalysis_Wire::CheckLacking` (`ShapeAnalysis_Wire.hxx:236`)

## Description

Between two consecutive edges of a wire, the pcurve endpoints differ by more than the shared vertex's tolerance can cover. The 3D coordinates may agree but in UV there is a gap that requires *insertion* of a connecting edge — not just snapping.

## Reproducer recipe

An edge ends at UV `(10, 0)`; the next edge starts at UV `(10.5, 0)` — pcurve gap 0.5 in UV space, but 3D distance is small because the host surface compresses parameter space at that location.

## Expected kernel behavior

report DONE1 (gap too large for vertex tolerance) vs DONE2 (vector across gap is anti-parallel to neighboring pcurves); insert a synthetic connecting edge or reject. The insertion's geometry must be consistent with both neighbors.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

One of the canonical "needs a degenerate seam edge" defects in periodic-surface land.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-3` (rank 3) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
