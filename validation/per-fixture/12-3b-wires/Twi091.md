# Twi091 — Inner wire on closed surface lacks natural bound: face read with no boundary

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi091.stp`](../../step-examples/12-3b-wires/Twi091.stp)  

## Sources
- OCCT MANTIS#0028414

## Description

A face on a closed surface (cylinder, sphere, torus) has a single inner wire (`FACE_INNER_BOUND`) representing a hole, but no outer bound — under STEP semantics for closed surfaces, the natural surface boundary plays the role of outer bound. STEP reader does not synthesise the natural bound and reads the face as having only an inner wire — making it ill-formed (no outer boundary at all) or interpreted as a tiny disk rather than the cylinder-minus-hole.

## Reproducer recipe

`ADVANCED_FACE` on a `CYLINDRICAL_SURFACE` with one `FACE_INNER_BOUND` (a circular wire on the cylinder representing a window cut-out) and no `FACE_OUTER_BOUND` element in the bounds list.

## Expected kernel behavior

when a face on a closed surface has no outer bound, treat the entire surface (modulo seam handling) as the implicit outer bound; the inner wire then describes a hole through the natural face.

## Notes

**See also**: Tfa038, Tsh001.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `signal(11)` |
| `occt_heal_off` | `signal(11)` |
| `gmsh_autofix_on` | `signal(11)` |
| `gmsh_autofix_off` | `signal(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `process_signal` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Spec-clean Part-21; OCC crashes (kernel-side bug)

## Tier-3 geometric introspection

- **load**: `None`
- **n_faces**: 0
- **n_edges_total**: None
- **n_vertices_total**: None
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=signal(11)/signal(11) gmsh=signal(11) ifc=schema_n/a` |

## Cross-references

**See also**: [Tfa038](Tfa038.md), [Tsh001](Tsh001.md)
