# Twi081 — Edge has multiple distinct 3D curves attached

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi081.stp`](../../step-examples/12-3b-wires/Twi081.stp)  
**Sender**: deduced — OCCT uncovered-class sweep, Pass C  

## Sources
- OCCT BRepCheck_Edge.cxx:128 (Multiple3DCurve) (uncovered class evidence)

## Description

A topological edge carries more than one 3D-curve representation (different `EDGE_CURVE.edge_geometry` references coalesced onto the same edge). The two curves disagree about where the edge lies in space. Bug-reporter language: "edge has two curves", "curve mismatch on edge", "edge geometry conflict".

## Reproducer recipe

Two distinct `EDGE_CURVE` entities sharing endpoints `#V1`, `#V2` but referencing different `LINE` or `B_SPLINE_CURVE` geometries. Both are referenced by `ORIENTED_EDGE`s in the same `EDGE_LOOP`.

## Expected kernel behavior

Choose one curve canonically and emit a warning, or reject.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, warn-and-proceed`

## Notes

Different from Twi-style pcurve disagreement; this is *3D* curve disagreement.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-10` (rank 8) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
