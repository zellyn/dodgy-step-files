# Twi079 — Internal hole wire whose enclosed area is below threshold

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi079.stp`](../../step-examples/12-3b-wires/Twi079.stp)  

## Sources
- OCCT `ShapeUpgrade_RemoveInternalWires::Perform` (`ShapeUpgrade_RemoveInternalWires.hxx:51`)

## Description

A face has one or more inner FACE_BOUND wires whose enclosed area is below a caller-specified minimum. The hole is sub-feature: a manufacturing artifact, sliver-cut residue, or import precision noise. Removing the wire enlarges the face's effective area.

## Reproducer recipe

A square face with a tiny inner triangle hole of area 5e-7 mm² (vertices within 1e-3 of each other).

## Expected kernel behavior

detect inner wires below the threshold, remove them, and re-validate the face's outer-bound topology. The threshold is caller policy.

## Notes

Search terms: "remove tiny holes", "sub-feature holes", "hole sliver".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `signal(11)` |
| `occt_heal_off` | `signal(11)` |
| `gmsh_autofix_on` | `signal(11)` |
| `gmsh_autofix_off` | `signal(11)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `process_signal` |

**Cross-oracle classification**: Spec-clean Part-21; OCC crashes (kernel-side bug)

## Tier-3 geometric introspection

- **load**: `None`
- **n_faces**: 0
- **n_edges_total**: None
- **n_vertices_total**: None
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=signal(11)/signal(11) gmsh=signal(11) ifc=schema_n/a` |
