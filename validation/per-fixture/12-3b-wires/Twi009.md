# Twi009 — Common vertex shared between two distinct wires (STEP-invalid)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi009.stp`](../../step-examples/12-3b-wires/Twi009.stp)  

## Sources
- 05-occt-shapefix.md F021 (FixSplitCommonVertex)

## Description

STEP requires that each wire of a face own its own distinct `VERTEX_POINT`s. The sender produces a face whose outer wire and inner wire reference the same `VERTEX_POINT` instance — legal in some kernel-internal BRep representations, but invalid in STEP and ambiguous to receivers expecting per-wire vertex ownership.

## Reproducer recipe

`FACE_OUTER_BOUND` and `FACE_BOUND` whose `EDGE_LOOP`s reference the same `VERTEX_POINT` `#v`.

## Expected kernel behavior

split the shared vertex into per-wire copies, or reject as malformed.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`

## Notes

**Originally also catalogued as**: Tsh038. **OCC behavior**: the validity-checker does not flag the shared `VERTEX_POINT` between distinct wires; tier-3 valid-flag should be ignored for this entry. The reader most likely silently fixes or ignores the shared vertex on import.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(14)` |
| `gmsh_autofix_off` | `shape(14)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 7
- **n_vertices_total**: 14
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 97.99999999999999 | 50000001.00000001 | 7 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 7`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-3` (rank 3) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(14) ifc=schema_n/a` |

## Cross-references


**Originally also catalogued as**: Tsh038
