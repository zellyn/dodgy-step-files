# Twi072 — Per-junction 3D gap query returns localized fault (EDGE_LOOP with missing closing edge; last ORIENTED_EDGE's end VERTEX_POINT also disagrees with its 3D LINE endpoint)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi072.stp`](../../step-examples/12-3b-wires/Twi072.stp)  

## Sources
- OCCT `ShapeAnalysis_Wire::CheckGap3d` (`ShapeAnalysis_Wire.hxx:340`)

## Description

Targeted single-junction 3D gap query. Caller specifies `num`; analyzer reports whether the gap between edge `num-1` and edge `num` exceeds working precision and the actual distance. Used by interactive healing tools to focus on one defect at a time.

## Reproducer recipe

As Twi053 but the caller queries junction 4 specifically.

## Expected kernel behavior

O(1) check returning OK/DONE/FAIL plus the gap value.

## Notes

Per-junction counterpart to Twi068 (whole-wire).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `reject` |
| `gmsh_autofix_off` | `reject` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 3
- **n_vertices_total**: 6
- **brepcheck.valid**: `False`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 99.99999999999999 | 50000000.99999999 | 3 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 3`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=reject ifc=schema_n/a` |
