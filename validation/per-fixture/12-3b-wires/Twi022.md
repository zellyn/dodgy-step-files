# Twi022 — Seam edge with swapped or duplicated pcurves on a periodic face

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi022.stp`](../../step-examples/12-3b-wires/Twi022.stp)  

## Sources
- 05-occt-shapefix.md F042 (FixSeam)
- 04-occt-translation.md Q032 (seam curve with same pcurve twice)

## Description

A seam edge on a periodic face is supposed to carry two pcurves — one on the U=0 side and one on the U=period side. The input either swaps the two pcurves (each is on the wrong side) or assigns the same pcurve to both slots. The wire fails to close in UV: a step of one full period that the seam edge should take in parameter space is missing.

## Reproducer recipe

`SEAM_CURVE` referencing the same `PCURVE` for both `associated_geometry` slots.

## Expected kernel behavior

detect the missing period-shift and either shift one pcurve by the surface period to restore UV closure, swap the two pcurves to their correct sides, or reject as malformed.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

**See also**: Gp011, Gp012.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(4)` |
| `gmsh_autofix_off` | `shape(4)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `empty_mesh` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 2
- **n_vertices_total**: 4
- **brepcheck.valid**: `False`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | cylinder | 0.0 | 3.9999998800000043 | 2 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 2`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-10` (rank 4) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(4) ifc=schema_n/a` |

## Cross-references

**See also**: [Gp011](Gp011.md), [Gp012](Gp012.md)
