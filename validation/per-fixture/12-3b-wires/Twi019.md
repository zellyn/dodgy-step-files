# Twi019 — Closed edges need splitting at parametric break

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi019.stp`](../../step-examples/12-3b-wires/Twi019.stp)  

## Sources
- 05-occt-shapefix.md F050 (ShapeUpgrade_ClosedEdgeDivide)
- 09-healing-menus.md H053 (SplitClosedFaces)

## Description

An `EDGE_CURVE` whose underlying curve is closed (full circle / ellipse) is used as a single edge — the curve goes around 360° and the edge's start vertex equals its end vertex. Downstream tools that expect every edge to be a topologically open arc (with two distinct endpoints) require the edge to be split at a seam parameter or a natural break.

## Reproducer recipe

Edge wraps full 360° on cylindrical face without explicit seam; receiver needs to split into two half-edges.

## Expected kernel behavior

split the closed edge at the seam parameter (u=0 / u=period) into two open arcs, or accept a single closed edge as a kernel-internal representation. Choice is design-dependent; consumers of the result must agree on the convention.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**Originally also catalogued as**: Tfa029.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(6)` |
| `gmsh_autofix_off` | `shape(6)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | cylinder | 314.1592653589793 | 3.9999998800000043 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].surface_type == "cylinder"`
- `n_edges_total >= 4`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(6) ifc=schema_n/a` |

## Cross-references


**Originally also catalogued as**: Tfa029
