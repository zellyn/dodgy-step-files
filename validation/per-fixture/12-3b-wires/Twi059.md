# Twi059 — Vertex 3D point and edge 3D-curve endpoint disagree beyond tolerance

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.3b (`12-3b-wires`)  
**Fixture**: [`step-examples/12-3b-wires/Twi059.stp`](../../step-examples/12-3b-wires/Twi059.stp)  

## Sources
- OCCT `ShapeAnalysis_Edge::CheckVerticesWithCurve3d` (`ShapeAnalysis_Edge.hxx:156`)

## Description

An edge's start (or end) VERTEX_POINT lies further than the vertex's tolerance from where the 3D curve's first (or last) parameter evaluates. The edge is "broken" — geometric path doesn't match the topological endpoint.

## Reproducer recipe

VERTEX_POINT at `(10.005, 10, 0)` paired with a 3D LINE that evaluates to `(10, 10, 0)` at the edge's last parameter; declared vertex tolerance 1e-6.

## Expected kernel behavior

report which vertex (start, end, or both) and the actual distance; let the caller decide whether to inflate vertex tolerance, snap the vertex, or reject the edge.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Diagnostic-only check (`Check*` not `Fix*`); fix counterpart is FixVertexTolerance (Twi048). **See also**: Gp038.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 3 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 3 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 3 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |

## Cross-references

**See also**: [Gp038](Gp038.md)
