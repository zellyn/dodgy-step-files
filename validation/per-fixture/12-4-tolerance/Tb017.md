# Tb017 — Two coincident faces at distance equal to declared tolerance

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/Tb017.stp`](../../step-examples/12-4-tolerance/Tb017.stp)  

## Sources
- synthesized time-bomb (Pass F).

## Description

Two `ADVANCED_FACE`s (planar, parallel, same outline) are separated by exactly 1.0e-6 mm — equal to the declared tolerance. A kernel comparing strict `<` keeps them as separate faces (a thin slab). A kernel comparing `<=` merges them and the slab disappears, leaving a single face. Both interpretations are mathematically reasonable but produce different geometry.

## Reproducer recipe

Two coplanar `ADVANCED_FACE`s on parallel `PLANE`s offset by 1.0e-6 mm; declared uncertainty 1.0e-6 mm. Wrap in PRODUCT chain.

## Expected kernel behavior

equality-vs-strict-inequality at the tolerance boundary must be specified; tie-breaking documented.

## Notes

Searchable as "duplicate coincident faces", "thin slab disappeared after import".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(18)` |
| `gmsh_autofix_off` | `shape(18)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 2
- **n_edges_total**: 8
- **n_vertices_total**: 16
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 1.0 | 5000000.999999998 | 4 |
| 1 | plane | 1.0 | 5000001.000000001 | 4 |

## Tier-3 assertions (machine-checked)

- `n_edges_total >= 8`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(18) ifc=schema_n/a` |
