# Tb011 — Periodic-surface seam matching requires period-relative tolerance

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/Tb011.stp`](../../step-examples/12-4-tolerance/Tb011.stp)  

## Sources
- synthesized time-bomb (Pass F).

## Description

A `CYLINDRICAL_SURFACE` of radius 1e6 mm has period 2π·R ≈ 6.28e6. A wire's pcurve U-coordinates differ by `2π + 1e-3` between consecutive edges (one period plus a small drift). A kernel using *fixed* parametric tolerance (e.g. 1e-7 rad) rejects the seam as misaligned. A kernel using `fmod(diff, 2π)` semantics absorbs the period and reports alignment within 1e-3. The same wire is closed or not depending on whose arithmetic policy.

## Reproducer recipe

`CYLINDRICAL_SURFACE` with R = 1e6 mm; a `FACE_OUTER_BOUND` whose two consecutive `EDGE_CURVE`s have pcurve endpoints differing by exactly `2π + 1e-3` rad in U. Wrap in PRODUCT chain.

## Expected kernel behavior

seam-matching policy must be documented and consistent; on a periodic surface, "same modulo period" must be the canonical equality.

## Notes

Searchable as "cylinder seam not closed", "huge cylinder broken pcurve". **See also**: Twi-XX (the "wire closed in 3D, open in UV" entry).

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 2 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
