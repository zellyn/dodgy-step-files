# N005 — Splitting a closed periodic face leaves new edges with reversed pcurve vs 3D curve parameterisation

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/N005.stp`](../../step-examples/12-4-tolerance/N005.stp)  

## Sources
- 13-quaoar B008

## Description

Splitting a closed periodic shape (full `CYLINDRICAL_SURFACE`, `TOROIDAL_SURFACE`) along its seam produces new edges whose 3D-curve and pcurve parameterisations no longer match — they were inherited from the unsplit edge but split independently. Common signature: a seam `EDGE_CURVE` on a cylinder with a 3D `LINE` from z=0 to z=10 (sense `.T.`) but its pcurve running from v=10 down to v=0 along −V (the `EDGE_CURVE` sense or pcurve direction inverted, e.g. `.F.`) — pcurve and 3D curve have opposite parameterisations across the seam. A topology checker reports invalid edges until a follow-up same-parameter recomputation runs.

## Reproducer recipe

Full cylinder solid → split along seam → run topology check; observe invalid edges. Recompute same-parameter; check passes.

## Expected kernel behavior

a splitter that produces new edges must restore the same-parameter invariant on those edges before returning, or document that callers must do so. Returning a topologically-invalid intermediate result puts the burden on every caller.

## Notes

**See also**: N006.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 20) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [N006](N006.md)
