# Tb015 — Sliver edge length at sqrt(tol) — under squared comparison or linear?

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/Tb015.stp`](../../step-examples/12-4-tolerance/Tb015.stp)  

## Sources
- synthesized time-bomb (Pass F).

## Description

An edge has 3D length 1e-3 mm in a file with declared tolerance 1e-6. A kernel comparing `length < tol` says "alive". A kernel comparing `length < sqrt(tol) ≈ 1e-3` says "right at the boundary — ambiguous". A kernel comparing `length*length < tol` (≡ `length < sqrt(tol)`) says the same boundary. The triple ambiguity (linear, squared, square-root) means three reasonable kernels can disagree on whether the edge survives heal.

## Reproducer recipe

`EDGE_CURVE` of 3D length 1e-3 mm in a wrapped shell representation with declared uncertainty 1e-6.

## Expected kernel behavior

comparison policy must be documented; documentation should specify whether thresholds are linear, squared, or square-rooted.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
