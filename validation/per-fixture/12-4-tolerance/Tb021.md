# Tb021 — Multiple UNCERTAINTY entries with conflicting values, no labels

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/Tb021.stp`](../../step-examples/12-4-tolerance/Tb021.stp)  

## Sources
- synthesized time-bomb (Pass F).

## Description

A `GEOMETRIC_REPRESENTATION_CONTEXT` lists three `UNCERTAINTY_MEASURE_WITH_UNIT`s with values 1e-3, 1e-6, 1e-9 and no distinguishing `name` field. The spec allows the list. A kernel taking `min` (most strict) uses 1e-9. A kernel taking `max` (most lax) uses 1e-3. A kernel taking the first uses 1e-3. A kernel taking the last uses 1e-9. Same file, four different working tolerances depending on receiver policy. All other tolerance-boundary defects in this section are amplified by this ambiguity.

## Reproducer recipe

PRODUCT-wrapped simple shape; context lists three unlabelled `UNCERTAINTY_MEASURE_WITH_UNIT`s with values 1e-3, 1e-6, 1e-9.

## Expected kernel behavior

multiple uncertainties must be either resolved by a documented selection policy (min/max/labelled) or rejected as ambiguous.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Searchable as "multiple uncertainty values STEP", "tolerance ambiguity in context".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
