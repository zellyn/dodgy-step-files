# N043 — Shape-to-shape distance reports nonzero gap for clearly intersecting parts

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/N043.stp`](../../step-examples/12-4-tolerance/N043.stp)  

## Sources
- 18-other-issues I056 (OCCT #1123, FreeCAD #25158)

## Description

For two shapes that demonstrably intersect (their volumes clearly overlap), the minimum-distance query returns a small but nonzero value (e.g. 6e-5 or 0.083) instead of zero. The distance algorithm samples corner points and edge endpoints but does not verify true intersection, so a transverse intersection between two interior surfaces is missed.

## Reproducer recipe

4 cylinders intersecting a main part; compute pairwise minimum distance.

## Expected kernel behavior

a minimum-distance query between two intersecting bodies must return zero (or a value smaller than the kernel's working precision); when sampling reports a near-zero distance, the kernel should fall back to a true intersection test rather than reporting the sample value.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-10` (rank 5) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
