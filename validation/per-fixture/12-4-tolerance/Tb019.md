# Tb019 — UNCERTAINTY value of `1.0E-30` — below any sane working precision

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/Tb019.stp`](../../step-examples/12-4-tolerance/Tb019.stp)  

## Sources
- synthesized time-bomb (Pass F).

## Description

`UNCERTAINTY_MEASURE_WITH_UNIT(LENGTH_MEASURE(1.0E-30), ...)`. The value is below any reasonable kernel's working precision. A kernel that respects the declared value tries to compare features at 1e-30 precision and rejects everything as having gaps. A kernel that floors at `Precision::Confusion()` silently uses 1e-7 — the file imports cleanly but its declared expectation has been overridden. Producer and receiver silently disagree about precision.

## Reproducer recipe

PRODUCT-wrapped simple shape with `UNCERTAINTY_MEASURE_WITH_UNIT(LENGTH_MEASURE(1.0E-30), ...)`.

## Expected kernel behavior

tolerance values outside the kernel's documented operating range must be flagged; either coerce-and-warn or reject-with-diagnostic — never silent substitution.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject, heal`
- disallowed: `silent-accept`

## Notes

**See also**: Tb018, Tb020. **OCC behavior**: silently substitutes the floored working-precision value for the absurdly-tight declared uncertainty; kernel mishandling — the catalog above forbids silent substitution.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Tb018](Tb018.md), [Tb020](Tb020.md)
