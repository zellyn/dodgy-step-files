# N018 — `GEOMETRIC_REPRESENTATION_CONTEXT` missing `GLOBAL_UNCERTAINTY_ASSIGNED_CONTEXT` leaf → fall back to read.precision

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/N018.stp`](../../step-examples/12-4-tolerance/N018.stp)  

## Sources
- 04-occt-translation Q043, Q044

## Description

When a STEP file's `GEOMETRIC_REPRESENTATION_CONTEXT` lacks the `GLOBAL_UNCERTAINTY_ASSIGNED_CONTEXT` leaf — for example carries only `GLOBAL_UNIT_ASSIGNED_CONTEXT` and no `UNCERTAINTY_MEASURE_WITH_UNIT` for the model — OCCT silently falls back to the `read.precision.val` parameter. Models from such files all share whatever precision the user happened to configure, regardless of source.

## Reproducer recipe

STEP file lacking `*_AND_GLOBAL_UNCERTAINTY_ASSIGNED_CONTEXT` complex entity; reference only bare `geometric_representation_context`.

## Expected kernel behavior

heal — emit explicit "no uncertainty context" diagnostic; require user to confirm fallback; do not silently default.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`
- disallowed: `silent-accept`

## Notes

**Originally also catalogued as**: U019. **See also**: U025. **OCC behavior**: silently falls back to the configured working precision with no diagnostic; kernel mishandling — the catalog above forbids silent default.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `beyond-10` (rank 11) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [U025](U025.md)

**Originally also catalogued as**: U019
