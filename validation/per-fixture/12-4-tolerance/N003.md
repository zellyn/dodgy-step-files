# N003 — Same-domain face merge inflates shared-vertex tolerance to worst input

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/N003.stp`](../../step-examples/12-4-tolerance/N003.stp)  

## Sources
- 03-occt-tests R030 (OCC26642, OCC26656)

## Description

When two adjacent faces with the same supporting surface are merged into one (their shared edge is dissolved), the resulting shared vertex inherits a tolerance equal to the worst-case vertex among the merged inputs — e.g. `1e-1` even though one input vertex was `2e-7`. Downstream algorithms then treat points as fuzzy-equal where they were previously precise.

## Reproducer recipe

Two coplanar faces with vertex tolerance 2e-7 and one vertex with tolerance 1e-1 (from prior healing). Apply same-domain face merging.

## Expected kernel behavior

recompute tolerance from actual post-merge geometric deviation rather than aggregating the worst input. A merge that takes a previously-precise vertex and balloons its tolerance is a fidelity loss.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: A019, N040.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-10` (rank 6) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [A019](A019.md), [N040](N040.md)
