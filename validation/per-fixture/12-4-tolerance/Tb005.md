# Tb005 — Face area sub-tolerance squared (`tol*tol` threshold defect)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/Tb005.stp`](../../step-examples/12-4-tolerance/Tb005.stp)  

## Sources
- synthesized time-bomb (Pass F).

## Description

Many kernel algorithms compare *areas* to `tol*tol` rather than `tol`. A planar face with side length 3e-4 mm has area 9e-8 mm². The file declares `tol = 1e-4`; a kernel that filters degenerate faces with `area < tol*tol = 1e-8` keeps it. If the kernel later uses `tol = 1e-3` (for example, after a heal that bumped tolerance), the threshold becomes `tol*tol = 1e-6`, and the face is now sub-threshold and silently dropped. A face that survives one Boolean pass disappears in the next.

## Reproducer recipe

A small planar `ADVANCED_FACE` (3e-4 × 3e-4 mm) on a `PLANE`, wrapped in a closed shell so OCCT can transfer; declared uncertainty 1e-4. Wrap in PRODUCT chain so tier-3 can compute area.

## Expected kernel behavior

face-area thresholds should be derived from a documented invariant (e.g. minimum-feature-size) rather than `tol*tol`; a face that is non-degenerate at one tolerance must remain non-degenerate at any other tolerance within the operating range. Silent drop of an "infinitesimal" face is unacceptable; reject loudly or preserve the face.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Searchable as "face disappeared after Boolean", "small feature silently removed", "ShapeFix dropped my face".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `shape(1)` |
| `occt_heal_off` | `shape(1)` |
| `gmsh_autofix_on` | `shape(9)` |
| `gmsh_autofix_off` | `shape(9)` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `not_manifold` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 1
- **n_edges_total**: 4
- **n_vertices_total**: 8
- **brepcheck.valid**: `True`

Face details (first 3):

| i | type | area | sliver_aspect | edges |
|---|---|---|---|---|
| 0 | plane | 8.999999999999999e-08 | 1501.0 | 4 |

## Tier-3 assertions (machine-checked)

- `face[0].area < 1e-6`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | `detected` (3 attempts) |
| Status | `pending` |
| Expected validation | `occt=shape(1)/shape(1) gmsh=shape(9) ifc=schema_n/a` |
