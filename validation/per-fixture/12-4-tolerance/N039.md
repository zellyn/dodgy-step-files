# N039 — Healing pass over-eagerly inflates tolerances 1000× on sub-shapes that needed no fix (post-fix `UNCERTAINTY_MEASURE_WITH_UNIT` bloat)

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/N039.stp`](../../step-examples/12-4-tolerance/N039.stp)  

## Sources
- 08-occt-gitlog G093 (Mantis 0028601)
- 03-occt-tests R031 (OCC24658)

## Description

A healing pass touches every sub-shape and inflates edge/vertex tolerances even on those whose existing tolerance already covers the actual deviation. Common file signature: `GLOBAL_UNCERTAINTY_ASSIGNED_CONTEXT` carries two `UNCERTAINTY_MEASURE_WITH_UNIT` entries — a native 1e-6 mm precision and a post-fix "over-bumped" 1e-3 mm — a 1000× bump applied across the whole model after a healing pass on two consecutive edges sharing a single vertex. Combined with an in-place mutation mode, the input shape itself is silently bloated. The user-visible symptom is a healed model whose tolerance discs grew across every feature, even features that were valid to begin with.

## Reproducer recipe

Pass a shape whose deviations are all within current tolerance through a full healing pass. Compare per-vertex tolerances before and after.

## Expected kernel behavior

only inflate tolerance when the actual deviation requires it; default to non-mutating (copy-on-write) input handling.

## Notes

**See also**: N040.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `not-found` (rank n/a) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [N040](N040.md)
