# Tb010 — Self-intersection that vanishes when round-tripped through float32

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/Tb010.stp`](../../step-examples/12-4-tolerance/Tb010.stp)  

## Sources
- synthesized time-bomb (Pass F).

## Description

Variant of Tb009 where the rounding is done by the format-converter step (e.g. STEP → STL → STEP through a tool that uses `float` not `double`). Coordinates `(0, 0, 0)`, `(1.0000003, 0, 0)`, `(2.0000007, 0, 0)`, `(0.9999997, 0.0000003, 0)` form a near-degenerate quad whose diagonals cross. After float32 round-trip the coordinates collapse to `(0, 0, 0)`, `(1, 0, 0)`, `(2, 0, 0)`, `(1, 0, 0)` — the diagonals no longer cross because the duplicate point makes one edge zero-length. The "fix" was unintentional precision loss.

## Reproducer recipe

Quad face with above coordinates exactly; wrap in PRODUCT chain. The fixture itself uses double precision so the self-intersection is real; the time bomb is documented in the comment as "input was generated by a float32-precision producer".

## Expected kernel behavior

result must be invariant under coordinate precision changes within the operating range; if not invariant, the kernel must flag the precision sensitivity.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
