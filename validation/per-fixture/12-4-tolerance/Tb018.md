# Tb018 — UNCERTAINTY value of exactly zero — no fuzzy compare

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/Tb018.stp`](../../step-examples/12-4-tolerance/Tb018.stp)  

## Sources
- synthesized time-bomb (Pass F).

## Description

`UNCERTAINTY_MEASURE_WITH_UNIT(LENGTH_MEASURE(0.0), ...)` declares zero tolerance. A receiver that uses the declared value verbatim performs exact-equality comparisons — any sub-bit drift is a "real" defect. A receiver that floors the working tolerance at `Precision::Confusion()` silently substitutes 1e-7 and behaves identically to a normal file. The zero declaration is a producer's request for exact arithmetic that may or may not be honoured.

## Reproducer recipe

A simple shape (planar quad or single edge) in a PRODUCT-wrapped representation with `UNCERTAINTY_MEASURE_WITH_UNIT(LENGTH_MEASURE(0.0), ...)`.

## Expected kernel behavior

zero uncertainty must be either honoured (exact arithmetic enabled if available) or rejected; silently substituting a non-zero working tolerance must be flagged in diagnostics.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Searchable as "zero tolerance file", "exact arithmetic STEP".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
