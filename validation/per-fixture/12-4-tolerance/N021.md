# N021 — VRML/STEP origin convention mismatch within same producer

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/N021.stp`](../../step-examples/12-4-tolerance/N021.stp)  
**Sender**: KiCad pcbnew (VRML vs STEP exporters)  

## Sources
- 22-niche-forums Y002 (KiCad #23235)

## Description

KiCad VRML export places Z=0 at the board *center*; STEP export places Z=0 at the board *bottom*. Downstream tools mixing the two formats see components shifted by half-board-thickness, even though both come from the same producer.

## Reproducer recipe

For a 1.6 mm board, export both VRML and STEP from the same KiCad project. Inspect the bounding box and assembly origin in each.

## Expected kernel behavior

heal — when a producer with known origin-convention drift is detected (via `FILE_DESCRIPTION` heuristics), warn and offer re-anchoring.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal, warn-and-proceed`

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `ambiguous` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
