# N045 — Vertex tolerance under-checked: edge-internal intersection ignored

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/N045.stp`](../../step-examples/12-4-tolerance/N045.stp)  

## Sources
- OCCT MANTIS#0024922

## Description

Two edges intersect inside their parameter range but the intersection point is within vertex tolerance of one edge's endpoint. A wire-intersection check filters out the intersection because the vertex "owns" that point — leaving an undetected interior intersection that invalidates the wire downstream.

## Reproducer recipe

Two edges meeting at a `T`-junction: one edge ends at vertex V; the other passes through V's tolerance ball but does not end at V. Edge-intersection check returns "no intersection" because the point is inside V's tolerance.

## Expected kernel behavior

edge-intersection report "intersection inside vertex tolerance" as a distinct status; do not silently filter.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- disallowed: `silent-accept`

## Notes

**See also**: Twi049, N039. **OCC behavior**: silently filters out the edge-internal intersection with no diagnostic; kernel mishandling — the catalog above forbids silent filtering.

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `does-not-demonstrate` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Twi049](Twi049.md), [N039](N039.md)
