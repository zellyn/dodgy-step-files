# Tb002 — Sub-tolerance vertex gap that closes at 1e-7 but inflates at 1e-3

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/Tb002.stp`](../../step-examples/12-4-tolerance/Tb002.stp)  

## Sources
- synthesized time-bomb (Pass F).

## Description

Inverse of Tb001. Vertices are 5e-5 mm apart but the file declares `1e-7` uncertainty. A 1e-7 receiver treats them as separate (which the input is honestly representing). A receiver that loosens tolerance to 1e-3 — for example a downstream Boolean engine that uses `tol*1000` for fuzzy operations — folds them together and silently changes the topology (shared edge becomes single edge, two faces become one). The "fix" applied by the lax tolerance receiver actively corrupts the input.

## Reproducer recipe

Two `VERTEX_POINT`s at (0,0,0) and (5e-5,0,0) on separate `EDGE_CURVE`s; `UNCERTAINTY_MEASURE_WITH_UNIT` is 1e-7. Wrap in PRODUCT chain.

## Expected kernel behavior

produce a consistent result regardless of tolerance setting; if the kernel relaxes tolerance for any operation, it must flag the topology change and not silently merge entities the input declared distinct.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `heal`

## Notes

**See also**: Tb001. Searchable as "Boolean op silently merged separate features", "two edges became one after import".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Tb001](Tb001.md)
