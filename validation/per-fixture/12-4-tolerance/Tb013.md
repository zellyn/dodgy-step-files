# Tb013 — Coordinate at 1e-300 tests as "near zero" only under denormalized math

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/Tb013.stp`](../../step-examples/12-4-tolerance/Tb013.stp)  

## Sources
- synthesized time-bomb (Pass F).

## Description

A `CARTESIAN_POINT` has coordinate `(1.0E-300, 0, 0)` — a denormalized double. A kernel comparing `|x| < tol` for tol=1e-7 says "yes, near zero". A kernel comparing `x*x < tol*tol = 1e-14` says `1e-600` which underflows to 0, also "yes, near zero". A kernel that flushes-to-zero in its math library may treat `1e-300` as exactly zero before any tolerance compare. A kernel without flush-to-zero may keep the value alive and trip on `1.0/(1e-300)` overflow elsewhere. The same input behaves differently depending on the host's float environment.

## Reproducer recipe

`CARTESIAN_POINT('escape',(1.0E-300,0.0,0.0))` used as one vertex of a degenerate edge in a wrapped face; declared tolerance 1e-7.

## Expected kernel behavior

subnormal/denormal coordinates must be rejected or normalized to a documented sentinel before downstream math; the "near zero" classification must be defined explicitly.

**Structured outcomes** (extracted from prose; for downstream auto-grading):
- allowed: `reject`

## Notes

Searchable as "NaN after import", "kernel crash on tiny coordinate", "underflow during transform".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

- **load**: `ok`
- **n_faces**: 0
- **n_edges_total**: 0
- **n_vertices_total**: 0
- **brepcheck.valid**: `None`

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |
