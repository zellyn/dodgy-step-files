# Tb009 — Self-intersection visible at full precision but rounded out at receiver tolerance

<!-- Auto-generated; edit `STEP_PROBLEM_CATALOG.md` instead. -->

**Section**: §12.4 (`12-4-tolerance`)  
**Fixture**: [`step-examples/12-4-tolerance/Tb009.stp`](../../step-examples/12-4-tolerance/Tb009.stp)  

## Sources
- synthesized time-bomb (Pass F).

## Description

A wire's two consecutive edges cross at a separation of 3e-7 mm (well-defined and detectable by an exact-arithmetic kernel). A receiver that rounds coordinates to its working precision (1e-6) before intersection-testing sees the two edges as touching at a single point and reports the wire as simple. An exact kernel, or one with `tol < 3e-7`, reports a self-intersection. Two kernels disagree on whether the input is malformed.

## Reproducer recipe

Two short B-spline edges crossing in 3D at separation 3e-7 mm; declared `UNCERTAINTY_MEASURE_WITH_UNIT` is 1e-6 (looser than the crossing). Wrap in a face with the crossing wire as `FACE_OUTER_BOUND` and a PRODUCT chain.

## Expected kernel behavior

self-intersection detection must use a documented threshold, not the working precision; if the threshold is the working precision, the kernel must warn that "self-intersection within working precision" is the same input class as "no self-intersection".

## Notes

**See also**: Twi045. Searchable as "wire ok in one tool, self-intersecting in another", "rounded coordinates fixed self-intersection".

## Live oracle output

| Oracle | Verdict |
|---|---|
| `occt_heal_on` | `empty` |
| `occt_heal_off` | `empty` |
| `gmsh_autofix_on` | `empty` |
| `gmsh_autofix_off` | `empty` |
| `ifcopenshell` | `schema_n/a` |
| `part21_strict` | `accept` |
| `manifold` | `no_shapes_loaded` |
| `occt_diag_on` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `occt_diag_off` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |
| `gmsh_diag` | 1:**** ERR StepFile : Incorrect Syntax : Fails Count : 1 **** |

**Cross-oracle classification**: Pure-Python and OCC agree

## Tier-3 geometric introspection

*(tier-3 not run — fixture didn't load or wasn't introspected)*

## Confidence signals

| Signal | Result |
|---|---|
| Forward adversarial review | `demonstrates` |
| Reverse self-evidence | `top-1` (rank 1) |
| Mutation sensitivity | not tested |
| Status | `pending` |
| Expected validation | `occt=empty/empty gmsh=empty ifc=schema_n/a` |

## Cross-references

**See also**: [Twi045](Twi045.md)
